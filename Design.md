# FkeMark 产品设计与系统策划方案

## 一、 产品定位与核心理念

- **产品名称**：FkeMark
- **一句话定义**：一款无数据库、文件系统优先、高颜值的极简 Markdown 混合即时渲染编辑器。
- **核心理念**：
  - **文件即数据**：软件不建立任何私有数据库或索引。双击 `.md` 文件即开即写，关闭即走。
  - **极简视觉**：全面承袭 Tolaria 的扁平化（Flat UI）、无缝窗口（Borderless）以及优雅的现代冷色调排版。
  - **所见即所得**：光标所在处显示 Markdown 源码，光标移开即刻渲染为精美排版，杜绝传统分屏编辑的割裂感。

---

## 二、 功能规划与重要等级 (Priority Matrix)

我们将 FkeMark 的功能模块按重要程度（P0 核心、P1 体验、P2 完善）进行划分：

| 模块名称 | 功能描述 | 重要等级 | 评级理由 |
|---|---|---|---|
| **混合即时渲染引擎** | 核心编辑区，支持光标处显示 Markdown 源码，失焦后自动无缝渲染。 | **P0 (核心命脉)** | 决定产品是否具备“Typora 体验”的底层基础。 |
| **本地文件关联启动** | 在系统注册 `.md` 后缀关联，支持双击直接由 FkeMark 极速唤起并加载内容。 | **P0 (核心命脉)** | 区分“本地编辑器”与“云端/知识库软件”的核心特征。 |
| **极简本地文件树** | 侧边栏支持导入、浏览本地任意文件夹，展示纯净的文件目录结构。 | **P0 (核心命脉)** | 提供最基础的单机项目/多文件管理能力。 |
| **自动保存与防丢机制** | 停止输入 300 毫秒后自动触发后台增量保存，防止系统崩溃导致数据丢失。 | **P0 (核心命脉)** | 生产力工具最基本的安全防线。 |
| **TOC 动态大纲** | 自动提取文档中的 H1-H6 标题，在侧边栏生成目录，支持点击锚点平滑跳转。 | **P1 (体验升级)** | 极大提升阅读和长文编写时的定位效率。 |
| **Tolaria 风格斜杠命令** | 空行输入 `/` 唤起悬浮菜单，快速插入表格、代码块、公式等复杂组件。 | **P1 (体验升级)** | 继承 Tolaria 的高效率交互基因。 |
| **代码高亮与公式渲染** | 集成语法高亮（Prism/Shiki）与数学公式（KaTeX）实时预览。 | **P1 (体验升级)** | 满足技术人员、学者写技术文档的核心诉求。 |
| **多格式导出** | 支持一键将当前文档导出为 PDF、带内联样式的 HTML。 | **P2 (外围完善)** | 闭环分享、分发文档的最后一步。 |
| **无干扰专心模式** | 支持打字机模式（光标始终居中）和聚焦模式（非当前段落变淡）。 | **P2 (外围完善)** | 打磨写作者沉浸感的加分体验。 |

---

## 三、 系统架构设计

FkeMark 采用 **Tauri + React + TypeScript + ProseMirror** 的技术栈，其前后端职责与数据流向设计如下：

```
+---------------------------------------------------------------------------------+
|                                 前端 WebView (React)                             |
|                                                                                 |
|   +-----------------------+    User Typing     +----------------------------+   |
|   |  TipTap / ProseMirror | -----------------> |  TyporaActiveSyntaxPlugin  |   |
|   +-----------------------+                    +----------------------------+   |
|               ^                                              |                  |
|       Render  |                                              | Debounce Save    |
|               |  (动态计算光标位置与 Decorator)               v                  |
|   +-----------------------+                    +----------------------------+   |
|   |   富文本编辑画布      | <----------------- |   序列化为纯 Markdown 文本 |   |
|   +-----------------------+                    +----------------------------+   |
|   | TOC 大纲 / 侧边文件树 |                                  |                  |
|   +-----------------------+                                  | IPC Call         |
+--------------------------------------------------------------|------------------+
                                                               v
+---------------------------------------------------------------------------------+
|                                 Rust 核心 (Tauri)                               |
|                                                                                 |
|   +-----------------------+                    +----------------------------+   |
|   |  双击关联 / 命令行启动|                    | 异步非阻塞 File System I/O  |   |
|   | (提取 OS 传入文件路径)|                    |  (安全写入到本地物理磁盘)  |   |
|   +-----------------------+                    +----------------------------+   |
+---------------------------------------------------------------------------------+
```

### 1. 前端渲染层 (WebView)

- **富文本状态维护**：基于 ProseMirror 的 Schema，将整个文档看作单一连续文本流，不使用 BlockNote 的“硬性行块”设计。
- **实时转译**：利用 `@tiptap/markdown` 扩展，实时在内存中将 Markdown 字符转换为 ProseMirror 语法树（AST）。

### 2. 后端控制层 (Tauri/Rust)

- **零数据库设计**：彻底放弃任何本地数据库（如 SQLite）或复杂索引（如 Vector DB），所有配置以本地 JSON 文件形式保存在用户的 `AppData` / `Application Support` 中。
- **文件系统直连**：利用 Rust 的 `tokio::fs` 进行文件异步读取与写入，防止编辑大文件时界面出现卡顿。

---

## 四、 核心编辑逻辑与 WYSIWYG 实现机制

要实现 Typora 级别的即时渲染，其核心编辑逻辑不是“实时将 Markdown 字符串渲染成 HTML”，而是“在富文本 DOM 树中，根据光标动态插入/隐藏 Markdown 符号的装饰器（Decoration）”。

### 1. 光标追踪与符号显隐逻辑

- **初始化状态**：
  当用户输入 `## 这是一个二级标题` 后，ProseMirror 会将其解析为 `heading (level=2)` 节点。默认在界面上，前面的 `##` 标记不占位、不显示。
- **聚焦（Focus）阶段**：
  当用户点击或将光标移动到该标题内部时，插件监听到光标 Selection 变化：
  1. 计算当前光标在 AST 中的 Node 位置。
  2. 如果处于 `heading` 节点，使用 `Decoration.widget` 在该行头部动态插入一个包含 `##` 字符的虚拟 `span`（带有 `.md-delimiter` 类名）。
  3. 如果在加粗或斜体标记范围内部，在文本内容前后分别插入对应的 Markdown 定界符虚拟字符（如 `**`）。
- **失焦（Blur）阶段**：
  当光标离开当前行或当前 Mark 标记范围时，重新触发 Plugin 的 `apply` 方法，清空该节点上的所有 Widget Decoration，界面瞬间恢复为纯净的排版状态。

### 2. 连续排版平滑过渡技术

为了避免频繁插入和删除 DOM 节点导致光标跳动（Layout Jitter），在 CSS 中对 `.md-delimiter` 进行了过渡动画设计（使用 `max-width` 结合 `opacity`，避免直接切换 `display: none`），使其在 150 毫秒内平滑淡入淡出，保持舒适自然的视觉流动感。

---

## 五、 界面设计与视觉规范 (UI Spec)

FkeMark 整体界面完全向 **Tolaria** 的克制、超扁平设计看齐。

### 1. 颜色与主题规范

提供 Light（暖白纸质感）和 Dark（极客冷暗调）两种原生主题：

- **Light Theme**：
  - 主背景色：`#FAF9F6`（极浅米白，接近纸张质感，防止刺眼）
  - 侧边栏背景：`#F2F1EC`
  - 主文字：`#2D3748`
  - Markdown 标记符号（如 `#`, `*`）：`#A0AEC0`（淡灰色，降低视觉噪点）
  - 品牌强调色（Focus/光标）：`#4A5568`
- **Dark Theme**：
  - 主背景色：`#121212`（纯黑偏暖）
  - 侧边栏背景：`#1A1A1A`
  - 主文字：`#E2E8F0`
  - Markdown 标记符号：`#4A5568`
  - 品牌强调色：`#CBD5E0`

### 2. 界面布局与线框规范

窗口取消传统的操作系统标题栏，采用**融合一体式窗口（Borderless Window）**。

- **左侧导航栏（宽度 240px）**：
  - 无边框线，通过背景色微弱的明暗对比划分边界。
  - 顶部为“文件树”，采用 Tolaria 标志性的扁平无图标列表。
  - 底部为“TOC 目录”，仅在鼠标悬停在左侧边缘时渐显，平时保持低调。
- **核心编辑区（宽度自适应，内容最大行宽限制 800px）**：
  - 居中对齐，留白呼吸感极强。
  - 顶部正中显示当前文件名，旁边带有一个克制的“未保存修改”小圆点。
- **斜杠命令悬浮窗**：
  - 输入 `/` 时，在光标正下方弹出圆角为 `6px`、带柔和投影（`box-shadow`）的卡片，背景采用微透明磨砂（`backdrop-filter: blur`）。

---

## 六、 研发路线图 (Roadmap)

采取快速迭代、小步快跑策略，计划用 **6 周** 完成首个公测版的交付：

```
 Week 1-2: 核心引擎开发 (P0)      Week 3-4: 桌面端打通 (P0-P1)     Week 5-6: 体验打磨与导出 (P1-P2)
+----------------------------+   +----------------------------+   +----------------------------+
| 1. 基于 TipTap 搭建编辑器     |   | 1. 完成 Tauri 壳体封装      |   | 1. 优化斜杠命令的流畅度      |
| 2. 编写核心 Markdown 插件    |   | 2. 打通本地单文件关联双击启动|   | 2. 实现本地 PDF 无损打印导出 |
| 3. 完成最基础的文本读写与保存 |   | 3. 实现极简侧边栏本地文件树 |   | 3. 进行大文件加载性能调优   |
+----------------------------+   +----------------------------+   +----------------------------+
```

---

## 七、 系统性能指标 (System Performance Metrics)

作为一款本地编辑器，极致的性能是抗衡云端笔记工具的核心基石。FkeMark 必须满足以下性能硬指标：

1. **冷启动时间**：双击本地任意 `.md` 文件至界面完全渲染完成，耗时控制在 **400 毫秒** 以内。
2. **内存占用**：无文档打开状态下，常驻内存控制在 **60MB - 80MB** 之间（得益于 Tauri 调用系统原生 WebView，摆脱 Chromium 庞大开销）。
3. **大文件支持**：加载 **10MB**（约数百万字）的超大 Markdown 文档，解析并首次渲染时间不超过 **1.5 秒**，连续快速输入时**无感知延迟（输入延迟小于 16 毫秒/单帧）**。
4. **无损数据安全**：基于 300 毫秒 Debounce 的高频自动保存机制，在异常断电或进程强退测试中，用户至多损失最近一次停笔后不超过 **1 秒** 的编辑内容。

---

## 八、 产品整体模块规划 (Product Module Architecture)

FkeMark 采用「核心编辑能力 + 文件管理能力 + 用户体验配置 + 产品信息」四层模块结构。

```
FkeMark
│
├── 1. 编辑核心模块 (Editor Core)
│   ├── Markdown 即时渲染编辑器
│   ├── 文档解析引擎
│   ├── 格式化与快捷输入
│   ├── 代码/公式/表格支持
│   └── 编辑历史恢复
│
├── 2. 文件管理模块 (File Workspace)
│   ├── 本地文件浏览
│   ├── 最近打开文件
│   ├── 收藏文件
│   ├── 文件搜索
│   └── 文件状态管理
│
├── 3. 阅读与辅助模块 (Reading Experience)
│   ├── TOC 文档大纲
│   ├── 专注模式
│   ├── 打字机模式
│   ├── 阅读模式
│   └── 页面缩放
│
├── 4. 输入效率模块 (Productivity Tools)
│   ├── Slash Command 命令菜单
│   ├── Markdown 快捷语法
│   ├── 自动补全
│   ├── 快捷键系统
│   └── 命令面板
│
├── 5. 导入导出模块 (Import & Export)
│   ├── Markdown 文件打开
│   ├── PDF 导出
│   ├── HTML 导出
│   ├── 打印支持
│   └── 内容复制格式转换
│
├── 6. 个性化设置模块 (Customization)
│   ├── 外观主题
│   ├── 编辑体验
│   ├── 文件行为
│   ├── 快捷键配置
│   └── 高级参数
│
└── 7. 关于与支持模块 (About)
    ├── 产品介绍
    ├── 版本信息
    ├── 更新日志
    ├── 开源协议
    └── 联系反馈
```

---

## 九、 功能模块详细设计 (Functional Modules)

### 1. 编辑核心模块

#### 即时渲染编辑器
- **功能特性**：
  - Markdown 源码与排版实时切换
  - 光标所在区域显示 Markdown 语法标记，移出区域自动隐藏
  - 支持标题、列表、引用、链接、图片等基础 Markdown 语法
- **主要能力优先级**：
  | 功能项 | 优先级 |
  |---|---|
  | 标题编辑 | P0 |
  | 粗体 / 斜体 | P0 |
  | 列表 | P0 |
  | 引用 | P0 |
  | 表格 | P1 |
  | 数学公式 | P1 |
  | Mermaid 图表 | P2 |

### 2. 文件管理模块

#### 文件工作区
- **目标**：坚持“文件系统优先”原则，不建立私有内部知识库。
- **功能特性**：
  - 打开并浏览本地文件夹真实目录
  - 多文件快速切换
  - 最近打开文档列表（Recent）
  - 常用文件收藏（Favorites）
- **侧边栏结构示意**：
  ```
  Workspace

  📁 Documents
    README.md
    Design.md
    Notes.md

  ⭐ Favorites
    Project.md

  🕘 Recent
    Today.md
  ```

### 3. 阅读辅助模块

#### 文档大纲 (TOC)
- 自动提取文档 H1-H6 标题生成层级树。
- 支持点击标题锚点平滑滚动跳转。
- 随滚动实时高亮当前阅读位置。

#### 专注模式
- **普通模式**：完整展示文件树、编辑画布与工具区域。
- **聚焦模式**：当前光标所在段落高亮显示，其余段落降低对比度与透明度。
- **沉浸模式**：隐藏侧栏、工具栏及状态栏，仅保留居中编辑正文。

---

## 十、 个性化设置系统设计 (Personalization Settings)

设置面板采用清晰的树状分组结构：

```
设置
├── 外观 Appearance
├── 编辑 Editor
├── 文件 Files
├── 快捷键 Shortcut
├── 导出 Export
├── 高级 Advanced
└── 关于 About
```

### 1. 外观设置 (Appearance)

- **主题模式**：Light / Dark / System Auto
- **编辑区域排版参数**：
  | 设置项 | 默认值 |
  |---|---|
  | 内容最大宽度 | 800px |
  | 字体大小 | 16px |
  | 行间距 | 1.8 |
  | 段落间距 | 中等 |
  | 页面左右留白 | 自动 |
- **字体支持**：
  - 系统字体（System Default）
  - Serif 阅读字体（如 Inter / Noto Sans）
  - Mono 等宽字体（如 JetBrains Mono）

### 2. 编辑设置 (Editor)

- **自动保存机制**：
  - 自动保存开关：`[✓] 开启`
  - 保存间隔选项：实时 / 300ms / 1秒 / 5秒
- **Markdown 显示行为**：
  - `[✓] 显示当前编辑区域 Markdown 标记`
  - `[✓] 自动隐藏语法符号`
  - `[✓] 自动补全 Markdown`
  - `[✓] 输入列表自动延续`

### 3. 文件设置 (Files)

- **默认启动行为**：
  - 打开最近文件 / 恢复上次工作区 / 显示空白页面
- **文件保护策略**：
  - `[✓] 自动恢复上次打开文档`
  - `[✓] 保存前自动备份`
  - `[✓] 关闭前提醒未保存内容`

### 4. 快捷键设置 (Shortcut)

- **预设快捷键映射**：
  | 操作 | 默认快捷键 |
  |---|---|
  | 新建文档 | Ctrl + N |
  | 打开文件 | Ctrl + O |
  | 保存文档 | Ctrl + S |
  | 全局搜索 | Ctrl + P |
  | 命令面板 | Ctrl + Shift + P |
  | 专注模式 | Ctrl + Shift + F |
- **配置能力**：支持快捷键录入修改、恢复默认设置及方案导入导出。

### 5. 导出设置 (Export)

- **PDF 导出**：支持配置页面尺寸（A4/Letter）、页边距、打印字体及是否携带目录。
- **HTML 导出**：支持选择是否内联 CSS 样式表、是否转换嵌入图片及是否压缩 HTML 代码。

### 6. 高级设置 (Advanced)

- **性能优化**：
  - 大文件模式：`[✓] 开启`
  - 最大缓存限制：`512MB`
- **实验性特性**：
  - `[ ] Mermaid 图表支持`
  - `[ ] Vim 编辑模式`
  - `[ ] AI 辅助插件`
  - `[ ] 自动同步`

---

## 十一、 关于页面设计 (About Page)

保持极简扁平风格，直观呈现产品核心理念与版本状态。

### 1. 界面布局示意

```
             FkeMark

         Markdown Editor

          Write Simply.
          Think Clearly.

Version 1.0.0

特性：
✓ Local First
✓ No Database
✓ Instant Rendering

---------------------
检查更新 | 更新日志 | 用户协议 | 开源许可 | 反馈建议
```

### 2. 产品信息字段

- **产品名称**：FkeMark
- **当前版本**：v1.0.0
- **运行环境**：Windows / macOS / Linux
- **编辑引擎**：ProseMirror
- **桌面框架**：Tauri

---

## 十二、 用户体验细节规划 (UX Details)

### 1. 空状态页面
应用初次启动或未打开任何文档时展示极简引导：
```
欢迎使用 FkeMark

开始：
+ 新建 Markdown 文件
+ 打开本地文件夹

快捷提示：
Ctrl + N 创建文档
```

### 2. 更新提示
采用非侵入式轻量 Toast 通知：
```
右下角 Toast：
FkeMark 1.1.0 已发布
[查看更新 →]
```

### 3. 错误反馈
采用统一的操作级错误处理面板：
```
发生错误
文件无法保存
[重新尝试]  [查看详情]
```

---

## 十三、 后续版本规划 (Future Versions)

### 1. V1.0 基础版本 (已交付)

- **目标**：构建“本地优先、无数据库”的极简即时渲染编辑核心，提供生产级的数据安全与极速写作体验。
- **功能特性**：
  - `[x] 核心 Markdown 即时渲染`：基于 ProseMirror/TipTap 架构实现光标处语法显隐，提供实时、阅读、分栏与源码四种视图模式。
  - `[x] 本地文件树与工作区管理`：直连真实物理文件系统，支持目录浏览、多标签页调度、最近打开与文档收藏。
  - `[x] 300ms 自动保存与防丢机制`：前端防抖感知配合 Rust 异步非阻塞落盘，内置循环滚动的本地版本快照（Snapshot）备份。
  - `[x] 原生双主题与预设配色`：基于 CSS 语义 Token 打造纯净 Light / Dark 双主题，并预置 10+ 款开发者主题配色。
- **主要能力优先级**：
  | 功能项 | 状态 | 优先级 | 核心价值 |
  |---|---|---|---|
  | 即时渲染核心引擎 | [x] 已交付 | P0 | 实现所见即所得与 Markdown 纯文本的高保真往返 |
  | 极简本地文件树 | [x] 已交付 | P0 | 提供轻量无数据库的单机多文档组织管理 |
  | 自动保存与版本快照 | [x] 已交付 | P0 | 确保输入无感持久化，保障数据零丢失 |
  | 深浅主题与视觉规范 | [x] 已交付 | P1 | 保障多光照环境下的写作舒适度与沉浸感 |

---

### 2. V1.5 体验增强 (部分已落地 / 持续迭代)

- **目标**：在保持核心轻量稳定的前提下丰富格式流转生态与极客交互体验，持续打磨块级排版、长文检索与设置交互细节。
- **功能特性**：
  - `[x] 更多导出格式支持`：原生支持 PDF、Word (.docx)、ePub 电子书 (.epub)、富文本 (.rtf) 与 OPML 大纲一键导出。
  - `[ ] 源码模式语义语法高亮`：对源码模式下的 Markdown 标记符号（如标题 `#`、加粗斜体 `*`/`_`、引用 `>`、列表 `-`、围栏反引号、链接括号等）引入独立语法着色与透明度区分，清晰分离控制符号与正文内容。
  - `[ ] 块级浮动操作手柄与转换菜单`：在即时渲染与块级排版模式下，聚焦块元素时行首动态浮现双功能手柄：
    - **操作菜单手柄（⋮⋮）**：点击弹出轻量菜单，支持删除、复制、移动，以及“转换为…”格式切换（各级标题、引用块、代码块、待办项、列表等无损互转）；
    - **快捷添加手柄（+）**：点击立即在当前块下方插入新空行/新块并自动聚焦，提升连续记录流畅度。
  - `[ ] 工作区全文件极速全文检索与替换`：为侧边栏文件树构建高性能全局搜索面板，底层并发快速扫描工作区目录下的全部 Markdown 文件，支持纯文本与正则匹配、命中上下文即时高亮预览，以及跨文件批量安全替换。
  - `[ ] 自动保存时间策略设置`：在设置面板提供自动保存时间间隔的精细化配置，提供三档默认下拉预设（如：实时 300ms 防抖 / 1 秒静默 / 5 秒低频），兼顾编辑安全与写盘性能。
  - `[ ] 设置面板吸顶搜索栏视觉层叠优化`：重构设置面板顶部搜索框的吸顶布局与层叠上下文，增加实底背景或高斯模糊磨砂效果（`backdrop-filter: blur`）及微投影，消除滚动时内容穿透显示在搜索框背后的视觉重叠缺陷。
  - `[ ] Vim 编辑键位模式`：深度接入键盘状态机，支持 Normal/Insert/Visual 模式及 `h/j/k/l` 等无鼠标盲打导航。
  - `[ ] 插件扩展系统`：基于 Web Worker 或安全沙箱的受限轻量扩展机制，支持第三方功能即插即用。
  - `[ ] 自定义主题与配色编辑`：开放可视化语义 Token 编辑面板，支持用户自由调色并持久化保存。
- **主要能力优先级**：
  | 功能项 | 状态 | 优先级 | 核心价值 |
  |---|---|---|---|
  | 多格式导出 (Word/ePub等) | [x] 已交付 | P1 | 打破纯文本局限，满足一站式专业文档分发需求 |
  | 源码模式语法语义着色 | [ ] 规划中 | P1 | 强化源码视角的结构辨识度，清晰区分语法标记与内容 |
  | 块级操作手柄与转换菜单 | [ ] 规划中 | P1 | 降低段落结构调整门槛，实现一键格式转换与行级追加 |
  | 工作区全局检索与替换 | [ ] 规划中 | P1 | 突破单文件查找瓶颈，秒级定位并批量修改多文档内容 |
  | 自动保存时间策略下拉 | [ ] 规划中 | P2 | 开放时间档位自选，满足差异化的防丢与性能权衡诉求 |
  | 设置页吸顶搜索层叠修复 | [ ] 规划中 | P2 | 消除滚动内容穿透视觉干扰，保持设置面板整洁精致 |
  | Vim 编辑键位 | [ ] 规划中 | P2 | 贴合重度键盘流肌肉记忆，提升编辑吞吐效率 |
  | 插件扩展系统 | [ ] 规划中 | P2 | 允许按需扩展小众场景能力，保持主程序轻巧纯粹 |
  | 自定义配色编辑器 | [ ] 规划中 | P2 | 开放视觉掌控权，满足深度个性化与无障碍对比度需求 |

---

### 3. V2.0 专业版本 (部分已落地 / 架构演进)

- **目标**：引入跨端云同步、智能伴写与多任务工程化能力，在坚守本地优先和数据隐私的前提下，释放进阶生产力。
- **功能特性**：
  - `[x] 多窗口并行独立编辑`：利用 Tauri v2 异步管线创建独立窗口，主从生命周期隔离，支持跨屏对照编辑。
  - `[x] 本地 AI 辅助写作扩展`：集成独立侧边栏流式对话，支持多模型协议与续写、润色、摘要、翻译快捷动作。
  - `[ ] WebDAV 多端增量同步与自动推送`：支持配置标准 WebDAV 远端（如坚果云、Nextcloud、私有 NAS 等），允许指定工作区主目录名称进行映射，支持变更防抖自动推送与增量拉取，在零数据库前提下实现跨设备安全同步。
  - `[ ] Git 文档版本追踪与管理`：底层集成 Git 接口，侧栏提供行内 Diff、暂存 Commit 与分支管理。
  - `[ ] 局域网/点对点团队协作`：基于无中心 CRDT（Yjs）与 WebRTC/mDNS，实现无云端数据库的点对点实时协作。
- **主要能力优先级**：
  | 功能项 | 状态 | 优先级 | 核心价值 |
  |---|---|---|---|
  | 独立多窗口编辑 | [x] 已交付 | P0 | 消除单窗口标签切换摩擦，支持跨屏并排校对与多任务写作 |
  | 本地 AI 伴写侧栏 | [x] 已交付 | P1 | 作为非侵入式创作副驾驶，提供上下文相关的润色与提炼 |
  | WebDAV 增量同步与自动推送 | [ ] 规划中 | P1 | 以开放标准协议实现多设备无缝同步，坚决捍卫数据隐私 |
  | Git 版本追踪集成 | [ ] 规划中 | P1 | 赋予纯文本 Markdown 工程级版本溯源与分支管理能力 |
  | P2P 局域网团队协作 | [ ] 规划中 | P2 | 在 100% 捍卫本地数据隐私的前提下实现低延迟实时协同 |
