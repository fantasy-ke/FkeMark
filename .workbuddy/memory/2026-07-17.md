# 2026-07-17 工作日志

## GitHub Action / 仓库元数据 / README / 许可证 / 关于页关联
- 修复 `.github/workflows/build.yml` 打包问题：
  - 触发改为仅 tag（`v*`）推送，移除 `workflow_dispatch`
  - **关键 bug**：`beforeBuildCommand` 为空且未显式构建前端，dist 缺失导致 tauri build 失败 → 新增 `npm run build` 步骤
  - 便携版产物名 `fke-mark.exe` → `FkeMark.exe`（与 Cargo `[[bin]] name` 一致）
  - 完善 releaseBody 描述
- 作者名 `FkeMark Team` → `fantasyke`：Cargo.toml `authors` + `repository`；tauri.conf.json `copyright`；package.json 新增 `author/repository/homepage/bugs/license`
- 新增 `README.md`（项目介绍/特性/构建/链接）与 `LICENSE`（MIT, fantasyke, 2026）
- AboutPage.tsx 链接接入真实地址：GitHub/官网→`https://github.com/fantasy-ke/FkeMark`，反馈→issues，许可证→blob/main/LICENSE；用 `@tauri-apps/api/shell` open + 回退 `window.open`
- 假设 GitHub 仓库名为 `FkeMark`（用户 fantasy-ke 下），需用户确认
- 验证：tsc 0 错误；vite build 成功

## 许可证改为最严格开源许可 AGPL-3.0
- 用户要求「最严格的开源许可」→ 选用 GNU AGPL v3（约束最强，含网络/SaaS 使用须提供源码）
- `LICENSE` 重写为官方完整 AGPL-3.0 文本（FSF 版权声明原样保留），末尾追加 FkeMark 项目版权声明（Copyright (C) 2026 fantasyke + 源码获取地址）
- `package.json` license: `MIT` → `AGPL-3.0-only`
- `AboutPage.tsx` 与 `SettingsPanel.tsx` 中硬编码的 "MIT License" → "AGPL-3.0"
- `README.md` 许可证段落改为 AGPL-3.0 说明
- 未改动 `package-lock.json` 中依赖各自许可、未改动 `dist/` 构建产物
- 验证：tsc 0 错误

## 修复 GitHub Action 实际打包报错（用户反馈 CI 失败）
- 所有平台 ENOENT `Cargo.toml`：tauri-action 默认在仓库根找 Cargo.toml，但 Tauri 工程在 `src-tauri/`。修复：Build Tauri app 步骤加 `projectPath: src-tauri`
- Ubuntu `E: Unable to locate package libappindicator1-dev`：`libappindicator1-dev` 新版 Ubuntu 已移除，Tauri v1 链接 appindicator3。修复：`apt-get install` 改为 `libappindicator3-dev`，并补充 libgtk-3-dev/dpkg/build-essential/libssl-dev/libxdo-dev 以支撑 deb/appimage 打包与链接
- 显式设 `includeUpdaterJson: false`（updater.active 为 false，避免要求签名密钥）
- 校验：YAML 关键字段通过；apt 安装行已无 1-dev

## 第二轮 CI 报错修复（Windows/macOS rename + Ubuntu libsoup）
- Windows/macOS `failed to rename fke-mark(.exe) → FkeMark(.exe)` (os error 2, 源不存在)：根因 Cargo.toml 的 `[[bin]] name="FkeMark"` 让 cargo 直接产出 FkeMark(.exe)，但 Tauri 按 package 名 `fke-mark` 找源重命名 → 源缺失。修复：移除 `[[bin]]` 块，cargo 产出 `fke-mark(.exe)`，由 Tauri 正常重命名为 `FkeMark(.exe)`（产物名与 productName 对齐）。TOML 校验通过。
- Ubuntu `Package libsoup-2.4 was not found` / soup2-sys 失败：根因装的 `libwebkit2gtk-4.1-dev` 提供 libsoup3，而 Tauri v1 的 webkit2gtk 链接 libsoup-2.4。修复：改装 `libwebkit2gtk-4.0-dev` + 显式 `libsoup2.4-dev`。
- macOS `block v0.1.6` 仅为 future-incompat 警告，非失败，随 rename 修复一并解决。

## 第三轮 CI 修复（Release 创建权限 + 并行竞争）
- macOS 构建/打包已成功（rename 修复生效），新错误 `Error: Resource not accessible by integration`：根因 `GITHUB_TOKEN` 默认无创建 Release 权限。修复：build job 加 `permissions: contents: write`。
- 并行竞争规避：4 个 matrix job 各自用 tauri-action 查找/创建同一 tag 的 Release，macOS 两 job 几乎同时到达会竞争 `already exists`。修复：新增 `create-release` job（ubuntu-22.04，`softprops/action-gh-release@v2`，draft）先建好 Release，build job `needs: [create-release]` 再上传产物，消除竞争。
- 修正过时注释（便携产物名不再依赖 `[[bin]]`）。
- 校验：YAML 结构逐行核对无误（PyYAML 不可用，未做机解析）。

## 第四轮 CI 增强（changelog + dev 分支预发布）
- `on:` 新增 `branches: [dev]`，与 `tags: [v*]` 并列：推送 dev 分支即触发构建。
- `create-release` job 区分正式/开发版：`if` 改为 `startsWith(refs/tags/v) || ref==refs/heads/dev`；新增 outputs `tag_name/release_name/prerelease/draft`；dev 时 tag=`dev-latest`、name=`Dev Build (dev-<短SHA7>)`、`prerelease=true`/`draft=false`，并先 `gh release delete` + `gh api DELETE /git/refs/tags/dev-latest` 清理旧 dev-latest 再重建。
- `build` job 的 tagName/releaseName/releaseDraft/prerelease 全部改用 `needs.create-release.outputs.*`，dev 与正式版自动分流。
- 新增 `changelog` 分类（与本项目提交规范 feat/fix/chore/docs/ci/refactor/perf 对应）：新功能(feature/enhancement/feat)、问题修复(bug/fix)、文档与流程(docs/documentation/ci/workflow/chore)、重构与优化(refactor/perf/optimization)、其他更新(*)。
- 注意：tauri-action 的 changelog 按 **PR 标签** 归类；若提交未打对应标签则落入「其他更新」。若要精确归类，需为 PR 打 feature/bug/docs/ci/chore/refactor/perf 等标签。
- 校验：managed venv 装 pyyaml 后 `yaml.safe_load` 通过；on/jobs/outputs/changelog 字段全部正确（`changelog` 在 YAML 层为字符串块，由 tauri-action 解析，符合规范）。
- 本轮仅改 `.github/workflows/build.yml`，未提交 git（用户未要求）。

## 第五轮 CI 结构重构（拆分 release/dev 工作流 + changelog 独立文件）
- 删除单一 `.github/workflows/build.yml`，拆分为两个独立工作流：
  - `.github/workflows/release.yml`：触发 `tags: [v*]`，正式 Release（draft 手动发布）
  - `.github/workflows/dev.yml`：触发 `branches: [dev]`，开发预发布版（tag `dev-latest`、name `Dev Build (dev-<短SHA7>)`、prerelease、自动发布），每次推送先 `gh release delete` + `gh api DELETE /git/refs/tags/dev-latest` 清理旧版再重建
- changelog 分类配置抽到独立文件 `.github/changelog.yml`（`categories:` 五类：新功能/问题修复/文档与流程/重构与优化/其他更新），两个工作流通过 `Load changelog config` 步骤（`echo "CHANGELOG<<EOF" >> $GITHUB_ENV; cat .github/changelog.yml >> $GITHUB_ENV; echo EOF`）读入 env，再 `changelog: ${{ env.CHANGELOG }}` 引用。
- 注意：GitHub Actions 要求工作流文件必须直接在 `.github/workflows/` 下，不支持子文件夹；故 changelog 配置放在 `.github/changelog.yml`（非 workflows 内），避免被当作无效工作流。
- 校验：pyyaml `safe_load` 三个文件全部通过；release/dev 触发条件、Load 步骤、tauri.changelog 引用均正确；changelog.yml 五分类解析正确。
- 本轮改动未提交 git（用户未要求）。

## 第六轮 CI 调整（release 发布态 + dev 节点改名 + changelog 自生成）
- 需求1：release 完成后应为正式发布包（非 Draft）→ release.yml 的 prep 步骤 `draft: false`（发布态）。
- 需求2：dev 节点不应叫 create-release → dev.yml 的 job 改名 `dev-release`，build 的 `needs: [dev-release]` 同步更新。
- 需求3：release 正式包的提交日志要含「自上一正式版本以来」的全部 log。改为工作流自建 changelog：prep 步骤用 Python 跑 `git tag --list 'v*' --sort=-v:refname` 定位上一 v* tag，再 `git log <prev>..HEAD --pretty=format:%s` 取全部提交，按 `.github/changelog.yml` 的 `prefixes` 分类（feat→新功能/fix→问题修复/chore,ci,docs→文档与流程/refactor,perf→重构与优化/其他→其他更新）。生成结果作为 Release 正文（softprops `body` + tauri-action `releaseBody`），不再用 tauri-action 的 label 式 `changelog` 输入（会被 dev-latest 干扰且全部落其他更新）。
- `.github/changelog.yml` 由 `labels` 改为 `prefixes`（与本项目实际提交规范 feat/fix/chore/refactor/docs/ci/perf 对齐，分类才能真正生效）。
- prep 步骤 `actions/checkout` 加 `fetch-depth: 0` + `fetch-tags: true` 以拿到完整历史与全部 tag。
- dev 的 changelog 也复用同一生成逻辑（prev 取上一 v* tag）。
- 本地实跑 Python 生成器验证：分类正确；真实 CI 推 v0.2.0 时 prev=v0.1.0 只取区间提交。
- 校验：pyyaml 解析三个文件通过；release/dev 触发、jobs、outputs、tauri.releaseBody 引用、无 changelog 输入均正确。
- 本轮改动未提交 git（用户未要求）。


## 第七轮：版本更新系统（latest/dev 双通道 + GitHub 集成 + 自动检查）

### 需求
1. 添加版本更新系统，latest 通道跟 dev 通道，软件包在 GitHub 下载更新
2. 项目入口增加 GitHub 仓库访问、源代码、问题反馈、发布记录链接
3. 查看当前版本、最新版本、发布时间、更新内容、是否有可用更新
4. 每次打开自动获取是否有最新版本

### 实现概览

#### 1. 新增 `src/utils/updater.ts`（版本更新服务）
- **双策略检查更新**：
  - 主策略：从 GitHub Release 下载静态 JSON 清单（`latest.json` / `dev.json`），无 API 速率限制
  - 降级策略：GitHub API（`/releases/latest` 或 `/releases/tags/dev-latest`）
- `getLocalVersion()`：优先 `@tauri-apps/api/app.getVersion()`，降级 `__APP_VERSION__`（Vite define 注入）
- `checkForUpdate(channel, currentVersion)`：返回 `UpdateInfo`（version/releaseDate/releaseNotes/downloads/isNewer/isPrerelease）
- `compareVersions()`：语义化版本号比较
- `formatReleaseDate()` / `formatFileSize()`：格式化辅助
- `openExternalUrl()`：Tauri `shell.open` + 浏览器 `window.open` 降级
- `GITHUB_URLS`：集中管理所有 GitHub 链接（repo/issues/releases/newIssue/license/sourceCode）

#### 2. 类型定义 (`src/types/index.ts`)
- 新增 `UpdateChannel = 'latest' | 'dev'`
- AppSettings 新增 `updateChannel: UpdateChannel` 和 `autoCheckUpdate: boolean`

#### 3. Rust 后端
- `main.rs`：新增 `get_app_version` 命令（`env!("CARGO_PKG_VERSION")` 编译时注入版本号）
- `settings.rs`：AppSettings 新增 `update_channel`（默认 "latest"）和 `auto_check_update`（默认 true）

#### 4. Vite 配置 (`vite.config.ts`)
- `define: { __APP_VERSION__: JSON.stringify(pkg.version) }` 将 package.json 版本号注入前端

#### 5. App.tsx — 自动检查 + Toast 通知
- 新增状态：`appVersion` / `updateInfo` / `checkingUpdate` / `showUpdateToast`
- 启动时 useEffect（延迟 2 秒）自动调用 `doCheckUpdate(settings.updateChannel, false)`
- `doCheckUpdate(channel, showLoading)`：检查更新 + 设置 updateInfo + 如有新版触发 Toast
- 更新通知 Toast：右下角滑入，含「查看详情」/「下载更新」/「稍后提醒」三个按钮
- `hasUpdate` prop 传递给 TopBar（菜单「关于」项显示红点徽章）

#### 6. SettingsPanel.tsx — 关于页「检查更新」区块
- 新增 props：`appVersion` / `updateInfo` / `checkingUpdate` / `onCheckUpdate`
- About 区块新增「检查更新」CollapsibleGroup（默认展开）：
  - 更新通道选择器（正式版 / 开发版 radio group）
  - 自动检查更新 toggle
  - 当前版本 / 最新版本 / 发布时间 元数据行
  - 更新状态指示器（checking/available/uptodate/idle 四态 + SVG 图标）
  - 「检查更新」按钮（loading 态带旋转动画）
  - 「打开下载页面」按钮（有新版时显示，打开 GitHub Release 页面）
  - 更新日志展示区（`<pre>` 等宽字体，最高 280px 滚动）
- 链接区全部接入真实 URL：
  - GitHub 仓库 → `GITHUB_URLS.repo`
  - 查看源代码 → `GITHUB_URLS.sourceCode`
  - 提交问题或建议 → `GITHUB_URLS.newIssue`
  - 发布记录 → `GITHUB_URLS.releases`
  - 许可证 → `GITHUB_URLS.license`

#### 7. TopBar.tsx — 菜单新增 GitHub 链接区
- 菜单新增第 ⑥ 区「GitHub」：仓库 / 问题反馈 / 发布记录 三个按钮
- 「关于」按钮新增 `hasUpdate` prop → 显示橙色更新提示徽章（`menu-update-badge`）

#### 8. CSS 样式 (`src/styles/components.css`)
- `.update-status-row` / `.update-status.*` 四态样式
- `.update-check-btn` 按钮 + loading 态
- `.update-download-btn` 下载按钮
- `.update-release-notes*` 更新日志容器
- `.update-prerelease-badge` 预发布徽章
- `.spin` 旋转动画
- `.menu-update-badge` 菜单更新提示点
- `.update-toast*` Toast 通知全套样式（右下角滑入动画 + 卡片阴影）

#### 9. i18n 新增键值（zh-CN.ts + en.ts）
- `update.*`：title/currentVersion/latestVersion/releaseDate/releaseNotes/channel.*/autoCheck/checkBtn/checking/upToDate/available/download/downloadPage/checkFailed/noRelease/viewReleases/newVersionAvailable/newVersionDesc/remindLater/prerelease
- `github.*`：repo/sourceCode/issues/newIssue/releases/viewHistory

#### 10. CI/CD 工作流更新
- `release.yml`：`create-release` job 新增 `Generate update manifest (latest.json)` 步骤
  - 生成 `latest.json`（version/tagName/pubDate/releaseNotes/htmlUrl）
  - `softprops/action-gh-release@v2` 的 `files: latest.json` 上传为 Release 资产
- `dev.yml`：`dev-release` job 新增 `Generate update manifest (dev.json)` 步骤
  - 生成 `dev.json`（version=dev-<SHA7>/tagName=dev-latest/...）
  - 同样上传为 Release 资产
- App 优先从 `https://github.com/.../releases/latest/download/latest.json` 获取（无速率限制），降级到 GitHub API

### 构建验证
- tsc --noEmit：**0 错误**
- vite build：**通过**（383 modules, CSS 63.43KB / JS 250KB+540KB gzip 72KB+173KB）
- 无 linter 错误

### 技术决策说明
- **不使用 Tauri 内置 updater**：内置 updater 需要签名密钥对 + 签名每个构建 + `updater.active: true` + pubkey 配置。用户需求是"软件包在 GitHub 下载更新"，更适合自定义检查器 + 引导用户到 GitHub 下载
- **双策略检查**：静态 JSON 文件（主）+ GitHub API（降级）。静态文件无速率限制，API 作为兼容降级
- **GitHub API CORS**：GitHub API 返回 `Access-Control-Allow-Origin: *`，Tauri WebView2 的 `fetch` 可直接调用，无需 Tauri HTTP allowlist

### Commit 建议
```
feat(update): 版本更新系统 — latest/dev 双通道 + GitHub 集成 + 自动检查

1. 新增 updater.ts 服务：双策略检查（静态JSON + GitHub API降级）
2. Rust 新增 get_app_version 命令 + settings 新增 updateChannel/autoCheckUpdate
3. 设置页 About 新增「检查更新」区块（通道选择/自动检查/版本信息/更新日志）
4. TopBar 菜单新增 GitHub 仓库/问题反馈/发布记录链接
5. 启动时自动检查更新 + Toast 通知（右下角滑入）
6. CI release/dev 工作流生成 latest.json/dev.json 更新清单
7. i18n 新增 update.*/github.* 键值（zh-CN + en）
```
