# 2026-07-18 工作日志

## 归档外部项目 workflow 参考案例

### 调整内容
- 新建 `.github/忽略 案列/` 文件夹（未跟踪于 git，仅本地归档参考用）。
- 将两个来自**外部项目 GoNavi**（一款基于 Wails + Go 的数据库管理工具）的复杂 workflow 案例文件归档到该文件夹：
  - `dev-build.yml`（1415 行）：GoNavi 项目的 dev 分支预发布构建工作流
  - `release.yml`（1362 行）：GoNavi 项目的正式 Release 工作流（v* tag 触发）

### 参考案例文件技术栈对比
| 维度 | FkeMark 实际 workflow | GoNavi 参考案例 |
|------|----------------------|-----------------|
| 框架 | Tauri v1（Rust + WebView） | Wails v2（Go + WebView） |
| 构建工具 | npm + vite + cargo | Go + wails + npm（前端 dist） |
| 平台矩阵 | windows/macos×2/ubuntu | darwin×2/windows×2/linux×3（含 webkit41 变体） |
| 产物 | msi/nsis/dmg/app/deb/appimage + 便携 exe | dmg/exe/msi/tar.gz/AppImage + driver-agent 资产 |
| Release | softprops/action-gh-release@v2 + tauri-action@v0 | softprops/action-gh-release@v3 + 手动 gh release create |
| 额外能力 | changelog 自生成（Python + prefixes 分类） | driver-agent 检测/构建/签名/manifest、UPX 压缩、WiX MSI、Ad-hoc 签名、latest.json 更新清单 |

### GoNavi 案例的可借鉴点（若后续 FkeMark CI 需增强）
1. **concurrency 控制**：`dev-build.yml` 顶部用 `concurrency: { group, cancel-in-progress: true }` 取消同分支旧构建，节省 CI 资源。FkeMark 当前 dev.yml 未启用，后续如需可在 `on:` 后追加。
2. **多 Job 拆分 + 产物中转**：GoNavi 拆为 `frontend → driver_agents → build(matrix) → release` 四阶段，前端单独构建后用 upload/download-artifact 分发到各平台 build job。FkeMark 当前在每个 build job 内重复 `npm ci + npm run build`，若前端构建成为瓶颈可参考此拆分。
3. **UPX 二次压缩**：GoNavi 对 Windows/Linux 产物用 `upx --best --lzma --force` 二次压缩并打印压缩前后体积对比。FkeMark 的 Tauri 产物（msi/nsis）已自带压缩，但**便携 exe**（`FkeMark.exe`）若体积较大可考虑引入 UPX。
4. **SHA256SUMS 资产校验**：GoNavi release 阶段对全部产物生成 `SHA256SUMS` 供用户校验完整性。FkeMark 当前未生成，发布正式版时可作为信任增强项。
5. **静态更新清单**：GoNavi 生成 `latest.json` / `latest-dev.json` 静态清单放到 release assets，客户端通过 `github.com/.../latest/download/latest.json` 检查更新，避免打爆 api.github.com 未认证配额。FkeMark 当前 `includeUpdaterJson: false`（未启用 updater），若未来开启 Tauri updater 可参考此模式。
6. **changelog 分类**：GoNavi release.yml 用 emoji + 关键字双匹配（✨/feat、🐛/fix、⚡/perf、♻️/refactor、🌐、🔧/chore）分类提交记录，并附 `compare/prevTag...curTag` 链接。FkeMark 当前用 Python + `.github/changelog.yml` 的 prefixes 分类（更严格、不依赖 emoji），已能满足需求，但**可借鉴 compare 链接**增强正式版 Release 正文。
7. **MSI 版本号映射**：GoNavi 从 git tag `vX.Y.Z` 解析 major/minor/patch 映射到 WiX MSI productVersion（major.minor.build，build≤65535）。FkeMark 当前由 tauri-action 自动处理 msi 版本，若需自定义可参考。
8. **Linux AppImage 工具链**：GoNavi 显式下载 `linuxdeploy` + `linuxdeploy-plugin-gtk` 并设置 `DEPLOY_GTK_VERSION=3`，arm64 跳过 AppImage 仅产出 tar.gz。FkeMark 由 tauri-action 内部处理 appimage，若遇 AppImage 失败可参考其兜底逻辑（`touch /tmp/skip-appimage` + `exit 0`）。

### 当前 FkeMark workflow 状态（已调整，详见下文「流水线重构」）
- `.github/workflows/release.yml`：v* tag 触发，三阶段 `create-release → build(4平台) → publish`。
- `.github/workflows/dev.yml`：dev 分支触发，三阶段 `dev-release → build(4平台) → publish`，concurrency 已启用。
- `.github/changelog.yml`：五分类 prefixes（新功能/问题修复/文档与流程/重构与优化/其他更新）。

---

## 流水线重构（借鉴 GoNavi + 适配 Tauri v1）

### 需求
用户要求：读取 GoNavi 流水线文件，调整 FkeMark 的流水线文件，使打包产物和流程对齐 GoNavi 风格；dev 版本号采用 `dev-d0a6a97`（短 SHA7）格式；msi 打包不能出错。

### msi 打包不出错的关键决策
**根因**：Tauri v1 的 msi 用 WiX 打包，版本号必须是 `major.minor.patch` 纯数字格式。若把 `dev-d0a6a97` 这种带字母/连字符的版本号写入 `tauri.conf.json` 或 `Cargo.toml`，WiX 会报错导致 msi 打包失败。

**解决方案**（借鉴 GoNavi MSI 版本号映射思路）：
- **dev 构建**：**不修改** `Cargo.toml`/`tauri.conf.json` 的版本号（保持 `0.1.0`），msi 用 `0.1.0` 合法版本号打包。`dev-d0a6a97` 仅作为 Release name + 产物文件名后缀（显示用），不传入 Tauri 构建链路。
- **release 构建**：从 tag `vX.Y.Z` 提取数字版本号 `X.Y.Z`，构建前用 Python 写入 `Cargo.toml`/`tauri.conf.json`/`package.json`，msi 用合法 `X.Y.Z` 打包。Python 脚本含正则校验 `^\d+\.\d+\.\d+$`，非法版本号直接 fail。

### dev.yml 重构详情
**文件**：`.github/workflows/dev.yml`（从 207 行 → 约 280 行）

**结构变更**：从两阶段（dev-release + build）→ **三阶段**（dev-release → build → publish）

1. **concurrency 控制**（借鉴 GoNavi）：`concurrency: { group: ${{ github.workflow }}-${{ github.ref }}, cancel-in-progress: true }`，同分支新推送取消旧构建。

2. **dev 版本号**（用户要求）：
   - `DEV_VERSION="dev-${GITHUB_SHA::7}"`（如 `dev-d0a6a97`）
   - Release name 直接用 `dev-d0a6a97`（简化 GoNavi 的 `🧪 Dev Build (dev-xxx)` 格式）
   - tag 保持 `dev-latest`（固定，用于覆盖更新）

3. **dev-release job**（阶段一）：
   - 计算 `dev_version` output，清理旧 dev-latest release+tag
   - 生成分类 changelog（保留 Python + prefixes 逻辑）
   - 用 `softprops/action-gh-release@v2` 创建空 release（prerelease=true, draft=false）
   - outputs: `tag_name/release_name/dev_version/changelog`

4. **build job**（阶段二，4 平台 matrix）：
   - **关键变更**：tauri-action **不再传 tagName/releaseName/releaseBody** 等 release 参数，仅构建不上传（避免上传未重命名的产物）
   - 新增 `Rename and collect artifacts` 步骤：把 Tauri 产物重命名为 `FkeMark-{dev_version}-{os}-{arch}.{ext}` 格式
     - Windows: `FkeMark-dev-d0a6a97-windows-x64.msi` / `-setup.exe` / `-portable.exe`
     - macOS: `FkeMark-dev-d0a6a97-macos-x64.dmg` / `macos-arm64.dmg`
     - Linux: `FkeMark-dev-d0a6a97-linux-x64.deb` / `.AppImage`
   - 便携版 exe 路径用 `ls FkeMark.exe fke-mark.exe` 兼容匹配（Tauri 可能重命名也可能不重命名 cargo 产物）
   - macOS bundle 从 `dmg app` 改为仅 `dmg`（app 目录上传 artifact 不便，dmg 已含完整 app）
   - upload-artifact 到 `dev-build-{os}-{arch}`（retention 7 天）

5. **publish job**（阶段三，新增）：
   - download-artifact（pattern: `dev-build-*`, merge-multiple: true）收集所有平台产物
   - 生成 `SHA256SUMS` 校验文件（借鉴 GoNavi）
   - softprops/action-gh-release@v2 上传产物到 dev-latest release
   - Summary 步骤打印版本号/产物数

### release.yml 重构详情
**文件**：`.github/workflows/release.yml`（从 209 行 → 约 310 行）

**结构变更**：从两阶段（create-release + build）→ **三阶段**（create-release → build → publish）

1. **create-release job**（阶段一）：
   - 从 tag `vX.Y.Z` 提取 `VERSION=X.Y.Z`（output）
   - changelog 生成保留 Python + prefixes，**新增 compare 链接**（借鉴 GoNavi）：`---\n**完整变更**: [v0.1.0...v0.2.0](https://github.com/.../compare/v0.1.0...v0.2.0)`
   - 创建空 release（prerelease=false, draft=false）

2. **build job**（阶段二，4 平台 matrix）：
   - **新增 `Bump version in config files` 步骤**（关键，确保 msi 不出错）：
     - 用 Python 跨平台替换（避免 macOS BSD sed 与 GNU sed 的 `-i` 行为差异）
     - 校验 `VERSION` 必须匹配 `^\d+\.\d+\.\d+$`，否则 fail
     - 写入 `Cargo.toml`(`version = "x.y.z"`)/`tauri.conf.json`(`"version": "x.y.z"`)/`package.json`
   - tauri-action 不传 tagName，仅构建
   - 产物重命名为 `FkeMark-{version}-{os}-{arch}.{ext}` 格式
   - 便携版 exe 路径兼容匹配
   - macOS bundle 仅 `dmg`
   - upload-artifact 到 `release-build-{os}-{arch}`

3. **publish job**（阶段三，新增）：
   - download-artifact 收集所有平台产物
   - 生成 `SHA256SUMS`
   - 上传产物到正式 release

### 产物命名对照
| 平台 | dev 产物名 | release 产物名 |
|------|-----------|---------------|
| Windows msi | `FkeMark-dev-d0a6a97-windows-x64.msi` | `FkeMark-0.2.0-windows-x64.msi` |
| Windows nsis | `FkeMark-dev-d0a6a97-windows-x64-setup.exe` | `FkeMark-0.2.0-windows-x64-setup.exe` |
| Windows 便携 | `FkeMark-dev-d0a6a97-windows-x64-portable.exe` | `FkeMark-0.2.0-windows-x64-portable.exe` |
| macOS x64 | `FkeMark-dev-d0a6a97-macos-x64.dmg` | `FkeMark-0.2.0-macos-x64.dmg` |
| macOS arm64 | `FkeMark-dev-d0a6a97-macos-arm64.dmg` | `FkeMark-0.2.0-macos-arm64.dmg` |
| Linux deb | `FkeMark-dev-d0a6a97-linux-x64.deb` | `FkeMark-0.2.0-linux-x64.deb` |
| Linux AppImage | `FkeMark-dev-d0a6a97-linux-x64.AppImage` | `FkeMark-0.2.0-linux-x64.AppImage` |
| 校验文件 | `SHA256SUMS` | `SHA256SUMS` |

### 跨平台兼容性修复
1. **sed → Python 替换**：release.yml 的版本号写入步骤原用 `sed -i -E`，但 macOS BSD sed 的 `-i` 需要参数（`-i ''`），与 Linux GNU sed 不兼容。改用 Python `re.subn` 跨平台一致。
2. **portable exe 路径**：Cargo.toml `name = "fke-mark"`，cargo 产出 `fke-mark.exe`，Tauri 可能重命名为 `FkeMark.exe`。用 `ls FkeMark.exe fke-mark.exe 2>/dev/null | head -1` 兼容两种命名。
3. **macOS bundle 精简**：从 `dmg app` 改为仅 `dmg`。`.app` 是目录，upload-artifact 需压缩处理，dmg 已含完整 app，避免复杂度。

### 校验结果
- `yaml.safe_load` 两个文件全部通过
- 结构校验：jobs 依赖链正确（dev-release → build → publish / create-release → build → publish），outputs 引用全部匹配
- dev.yml：3 jobs, 4 matrix, concurrency 已配置
- release.yml：3 jobs, 4 matrix, 版本号写入步骤 + Python 校验

### 未变动
- `.github/changelog.yml`：五分类 prefixes 配置保持不变
- `.github/忽略 案列/`：GoNavi 参考案例文件保持不变
- Tauri 构建核心步骤（Rust setup / npm ci / Build frontend / Generate icons / Install Linux deps）保持不变

### 注意
- `.github/忽略 案列/` 文件夹名含空格且为中文（"忽略案例"的笔误或个人习惯），未被 git 跟踪（`git status` 显示为 untracked）。
- 该文件夹内的两个 yml 文件**不会被 GitHub Actions 当作工作流执行**——GitHub Actions 只识别 `.github/workflows/` 目录下的一级 yml 文件，子文件夹或其他路径下的 yml 不会被触发。因此归档在此安全无副作用。
- 若希望彻底避免误触发或被误读，可考虑：
  1. 将文件后缀改为 `.yml.disabled` 或 `.txt`；或
  2. 在文件顶部加注释明确标注「此为参考案例，非本仓库工作流」；或
  3. 移出 `.github/` 目录（如放到 `docs/ci-reference/`）。
- 当前两个案例文件顶部均无任何说明性注释，建议补充来源与用途说明，避免后续维护者误以为是本仓库的历史工作流。

---

## 二次重做（分支切换后，融入更多 GoNavi 特殊设置）

### 背景
用户首次调整时弄错了分支（改动落在错误分支），随后切换到 `dev` 分支并 revert 了之前的尝试 commit（`94a6131 Revert "feat(ci): ..."`)。git log 显示之前还试过用 `0.1.0-<数字>` 这种 semver 预发布格式（`ea224b5`）让 Tauri 打包，但 Tauri v1 msi/WiX 对 semver 预发布标识符支持有问题而失败——验证了「dev 保持 0.1.0 不改、release 写入纯数字版本号」方案的正确性。

### 本次新增的 GoNavi 特殊设置（在首次重构基础上补强）
1. **顶层 `permissions: contents: write`**：从每个 job 单独声明改为 workflow 顶层全局声明，简化配置。
2. **`actions/github-script@v7` 重置 dev-latest**（替换原 gh CLI）：借鉴 GoNavi，用 `github.rest.repos.listReleases` 分页查找 + 逐个 `deleteRelease` + `git.deleteRef`，并对 404/422 容错。比 `gh release delete || true` 更可靠（gh CLI 有时分页不全/认证问题）。
3. **`target_commitish: ${{ github.sha }}`**：dev release 指向具体 commit，避免 release 指向错误 ref。
4. **`Format build time` 步骤**：用 `TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M:%S'` 生成北京时间构建时间（比解析 `head_commit.timestamp` 更可靠，避免空值导致整个发布失败）。首次尝试用 Python 解析 `head_commit.timestamp`，已改为 date 命令。
5. **release body 含元数据表格**：dev release body 追加版本/分支/提交链接/构建时间的 Markdown 表格，方便测试者识别。
6. **`if-no-files-found: error`**：所有 upload-artifact 步骤启用严格模式，产物缺失直接报错而非静默成功。
7. **`retention-days: 7`**：dev/release 构建 artifact 保留 7 天。
8. **`make_latest: true`**（release.yml）：create-release 与 publish 上传步骤均设置，标记为最新 release。
9. **changelog compare 链接**（release.yml）：changelog 末尾追加 `[v0.1.0...v0.2.0](.../compare/...)`。
10. **`merge-multiple: true`**（download-artifact）：所有平台产物合并到同一目录，便于统一生成 SHA256SUMS。

### 教训：IDE 编辑器缓冲区会覆盖外部写入
- 首次 `write` dev.yml 报告成功，但随后 `git diff` 显示 dev.yml 无变化——磁盘文件被 IDE 编辑器缓冲区还原成旧版本（用户在 IDE 中打开了 dev.yml，cursor 在 line 22）。
- release.yml 因未在 IDE 打开，写入正常保留。
- **对策**：写入后必须立即用 `git diff --stat` 验证磁盘真实内容，不能只信 write 工具的返回。若发现被还原，重新 write 并再次验证。
- 后续编辑 workflow 文件时，若用户在 IDE 打开了该文件，写入后应提示用户重新加载。

### 最终校验结果（二次重做后）
- `git diff --stat`：dev.yml 271 行变化、release.yml 254 行变化，两文件均已落盘。
- YAML 语法：两文件 `yaml.safe_load` 通过。
- dev.yml 结构：3 jobs(dev-release/build/publish)、dev_version output、concurrency、顶层 permissions、github-script@v7、target_commitish、matrix os_name、if-no-files-found=error、publish SHA256SUMS 全部 ✅。
- release.yml 结构：3 jobs(create-release/build/publish)、version output、顶层 permissions、Bump version 步骤、create-release make_latest=true、publish Upload assets make_latest=true、SHA256SUMS 全部 ✅。

---

## 修复 dev 构建版本号显示与检查更新失败（第三次调整）

### 用户反馈的 3 个问题（本机构建后实测）
1. dev 构建安装后，本机构建显示在"正式版"中（通道徽章位置错）
2. 开发版显示的版本号是 `v0.1.0`，需要显示 `vdev-**`（带 v 前缀）
3. 检查更新会失败报错

### 根因分析
- 问题1：CI 未设 `VITE_UPDATE_CHANNEL=dev`，vite 注入默认 `'latest'`，`getBuildChannel()` 返回 latest
- 问题2：`vite.config.ts` 的 `__APP_VERSION__` 取自 `pkg.version`（0.1.0），CI 未注入 `VITE_APP_VERSION=dev-<SHA>`
- 问题3：CI 未生成 `latest.json`/`dev.json` 静态清单（404），降级 GitHub API 时通道判断错

### 关键设计：分离"打包版本号"与"显示版本号"
- 打包版本号（msi/WiX 用，纯数字）：dev 保持 `0.1.0`，release 写入 `X.Y.Z`
- 显示版本号（前端 UI 用，可含 dev-）：通过 Vite 环境变量 `VITE_APP_VERSION` 注入 `__APP_VERSION__`

### 修改的文件
1. `vite.config.ts`：`__APP_VERSION__` 改为 `process.env.VITE_APP_VERSION || pkg.version`
2. `src/components/AboutPage.tsx`：加 `appVersion` prop，3 处硬编码改为 `v{appVersion || '0.1.0'}`
3. `.github/workflows/dev.yml`：Build frontend 加 `env: VITE_UPDATE_CHANNEL=dev, VITE_APP_VERSION=dev-<SHA>`；publish 加 `Generate dev.json manifest`
4. `.github/workflows/release.yml`：Build frontend 加 `env: VITE_UPDATE_CHANNEL=latest, VITE_APP_VERSION=X.Y.Z`；publish 加 `Generate latest.json manifest`

### JSON 清单格式（updater.ts 的 UpdateManifest 接口）
客户端下载路径：dev 通道 `releases/download/dev-latest/dev.json`；latest 通道 `releases/latest/download/latest.json`

### 校验结果
- git diff：dev.yml +58、release.yml +58、AboutPage.tsx ±7、vite.config.ts ±6，全部落盘
- YAML 语法通过；CI 结构校验通过（env 注入 + JSON 清单步骤到位）
- 前端 lint：AboutPage.tsx 无错误


---

## 修复实验性功能开关 + 关闭窗口提示 + 关闭标签页确认框

### 用户反馈的 3 个问题
1. 设置中实验性功能开关（Mermaid / Vim）无法打开
2. 关闭窗口提示对话框的"隐藏至托盘"应为复选框而非按钮，勾选后以后点关闭都隐藏至托盘
3. 关闭有未保存修改的标签页时，点"确认"直接丢弃了修改，应该确认=保存，并新增"丢弃"按钮

### 问题 1：实验性功能开关无法打开
**根因**：`src/components/SettingsPanel.tsx` 中 Mermaid 和 Vim 的 toggle 开关被硬编码为 `checked={false} readOnly`，并加了 `style={{ opacity: 0.5 }}`，完全无法交互——纯占位 UI，未接线到实际设置。

**修复**：
- `src/types/index.ts` — `AppSettings` 新增 `mermaid: boolean` 和 `vim: boolean` 字段
- `src-tauri/src/settings.rs` — Rust 端 `AppSettings` 新增 `mermaid` / `vim` 字段（`#[serde(rename_all = "camelCase", default)]` 自动映射 + 兼容旧配置）
- `src/App.tsx` — `DEFAULT_SETTINGS` 新增 `mermaid: false, vim: false`
- `src/components/SettingsPanel.tsx` — 开关改为 `checked={settings.mermaid}` / `checked={settings.vim}`，`onChange` 调用 `update({ mermaid/vim: e.target.checked })`，移除 `readOnly` 和 `opacity: 0.5`

### 问题 2：重新设计关闭窗口提示对话框
**原设计**：三按钮（隐藏至托盘 / 关闭 / 取消）+ "以后不再提示"复选框

**新设计**：双按钮（关闭 / 取消）+ 复选框"以后不再提示，默认隐藏至托盘"
- 勾选复选框 + 点关闭 → 保存 `skipClosePrompt: true` + `closeAction: 'minimize'`，以后点关闭都自动隐藏至托盘
- 不勾选 + 点关闭 → 仅本次关闭窗口
- 点取消 → 什么都不做

**修改文件**：
- `src/components/ConfirmDialog.tsx` — `showCloseActionDialog` 简化：移除 `tertiaryText` 参数，返回 `{ action: 'close' | 'cancel', dontAskAgain }`；`DialogType` 新增 `closeTab` 类型；新增 `showCloseTabDialog` API
- `src/App.tsx` — `handleCloseWindow` 更新：勾选后同时设置 `closeAction: 'minimize'`；移除 `result.action === 'minimize'` 分支
- `src/i18n/locales/zh-CN.ts` / `en.ts` — 更新提示语

### 问题 3：关闭标签页确认框（保存 / 丢弃 / 取消）
**原问题**：`closeTab` 用 `showConfirm` 返回 `true` 时直接关闭标签页（丢弃修改），与提示语"是否保存？"矛盾——用户点"确认"期望保存，实际却丢弃了。

**新设计**：三按钮对话框（`closeTab` 类型）
- **保存**（确认按钮，primary 蓝色）→ 保存内容后关闭标签页
  - 有路径：直接 `invoke('write_file_command')` 保存
  - 无路径（新文件）：弹出目录选择 + 文件名输入对话框（Tauri 环境）/ 浏览器下载
- **不保存**（丢弃按钮，danger 红色）→ 不保存直接关闭
- **取消** → 不关闭

**修改文件**：
- `src/components/ConfirmDialog.tsx` — 新增 `closeTab` 对话框类型和 `showCloseTabDialog` API，返回 `'save' | 'discard' | 'cancel'`
- `src/App.tsx` — `closeTab` 函数完全重写，根据用户选择执行保存/丢弃/取消；导入 `showAlert` / `showPrompt` 用于保存失败提示和文件名输入；移除未使用的 `showConfirm` 导入
- `src/i18n/locales/zh-CN.ts` / `en.ts` — 新增 `tab.closeTitle`、`tab.save`、`tab.discard`、`tab.cancel`、`tab.saveFailed`、`tab.selectSaveLocation`、`tab.enterFileName`

### ConfirmDialog 对话框类型总结（重构后）
| 类型 | 用途 | 按钮 | 特殊元素 |
|------|------|------|----------|
| `alert` | 单纯提示 | 确定 | - |
| `confirm` | 二选一确认 | 取消 / 确定 | - |
| `prompt` | 输入框 | 取消 / 确定 | input 输入框 |
| `closeAction` | 关闭窗口提示 | 取消 / 关闭 | "以后不再提示"复选框 |
| `closeTab` | 关闭标签页提示 | 取消 / 保存 / 不保存 | - |

### 遮罩点击行为
- `alert` / `confirm`：点击遮罩 = 取消
- `prompt` / `closeAction` / `closeTab`：点击遮罩无效（防误关）

### 校验结果
- TypeScript 类型检查：`npx tsc --noEmit` 零错误
- Lint：无错误
- 涉及文件：`src/types/index.ts`、`src-tauri/src/settings.rs`、`src/App.tsx`、`src/components/SettingsPanel.tsx`、`src/components/ConfirmDialog.tsx`、`src/i18n/locales/zh-CN.ts`、`src/i18n/locales/en.ts`

---

## 7 项 UI/UX 优化与修复

### 1. 设置页面平铺化
- 移除 `CollapsibleGroup` 卡片式折叠分组，改为 `FlatGroup` 平铺展示
- 所有设置分组始终展开，group-header 不可点击、无 chevron 图标
- 移除 `collapsedGroups` 状态、`toggleGroup` 函数
- CSS: `.settings-group` 直接卡片样式、header 加底部边线分隔、移除 chevron/动画

### 2. 不悬浮模式下工具栏高度减小
- `.editor-toolbar` padding: `10px 20px` → `6px 16px`，gap: `8px` → `6px`
- `.editor-toolbar .tb-btn`: `32x32` → `28x28`，font-size: `16px` → `14px`
- 文件：`src/styles/editor.css`

### 3. 合并有序列表按钮 + 新增代码块图标
- 有序列表：`1.` 按钮 + `▾` 箭头合并为 `.tb-ol-group` 按钮组（视觉相连的光角半径）
- 新增代码块按钮：`<svg>` 图标（双尖括号 <>），`execCmd('codeblock')` → `toggleCodeBlock()`
- 文件：`src/components/Editor.tsx`、`src/styles/editor.css`

### 4. 标题工具栏按钮合并为下拉选择
- 移除 H1/H2/H3 三个独立按钮，改为单个 `H▾` 下拉按钮
- 下拉列表：H1-H6 + 分隔线 + "正文（段落）"
- 新增 `headingPickerOpen` 状态 + `mousedown` 外部点击关闭
- 文件：`src/components/Editor.tsx`、`src/styles/editor.css`

### 5. 确认弹窗按钮配色对齐主题
- `.confirm-dialog-btn.primary`: `#3b82f6` → `var(--accent)`（warm terracotta #c96442）
- `.confirm-dialog-btn.primary:hover`: `#2563eb` → `var(--accent-hover)`
- `.confirm-dialog-btn.cancel:hover`: `rgba(0,0,0,0.04)` → `var(--fg-soft)`
- `.confirm-dialog-btn.tertiary:hover`: `#3b82f6` → `var(--accent)`
- `.confirm-dialog-icon.primary`: `rgba(59,130,246,0.1)/#3b82f6` → `var(--accent-soft)/var(--accent)`
- 文件：`src/styles/components.css`

### 6. 移除编辑器通用右键菜单
- `onScrollContextMenu` 中移除通用 `EditorContextMenu` 的 fallback 调用
- 仅保留：图片右键菜单（`ImageContextMenu`）、表格右键菜单（`TableContextMenu`）
- 移除 `showContextMenu`/`contextMenuPos`/`setShowContextMenu` 状态
- 移除 `EditorContextMenu` 组件渲染和 import
- 文件：`src/components/Editor.tsx`

### 7. 移除菜单栏视图模式按钮
- TopBar 应用菜单中移除「视图模式切换（Live/Read/Source）」区域
- 底部状态栏已有 `view-mode-group` 入口，避免功能重复
- 移除 `viewModes` 常量，`editorMode`/`onEditorModeChange` props 保留但前缀 `_` 标记未使用
- 文件：`src/components/TopBar.tsx`

### 校验
- `npx tsc --noEmit`：零错误通过

## Bug 修复：工具栏+设置页三个问题

### 1. OL 按钮 split button 视觉合并
- 问题：两个 `.tb-btn` 各自独立 hover 背景，看起来是分离的两个按钮
- 修复：`.tb-ol-group` 改为真正 split button：按钮间加 `border-left` 分隔线，hover 时分隔线变透明；箭头按钮固定 `width: 16px, font-size: 9px`；移除 JSX 中的 inline style，全部走 CSS 类
- 文件：`src/styles/editor.css`、`src/components/Editor.tsx`

### 2. 标题下拉展开时位置左移
- 原因：`.tb-heading-dropdown` 是 `inline-flex` 无固定宽度，下拉展开时浏览器 reflow 导致工具栏 flex 布局重算
- 修复：给 `.tb-heading-dropdown` 设置固定 `width: 28px; height: 28px`，防止 DOM 变化触发 reflow
- 文件：`src/styles/editor.css`

### 3. 设置页去卡片化 + 关于更新内容裁切
- 移除 FlatGroup 的标题 header（`.settings-group-header`），不再渲染 title/badge
- 移除 `.settings-group` 的 border / border-radius / background / box-shadow
- `.settings-group` 的 `overflow: hidden` 改为 `overflow: visible`，修复关于中更新日志内容被裁切
- 文件：`src/components/SettingsPanel.tsx`、`src/styles/components.css`

### 校验
- `npx tsc --noEmit`：零错误通过

## Bug 修复：设置页 closeAction 开关 + 编辑器保存格式变更

### 1. 设置页 closeAction 开关无响应（stale closure bug）
- **根因**：`FlatGroup` 中 closeAction 单选按钮的 `onClick` 连续调用了两次 `update()`：
  1. `update({ closeAction: opt.value })` — 修改 closeAction
  2. `update({ skipClosePrompt: false })` — 但此时 `settings` 仍是闭包中的旧值，`closeAction` 被覆写回旧值
- React 批处理下两次 `setSettings` 合并，最后一次胜出（含旧的 `closeAction`），外观上按钮状态不变
- **修复**：合并为单次 `update({ closeAction: opt.value, ...(opt.value !== 'ask' ? { skipClosePrompt: false } : {}) })`
- 文件：`src/components/SettingsPanel.tsx`

### 2. 编辑器保存时引入多余换行符和语法变动
- **根因**：TipTap 的 `onUpdate` 回调对所有 transaction 都会触发 `htmlToMarkdown` 序列化，包括**纯装饰器 transaction**（如 markdown 语法符号的显示/隐藏）。初次打开文档后，decoration 更新就触发了 `onChange`，将原始 Markdown 替换为序列化后的版本，格式已被改变。
- **修复**：在 `onUpdate` 中增加 `if (!transaction.docChanged) return` 检查，跳过所有装饰器 transaction，仅文档内容实际变更时才序列化回存
- 文件：`src/components/Editor.tsx`

### 校验
- `npx tsc --noEmit`：零错误通过

## 工具栏下拉框 + OL 按钮 + 侧边栏右键修复

### 1. 工具栏标题下拉框居中闪烁修复
- **根因**：`.heading-picker-dropdown` 复用了 `toolbarFloatIn` 动画关键帧，其中 `translateX(-50%)` 是为浮动工具栏设计的（`left:50%`），但下拉框是 `left:0`，导致动画期间居中 → 结束后回弹到左对齐
- **修复**：新建 `pickerFadeIn` 动画关键帧，仅含 `opacity` + `translateY(-4px → 0)` 的淡入上浮，无水平位移
- 文件：`src/styles/editor.css`

### 2. 有序列表改为单一下拉按钮
- 移除 `.tb-ol-group` split button（`1.` + `▾` 两个按钮），改为单个按钮 `1.▾`
- `openOlPicker` → `toggleOlPicker`，支持点击切换开关（已展开时再次点击关闭）
- 移除 `.tb-ol-group` 全部 CSS 规则
- 文件：`src/components/Editor.tsx`、`src/styles/editor.css`

### 3. 禁用侧边栏区域右键菜单
- `<aside>` 容器添加 `onContextMenu={(e) => e.preventDefault()}`
- 移除文件列表项上的 `onContextMenu` 处理函数
- 移除 `fileCtxMenu` 状态、`useEffect` 关闭监听、`clampMenuPos` 函数、右键菜单 JSX 渲染
- 文件：`src/components/Sidebar.tsx`

### 校验
- `npx tsc --noEmit`：零错误通过

## OL 下拉闪烁 + 状态栏图标 + closeAction 同步

### 1. OL 编号样式下拉框居中闪烁 →
- **根因**：`.ol-style-picker` 使用 `animation: toolbarFloatIn`，含 `translateX(-50%)`，动画结束 transform 移除后弹回原位
- **修复**：改用 `pickerFadeIn`（仅 opacity + translateY），与标题下拉统一
- 文件：`src/styles/markdown.css`

### 2. 状态栏设置图标
- SVG 从太阳图标（circle+射线）改为标准齿轮图标
- 文件：`src/App.tsx`

### 3. closeAction 与主页关闭按钮同步
- **根因**：`handleCloseWindow` 中"不再提示"硬编码 `closeAction: 'minimize'`
- **修复**：仅设 `skipClosePrompt: true`，保留当前 closeAction；增加安全回退（ask + skip=true → minimize）
- 文件：`src/App.tsx`

### 校验
- `npx tsc --noEmit`：零错误通过
