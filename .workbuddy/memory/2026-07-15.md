# 2026-07-15 工作日志

## 前端 UI 原型对齐重构（重大）
- **读取 prototype_index.html**：完整 Tolaria 设计原型（~2187行），含 titlebar/sidebar/editor/statusbar/settings/slash-menu/focus-mode 全套 CSS + HTML 结构。
- **重写 `src/index.css`**：从 Tailwind-only 升级为完整 Tolaria CSS 变量系统（对齐原型的 ~946 行 CSS），新增 --bg/--sidebar-bg/--fg/--muted/--marker/--accent/--border/--surface/--code-bg/--quote-bar/--scrollbar 等全部语义变量，light/dark 双主题。补充所有组件样式类（.titlebar/.sidebar/.file-tree/.toc-list/.welcome-screen/.editor-toolbar/.editor-inner/Markdown 渲染/.statusbar/.settings-panel/.toggle-switch/.focus-overlay/.slash-menu 等）。
- **重写 `src/components/TopBar.tsx`**：三段式布局——左侧 logo SVG + FkeMark 品牌 + app-menu 下拉菜单（设置/切换主题/专注模式/新建文件/打开文件夹/关于）+ sidebar-toggle 折叠按钮；中间 filename + unsaved-dot；右侧 win-ctrls。
- **重写 `src/components/Sidebar.tsx`**：双区块可折叠设计——「文件」(file-tree，活跃左边框高亮) + 「大纲」(TOC h1/h2/h3 层级缩进)，宽度 240px。
- **新建 `src/components/WelcomeScreen.tsx`**：FkeMark logo + 标语 + 新建/打开按钮 + 快捷键提示。
- **重写 `src/components/Editor.tsx`**：增加 editor-toolbar（H1/H2/B/I/代码/引用/列表/斜杠命令）、editor-scroll 居中容器、focus-overlay 覆盖层；移除 AppSettings 依赖（settings prop 改为内部自洽）。
- **新建 `src/components/SettingsPanel.tsx`**：右侧滑入面板（380px）——Logo+版本、编辑器设置组（字号/行高/宽度 radio）、行为组（自动保存/显示标记/括号补全/行号 toggle）、外观组、快捷键列表、关于区块。
- **重写 `src/App.tsx`**：整合所有新组件，新增 focus mode / immersive mode 切换（body class 驱动）、状态栏增强（保存状态圆点 saved/saving/unsaved、字数、模式指示器）、TOC 自动提取、键盘快捷键（Ctrl+S/N/O/,/Shift+F）。
- **验证结果**：tsc --noEmit 0 错误、vite build 成功（154 模块, CSS 27KB, JS 517KB gzip 163KB）。vitest 无测试文件（tests 目录不存在），跳过。

## 关键架构决策
- 样式方案从纯 Tailwind utility 升级为 **CSS 变量 + 语义化类名混合方案**：Tailwind 仍用于快速布局，但核心组件样式使用与原型一致的命名 CSS 类（如 .titlebar、.sidebar、.statusbar 等），确保视觉 1:1 还原原型。
- TopBar 移除了 onSave 按钮和 startDragging（改用 CSS `-webkit-app-region: drag` 原生拖拽）。
- Editor 不再接收 settings prop，字号/行高等由外部 CSS 类控制。

## 注意
- tests 目录在当前环境不存在（之前会话的测试文件未保留），需后续重建测试套件。

## UI 修复迭代（6 项问题修复）
1. **CSS 变量冲突修复**：`--muted` 被重复定义两次（文字色 vs Tailwind 背景色覆盖），导致图标/文字几乎不可见。拆分为 `--muted`(文字/图标 #6B7280) + `--muted-bg`(背景 #F2F1EC)，新增 `--icon-default`/`--icon-hover`/`--brand-main`/`--brand-accent` 专用变量。所有 `.tb-btn`/`.win-btn`/`.app-menu-btn`/`.sidebar-toggle`/`.settings-close` 的默认色从 `var(--muted)` 改为 `var(--icon-default)`，确保图标默认可见。
2. **新建文件功能**：`handleNewFile` 创建带默认内容的未命名文档（`# 未命名文档\n\n开始编写...`），编辑器立即可编辑；保存时通过 Tauri dialog 选择目录 + 输入文件名；浏览器环境用 `<a download>` 模拟保存。
3. **打开文件夹功能**：接入 `@tauri-apps/api/dialog` 的 `open()` API（Tauri v1 `dialog-all` feature 已在 Cargo.toml 启用），支持 Markdown 文件选择；浏览器环境用 `<input type=file>` 降级。
4. **设置全部生效**：扩展 `AppSettings` 类型新增 `lineHeight`/`editorWidth`/`showMarkers`/`autoBracket`/`showLineNumbers`/`miniSidebar` 字段。SettingsPanel 所有 toggle/radio 绑定到实际状态。Editor 接收 settings prop 并应用：字号通过 `el.style.fontSize`、行高通过 `el.style.lineHeight`、宽度通过 `el.style.maxWidth`、标记显隐通过 `body.hide-markers` class、括号补全通过 keydown 拦截配对、行号通过 `LineNumbers` 组件渲染。
5. **构建验证**：tsc 0 错误、vite build 成功（155 模块, CSS 27KB, JS 519KB gzip 163KB）。tauri build 后台运行中。

## 8 项功能增强迭代
1. **状态栏模式点击切换**：右下角模式文字可点击循环切换 normal→focus→immersive→normal。
2. **设置按钮移至左下角**：状态栏左下角增加齿轮按钮，不再依赖标题栏下拉菜单。
3. **侧边栏宽度拉伸**：侧边栏右边缘增加 4px 拖拽手柄（.sidebar-resizer），范围 180-400px，实时拖拽改变宽度。
4. **侧边栏状态持久化**：sidebarOpen/sidebarWidth/sidebarCollapsed/filesCollapsed/tocCollapsed/expandedFolders 全部存 localStorage，重启恢复。
5. **字体大小自定义输入**：从 3 个 radio 按钮改为数字输入框 + range 滑块（8-48pt），支持手动输入精确值。
6. **文件夹树展示**：Rust 后端新增 `scan_directory` 命令（递归扫描 .md/.markdown 文件，跳过隐藏文件，文件夹优先排序），前端 Sidebar 递归渲染树形结构，文件夹可展开/折叠。
7. **小地图**：编辑器左侧 80px 宽缩略图（Minimap 组件），按行渲染 Markdown 内容缩略，标题/列表/引用/代码块有不同颜色。右击滚动条弹出菜单切换显隐，设置面板增加小地图 toggle。AppSettings 新增 `showMinimap` 字段。
8. **大纲跳转**：点击大纲标题项，通过 DOM 查询编辑器中对应 h1/h2/h3 标签 scrollIntoView 平滑滚动定位，支持模糊匹配。

### Rust 后端变更
- `settings.rs`：AppSettings 新增 7 个字段（lineHeight/editorWidth/showMarkers/autoBracket/showLineNumbers/miniSidebar/showMinimap），使用 `#[serde(default)]` 确保旧配置兼容。
- `file_system.rs`：新增 `FileTreeNode` 结构体 + `scan_directory` 递归扫描函数（只返回 .md/.markdown 文件和包含它们的文件夹）。
- `main.rs`：注册 `scan_directory` 命令。


