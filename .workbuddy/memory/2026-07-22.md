# 2026-07-22 工作日志

## 今日工作概览

今天按 P0 → P1 的顺序完成安全性、更新流程、提示语多语言、系统文件关联与编辑弹窗交互修复。功能修改均按独立事项分别提交，提交标题遵循现有 Conventional Commits 规范，提交正文使用中文详细列出子项。

---

## 1. P0：修复 Markdown 渲染和导出注入风险

### 问题
- 内置 Markdown 行内渲染器会把普通文本、链接地址、图片属性等内容直接拼接到 HTML 中。
- 恶意或异常 Markdown 可能通过标签、事件属性、危险协议或非法尺寸值进入预览和导出结果。
- HTML 导出维护了一套独立的简化 Markdown 转换逻辑，与应用预览渲染规则不一致。

### 修改
- 普通文本统一执行 HTML 转义，仅保留解析器主动生成的安全 HTML 片段。
- 链接和图片地址增加协议白名单，支持 http、https、mailto、锚点及相对路径，其他协议回退为安全地址。
- 图片的 src、alt、title、style 等属性统一执行属性转义。
- 图片宽高仅接受合法数字与 px、%、em、rem、vw、vh 等尺寸单位。
- HTML 导出复用 Markdown 预览渲染器，删除重复且安全边界不一致的转换实现。
- 更新核心测试，覆盖转义后的预期结果。

### 涉及文件
- `src/utils/markdown.ts`
- `src/utils/importExport.ts`
- `tests/core.test.ts`

### 提交
- `781889e fix: 修复 Markdown 渲染和导出注入风险`

---

## 2. P0：修复保存流程和更新安装安全边界

### 问题
- 回收站恢复和永久删除接口可接收任意路径，缺少“目标必须位于应用回收站目录内”的约束。
- 更新安装器文件名与安装路径缺少完整规范化和目录边界校验。
- 更新包在缺少 SHA-256 时仍可能继续处理，安装文件类型也缺少平台白名单。
- 顶栏保存按钮没有真正触发当前文档保存。
- 安装更新前未集中保存所有已修改文档，存在未保存内容丢失风险。

### 修改
- 对回收站路径执行 canonicalize，并限制恢复/永久删除目标必须位于应用回收站目录。
- 清理更新版本号和安装器文件名中的路径分隔符及非法值。
- 安装前校验安装器必须位于应用更新目录，并按平台限制可执行安装包扩展名。
- SHA-256 校验值改为必填，缺失时直接拒绝安装。
- 顶栏保存按钮接入实际保存函数。
- 安装更新前同步当前标签内容并逐一保存所有已修改且已有磁盘路径的文档。

### 涉及文件
- `src-tauri/src/file_system.rs`
- `src-tauri/src/updater.rs`
- `src/App.tsx`

### 提交
- `f9fd92b fix: 修复保存流程和更新安装安全边界`

---

## 3. P1：修复项目提示语并接入多语言配置

### 问题
- 编辑器、确认框、空状态、回收站、侧边栏、命令菜单、导入导出和更新流程中仍存在硬编码中文或英文提示。
- 切换语言后，部分按钮、标题、占位符、错误提示和模板内容不会同步切换。

### 修改
- 将界面中遗留的硬编码提示替换为 i18n key。
- 补充中文与英文词条，覆盖工具栏、源码编辑、分栏拖拽、图片编辑、确认框、空状态模板、回收站、侧边栏、Slash 菜单、导入导出和更新错误等场景。
- 空状态模板内容由固定文本改为多语言内容 key。
- 统一错误详情参数插值，避免仅标题多语言、具体提示仍硬编码。

### 涉及范围
- `src/App.tsx`
- `src/components/ConfirmDialog.tsx`
- `src/components/Editor.tsx`
- `src/components/EmptyState.tsx`
- `src/components/RecycleBinPanel.tsx`
- `src/components/SettingsPanel.tsx`
- `src/components/Sidebar.tsx`
- `src/components/SlashMenu.tsx`
- `src/components/editor/EditorPickers.tsx`
- `src/components/extensions/ImageUploadNode.tsx`
- `src/i18n/locales/zh-CN.ts`
- `src/i18n/locales/en.ts`
- `src/utils/importExport.ts`
- `src/utils/updater.ts`

### 提交
- `2c4f6da fix: 提示语接入多语言配置`

---

## 4. 修复更新成功后误报“更新未生效”

### 问题
开发版构建使用 `VITE_APP_VERSION=dev-提交号` 作为真实显示版本，但 MSI/WiX 包版本仍必须保持纯数字。安装完成后，Rust 自愈逻辑读取应用包版本，与待安装记录中的 `dev-提交号` 比较，导致实际安装成功仍显示“更新未生效，已保留当前版本”。

### 修改
- 新增统一的当前构建版本读取逻辑，优先读取构建时注入的 `VITE_APP_VERSION`。
- 未注入显示版本的正式构建继续回退到 Tauri 应用包版本。
- 更新安装记录、安装完成校验和回滚记录统一使用同一版本来源。
- 保持开发版显示版本与安装后的校验版本一致，消除成功安装后的误报。

### 涉及文件
- `src-tauri/src/updater.rs`

### 验证
- `cargo check`：通过。
- `cargo test`：2 个 Rust 测试通过。

### 提交
- `28ce43a fix: 修复更新成功误报未生效`

---

## 5. 增加启动及后台定时更新检查通知

### 需求
- 软件启动时主动检查更新。
- 软件隐藏到托盘或在后台持续运行时，定时检查新版本。
- 发现新版本后弹出包含版本信息的通知。

### 修改
- 启动后延迟 2 秒执行首次检查，避免与启动加载竞争。
- 主窗口运行期间每 30 分钟主动检查一次，辅助窗口不重复发起请求。
- 自动检查发现新版本后，同时展示应用内更新提示与 Windows 系统通知。
- 系统通知标题和正文接入中文、英文多语言配置。
- 同一版本在一次应用运行期间只发送一次系统通知，避免每个周期重复打扰。
- 启动检查、后台检查和手动检查共用运行锁，避免并发请求和重复弹窗。
- 接入 Tauri notification 插件、默认权限及生成的 ACL Schema。
- 更新设置提示，将“启动时检查”调整为“启动及后台运行时定时检查”。

### 涉及文件
- `src/App.tsx`
- `src/types/index.ts`
- `src/i18n/locales/zh-CN.ts`
- `src/i18n/locales/en.ts`
- `package.json` / `package-lock.json`
- `src-tauri/Cargo.toml`
- `src-tauri/src/lib.rs`
- `src-tauri/capabilities/default.json`
- `src-tauri/gen/schemas/*`

### 验证
- `npm run build`：通过。
- `npm test -- --run`：3 个测试文件、57 个测试全部通过。
- `cargo check`：通过。
- `cargo test`：2 个 Rust 测试通过。

### 提交
- `aec8bb9 feat: 增加后台定时更新检查通知`

---

## 6. 注册 Markdown 默认应用候选

### 问题
Windows 为 `.md` 文件选择默认应用或使用“打开方式”时，FkeMark 不在候选列表中，用户只能手动浏览安装目录选择可执行文件。

### 修改
- 在 Tauri 安装包配置中注册 `.md` 与 `.markdown` 文件关联。
- 将 FkeMark 声明为 Markdown 编辑器角色。
- 补充 `text/markdown` MIME 类型、文件类型名称和 Windows 文件描述。
- 安装后由系统使用已注册的安装路径和应用图标生成默认应用候选项。

### 涉及文件
- `src-tauri/tauri.conf.json`

### 验证
- Tauri 配置解析通过。
- 前端生产构建通过。
- Rust 桌面程序编译完成。
- Tauri 最后重命名调试程序时受项目现有自定义 target 目录影响失败；失败发生在配置解析和程序编译之后，与本次文件关联字段无关。

### 提交
- `5218895 fix: 注册 Markdown 默认应用候选`

---

## 7. 防止链接和图片编辑弹窗误关闭

### 问题
链接、图片地址/替代文本、图片尺寸弹窗点击遮罩空白区域会立即关闭。用户输入过程中误点空白区域时，未确认的内容会直接丢失。

### 修改
- 链接编辑弹窗不再因点击遮罩空白区域自动关闭。
- 图片地址与替代文本编辑弹窗不再因点击空白区域自动关闭。
- 图片尺寸调整弹窗不再因点击空白区域自动取消。
- 保留“确定”“取消”和 Escape 等显式关闭方式，用户必须明确操作后才会关闭弹窗。
- 图片右键菜单等非输入型临时菜单仍保留原有的点击外部关闭行为。

### 涉及文件
- `src/components/Editor.tsx`
- `src/components/editor/EditorMenus.tsx`

### 验证
- `npm run build`：通过。
- `npm test -- --run`：3 个测试文件、57 个测试全部通过。

### 提交
- `10288ec fix: 防止链接和图片编辑弹窗误关闭`

---

## 8. 统一 Rust 源码格式

### 修改
- 按 `rustfmt` 规则整理文件系统、应用入口、Markdown 转换和设置模块。
- 统一导入顺序、链式调用换行、缩进及匹配分支格式。
- 未修改业务逻辑、配置内容和运行行为。

### 涉及文件
- `src-tauri/src/file_system.rs`
- `src-tauri/src/lib.rs`
- `src-tauri/src/markdown.rs`
- `src-tauri/src/settings.rs`

### 验证
- `cargo check`：通过。
- `cargo test`：2 个 Rust 测试通过。
- 涉及文件的 `rustfmt` 检查通过。

### 提交
- `e799709 style: 统一 Rust 源码格式`

---

## 9. 修复拖拽图片初始化显示失败

### 问题
图片拖拽上传成功且磁盘路径正确，但实时编辑和阅读模式显示图片加载失败；分栏模式可以正常显示，进入源码模式修改内容后图片也会恢复。原因是 Rust 上传完成事件先返回 Markdown 相对路径，编辑器直接用该相对路径替换上传占位节点。WebView 无法按文档目录解析该地址，而分栏预览及源码重新解析流程会额外执行本地资源路径转换，因此不同模式表现不一致。

### 修改
- 上传完成事件返回图片相对路径后，先通过 Tauri 资源路径转换生成 WebView 可加载的 URL。
- 转换完成后再将上传占位节点替换为正式图片节点。
- Markdown 序列化时仍保存 `./assets/图片名` 相对路径，保持文档可迁移性。
- 统一实时编辑、阅读和分栏模式首次上传后的图片显示结果。

### 涉及文件
- `src/components/Editor.tsx`

### 验证
- `npm run build`：通过。
- `npm test -- --run`：3 个测试文件、57 个测试全部通过。

### 提交
- `3ad7938 fix: 修复拖拽图片初始化显示失败`

---

## 10. 修复系统“打开方式”文件定位错误

### 问题
Windows 通过“打开方式”使用 FkeMark 打开 Markdown 文件时，应用窗口显示的是原有活动标签或其他文件，并非用户本次选择的目标文件。根因是单实例插件虽然收到了系统传入的命令行参数和工作目录，但回调直接忽略了这些参数；首次启动流程也没有读取文件关联传入的目标路径。

### 修改
- 新增系统启动参数解析逻辑，从命令行参数中提取 `.md` 与 `.markdown` 文件。
- 相对路径统一按启动进程工作目录解析，并过滤选项参数、不存在文件和非 Markdown 文件。
- 对重复路径进行规范化和去重，避免同一文件重复创建标签。
- 首次启动时由主窗口主动获取系统传入的目标文件并复用现有打开流程。
- 已有实例运行或隐藏在后台时，单实例回调恢复主窗口并把本次目标文件事件定向发送给主窗口。
- 前端监听系统打开事件，并通过最新的文件打开函数加载或激活用户选择的文件，避免继续显示旧标签。
- 新增 Rust 单元测试，覆盖相对路径、大小写扩展名、无效文件和重复参数。

### 涉及文件
- `src-tauri/src/lib.rs`
- `src/App.tsx`

### 验证
- `cargo test`：3 个 Rust 测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `npm test -- --run`：3 个测试文件、57 个前端测试全部通过。

### 提交
- `75a4134 fix: 修复系统打开方式文件定位`

---

## 11. 兼容更新清单缺失 SHA-256 校验值

### 问题
软件检查到更新后执行应用内下载，会报错 `missing SHA-256 checksum; automatic installer download was rejected`，导致自动更新无法继续。

### 根因
2026 年 7 月 18 日发布的当前正式版 `latest.json` 生成时间早于更新清单内嵌 `sha256` 的流程调整，下载资源只包含文件名、URL 和大小。同一 GitHub Release 已包含 `SHA256SUMS`，但客户端未读取该文件回填哈希。后端安全校验按设计拒绝了缺少 SHA-256 的安装包。

### 修改
- 静态更新清单缺少 `sha256` 时，自动读取同一 Release 的 `SHA256SUMS`。
- 优先使用清单中的 `tagName` 锁定校验文件版本，避免 `latest` 切换期间读取到不同版本的哈希。
- GitHub API 降级路径优先使用 Release 资源中的 `SHA256SUMS` 下载地址，并保留按 tag 构造地址的兼容处理。
- 新增 sha256sum 标准格式解析，支持双空格、二进制 `*` 标记、带空格文件名和 `./` 相对路径。
- 只回填缺失的哈希，不覆盖新版清单已提供的 `sha256`。
- 下载前在前端再次检查校验值，仍缺失时显示中英文多语言错误提示。
- 保留 Rust 后端“缺少 SHA-256 即拒绝自动下载”的安全边界，未降低安装包完整性要求。
- 新增单元测试，覆盖校验清单格式和旧版更新清单的哈希回填流程。

### 涉及文件
- `src/utils/updater.ts`
- `src/i18n/locales/zh-CN.ts`
- `src/i18n/locales/en.ts`
- `tests/updater.test.ts`

### 验证
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `npm test -- --run`：4 个测试文件、60 个测试全部通过。

### 提交
- `5eea293 fix: 兼容更新清单缺失 SHA-256 校验值`

---

## 12. 修复编辑弹窗边界与链接、网络图片交互

### 问题
- 链接和图片编辑弹窗虽然已禁止点击遮罩关闭，但靠近编辑区边缘时仍可能被裁切，只显示部分内容。
- 编辑器内临时菜单使用全局点击监听时，点击侧边栏、顶部栏或底部状态栏也会触发关闭，影响应用其他区域的正常操作。
- 实时编辑模式下已有链接不能稳定修改显示文本和地址，普通点击、双击及组合键点击也缺少明确区分。
- HTTP/HTTPS 网络图片地址需要明确保留为网络资源，不能被本地资源路径转换逻辑误处理。

### 根因
- 图片编辑浮窗按窗口坐标直接定位在编辑器容器内，没有把窗口坐标换算为编辑区坐标，也没有根据弹窗实际尺寸计算右侧和底部边界。
- 斜杠菜单、表格选择器、有序列表选择器、代码语言选择器及右键菜单各自维护定位或关闭逻辑，边界处理不一致，部分关闭监听挂载在整个文档上。
- 链接弹窗打开后依赖编辑器当前选区；输入框获取焦点后选区可能变化，保存时无法可靠定位原链接范围，也无法同时更新显示文本。
- 网络资源识别规则大小写敏感，且未统一排除 `blob:` 等无需本地路径转换的地址。

### 修改
- 新增统一浮层边界计算工具，根据弹窗真实宽高把位置限制在窗口或编辑区可视范围内。
- 链接弹窗、图片尺寸弹窗和图片地址编辑弹窗限制在编辑区域内显示，不再覆盖侧边栏、顶部栏和底部状态栏。
- 图片编辑弹窗将窗口点击坐标换算为编辑区本地坐标；靠近四角时自动向内调整，并为小窗口增加最大宽高和内部滚动。
- 为图片/表格右键菜单、斜杠菜单、表格网格选择器、有序列表选择器和代码语言选择器统一增加边界自适应。
- 将编辑器临时菜单的外部关闭监听收敛到编辑区域；点击侧边栏、顶部栏或底部状态栏不再误关闭，点击编辑视图空白区域仍可关闭非输入型菜单。
- 复查其他弹出组件：需要填写内容的链接、图片地址和图片尺寸弹窗只允许通过确定、取消或 Escape 显式关闭；非输入型临时菜单保留编辑视图内点击外部关闭。
- 打开已有链接时保存稳定的文档范围，保存时可同时修改显示文本和链接地址，并保留原文本上的其他格式标记。
- 实时编辑模式单击链接打开编辑弹窗；双击或 Ctrl/Command 点击链接时调用系统外部打开接口，交给默认浏览器处理，不在软件内部导航。
- 阅读模式单击链接继续使用系统默认浏览器打开，双击和 Ctrl/Command 点击同样立即外部打开。
- 外部链接统一校验 HTTP、HTTPS 和邮件协议；Tauri 调用系统应用失败时不再回退到 WebView，防止链接在软件内部打开。
- 本地资源识别改为大小写不敏感，明确保留 `http:`、`https:`、`data:` 和 `blob:` 地址；第三方 Markdown 引擎不再把这些网络或内存图片地址改写为本地资源协议。
- 新增浮层四角边界、两套 Markdown 引擎网络图片地址与外链协议校验回归测试。

### 涉及文件
- `src/components/Editor.tsx`
- `src/components/SlashMenu.tsx`
- `src/components/editor/EditorMenus.tsx`
- `src/components/editor/EditorPickers.tsx`
- `src/styles/menus.css`
- `src/utils/popupPosition.ts`
- `src/utils/asset.ts`
- `src/utils/updater.ts`
- `src/utils/markdown.third.ts`
- `src/i18n/locales/zh-CN.ts`
- `src/i18n/locales/en.ts`
- `tests/core.test.ts`
- `tests/markdown.third.roundtrip.test.ts`
- `tests/updater.test.ts`

### 验证
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `npm test`：4 个测试文件、66 个测试全部通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- `84a8fd9 fix: 修复编辑弹窗边界与链接交互`

---
## 13. 增加编辑器主题配色

### 需求
在设置页主题选项中保留基础主题，并增加 Absolutely、Ayu、Catppuccin、Codex、Dracula、Everforest、GitHub、Gruvbox、Linear、Vercel、VS Code Plus、Xcode 等 12 套配色，同时保持系统主题深浅色跟随逻辑稳定。

### 修改
- 新增统一主题列表，集中维护主题 id、显示文案、亮暗属性、分组和预览色。
- 设置页主题入口从三项按钮改为分组下拉；基础主题与配色主题分组展示，方便后续选择。
- 扩展应用主题类型与启动/设置保存逻辑，未知旧主题会回退到系统主题；系统主题继续跟随系统深浅色。
- 为 12 套配色增加 CSS 变量，并为深色配色复用深色语法高亮、选择器和设置页提示样式。
- 补充中英文文案与主题回归测试，覆盖清单完整性、持久化容错和深浅色解析。

### 涉及文件
- `src/types/index.ts`
- `src/utils/themes.ts`
- `src/App.tsx`
- `src/main.tsx`
- `src/providers/ThemeProvider.tsx`
- `src/components/SettingsPanel.tsx`
- `src/components/TopBar.tsx`
- `src/styles/variables.css`
- `src/styles/editor.css`
- `src/styles/markdown.css`
- `src/styles/components/select.css`
- `src/styles/components/settings-page.css`
- `src/i18n/locales/zh-CN.ts`
- `src/i18n/locales/en.ts`
- `tests/core.test.ts`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `npm test`：4 个测试文件、69 个测试全部通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- `c92586a feat: 增加编辑器主题配色`

---
## 14. 优化侧栏文件树展示布局与折叠图标

### 需求
优化侧栏文件树的展示布局与文件夹折叠图标，让层级、图标和交互更符合软件整体风格。

### 修改
- 将文件夹折叠状态从右侧文字箭头调整为文件夹图标前的 SVG 折叠箭头，展开时使用旋转状态表达层级开合。
- 统一文件夹和文件行的图标列，为文件、历史文件夹和最近文件列表补充不可见占位，保证名称纵向对齐。
- 调整文件树行高、字号、间距、缩进和圆角选中态，让侧栏更紧凑，更接近桌面软件文件树布局。
- 重绘打开和关闭文件夹图标，移除旧的含文件圆点状态，保持更清爽的软件风格。
- 删除不再使用的旧 chevron、folder-toggle 和 file-children 样式，避免遗留样式影响后续维护。

### 涉及文件
- `src/components/Sidebar.tsx`
- `src/styles/sidebar.css`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `npm test`：4 个测试文件、69 个测试全部通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- `e699ce0 style: 优化侧栏文件树展示`

---
## 15. 修复设置行为菜单多语言文案缺失

### 问题
设置页“行为”菜单中的关闭窗口行为配置显示了 `window.closeAction.*` 多语言 key，缺少对应的中英文语言文案。

### 根因
设置页新增关闭窗口行为配置后，组件已经引用 `window.closeAction.*` 文案，但 `zh-CN` 和 `en` 语言包没有补齐这些 key；同一关闭流程中的提示弹窗也缺少 `window.closePrompt.*` 文案。

### 修改
- 为“关闭窗口行为”设置项补齐标题、说明、三个选项、已记住提示和恢复询问按钮的中英文文案。
- 为关闭窗口确认弹窗补齐标题、正文、“以后不再提示”、最小化到托盘和直接退出按钮的中英文文案。
- 新增设置多语言回归测试，覆盖窗口关闭行为相关文案在所有语言包中都有实际内容，避免再次显示 key。

### 涉及文件
- `src/i18n/locales/zh-CN.ts`
- `src/i18n/locales/en.ts`
- `tests/core.test.ts`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `npm test`：4 个测试文件、70 个测试全部通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- `18025b0 fix: 补齐设置行为窗口关闭文案`

---
## 16. 修复第三方 Markdown 引擎松散表格渲染

### 问题
从聊天或文档中复制的 Markdown 表格在每行之间带空行时，预览没有生成表格，而是显示为普通段落。

### 根因
第三方引擎使用 markdown-it，表格语法要求表头行、分隔行和内容行连续。空行会结束表格块；原管线没有在进入 markdown-it 前兼容清理表格内部空行，导致分隔行无法紧跟表头行。

### 修改
- 在第三方 Markdown 渲染入口增加松散表格归一化，只移除同一个表格块内部的空行。
- 保持代码块内内容不参与归一化，避免示例代码里的管道文本被误处理。
- 在归一化后再提取表格分隔线和列表元数据，保证 `data-separators` 仍能正确注入。
- 新增第三方引擎回归测试，覆盖表格行之间有空行时仍渲染为 `<table>`。

### 涉及文件
- `src/utils/markdown.third.ts`
- `tests/markdown.third.roundtrip.test.ts`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- `npm test -- tests/markdown.third.roundtrip.test.ts`：1 个测试文件、29 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `npm test`：4 个测试文件、71 个测试全部通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `fix: 兼容第三方引擎松散表格渲染` 一并提交。

---
## 17. 修复引用渲染高度和空引用分裂

### 问题
引用语法在编辑区渲染后整体高度偏高；在部分内容中还会偶尔出现上下两个引用块，上方引用包含文字，下方引用为空，而分栏静态预览看起来正常或不明显。

### 根因
- 引用块自身上下 margin、padding 与内部段落默认 16px 底部间距叠加，导致单条引用显示偏高。
- 第三方引擎 markdown-it 遇到分隔后的单独空引用行（如 `> 文本` 后隔空行再出现 `>`）会生成第二个空 `blockquote`。
- 静态预览中的空 `blockquote` 没有段落内容，不太明显；进入 TipTap 编辑器后，TipTap 会按引用块 schema 自动补一个空段落，于是显示成下方独立的空引用块。

### 修改
- 收紧引用块上下间距、左侧缩进和左边框宽度，让引用展示更紧凑，更符合编辑器整体风格。
- 单独重置引用块内部段落间距，避免通用段落底部间距叠加到引用块中。
- 在第三方 Markdown 渲染后处理阶段移除空引用块，避免 TipTap 自动补空段落后形成可见的第二个空引用。
- 新增第三方引擎回归测试，覆盖分隔空引用不会生成第二个空引用块，以及仅包含空引用时回退为空段落。

### 涉及文件
- `src/styles/editor.css`
- `src/utils/markdown.third.ts`
- `tests/markdown.third.roundtrip.test.ts`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- `npm test -- tests/markdown.third.roundtrip.test.ts`：1 个测试文件、31 个测试全部通过。
- `npm test`：4 个测试文件、73 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `fix: 修复引用渲染高度和空引用分裂` 一并提交。

---
## 18. 修复 Dev 流水线 changelog 获取范围

### 问题
Dev GitHub 流水线生成开发版 changelog 时，通过本地版本标签排序猜测上一版本；当未找到标签时会直接执行 `git log HEAD`，从而把仓库全部历史提交写入开发版 Release，而不是只展示上一次正式发布后的变更。

### 根因
- 原逻辑只选择本地版本号最大的 `v*` 标签，无法确认该标签是否对应 GitHub 已发布的正式 Release 包。
- 无匹配标签时将日志范围回退为 `HEAD`，等价于遍历并打印当前分支可见的全部提交历史。
- changelog 生成步骤没有接收 GitHub 正式 Release 的真实 tag，发布状态与 Git 标签扫描逻辑彼此脱节。

### 修改
- 在 Dev 工作流中新增正式发布节点解析步骤，调用 GitHub `getLatestRelease` 接口，只获取已发布、非草稿、非预发布的最新正式 Release。
- 将正式 Release 的 tag 显式传入 changelog 生成脚本，不再扫描和猜测本地 `v*` 标签。
- changelog 范围固定为 `refs/tags/<正式版本>..<当前构建提交>`，只收集正式发布之后的开发分支提交。
- 当正式 Release tag 未在本地检出时，仅拉取该精确 tag 后继续生成，避免范围基准缺失。
- 仓库尚无正式 Release 时跳过提交历史，只保留开发版说明，不再退化为打印全部日志。
- 为 Git 提交信息读取和 GitHub 输出文件显式使用 UTF-8，保证中英文 changelog 在不同运行环境下稳定处理。

### 涉及文件
- `.github/workflows/dev.yml`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- GitHub `releases/latest` 实际返回 `v0.1.5`，确认当前正式发布基准识别正确。
- 正式发布解析脚本模拟验证通过：正常场景输出 `v0.1.5`，404 场景输出空 tag 并给出警告。
- changelog 脚本以 `v0.1.5..HEAD` 运行通过：验证时全部历史为 108 条，正式发布后的范围仅包含 6 条提交。
- 无正式 Release 回退场景运行通过：输出中不包含任何历史提交。
- `.github/workflows/dev.yml` YAML 解析通过，`dev-release` 共识别 7 个步骤。
- `npm test`：4 个测试文件、73 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `fix: 修复开发版变更日志获取范围` 一并提交。

---
## 19. 编辑器标签右键增加打开文件所在位置

### 需求
在编辑器顶部的文件标签右键菜单中新增“打开当前文件所在位置”，点击后调用当前操作系统的文件管理器，并定位到该标签对应的文件。

### 修改
- 为具有真实文件路径的编辑器标签增加“打开当前文件所在位置”菜单项；未保存或没有路径的临时标签不显示该操作。
- 新增 Tauri `reveal_in_file_manager` 命令，并通过现有前后端命令通道调用，避免放宽 Shell 插件的文件系统访问范围。
- Windows 使用资源管理器选中文件，macOS 使用 Finder 定位文件，Linux 打开文件所在目录。
- 系统调用前校验目标路径存在且为普通文件，定位失败时通过多语言 Toast 展示具体错误。
- 补充中英文菜单和错误文案，并增加所有语言文案完整性、文件路径校验回归测试。

### 涉及文件
- `src/components/TabBar.tsx`
- `src/i18n/locales/zh-CN.ts`
- `src/i18n/locales/en.ts`
- `src-tauri/src/file_system.rs`
- `src-tauri/src/lib.rs`
- `tests/core.test.ts`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- `npm test -- tests/core.test.ts`：1 个测试文件、30 个测试全部通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：5 个 Rust 测试全部通过。
- `npm test`：4 个测试文件、74 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `feat: 标签右键增加打开文件所在位置` 一并提交。

---
## 20. 修复开始写作浮层和模板换行渲染

### 问题
通过新建文本文件或标签栏新建按钮进入新文档后，“开始写作”浮层存在两项异常：选择空白文档后浮层仍覆盖在编辑器上方；选择日记、会议记录、待办清单等模板后，内容中的 `\n` 被显示为普通字符，Markdown 标题、列表和代码块均未渲染。

### 根因
- 空状态显示条件只判断文档内容是否为空，没有利用已有的修改状态区分“初始空文档”和“用户已经选择空白文档”。
- 中文模板内容和中英文默认文档内容使用了双重转义换行 `\\n`，翻译函数返回后仍是字面量字符，Markdown 引擎没有收到真实的多行文本。

### 修改
- 空状态浮层只在活跃文档尚未修改且内容为空时显示；选择空白文档后，现有 `isModified` 状态会立即阻止浮层继续覆盖编辑器。
- 将中文日记、会议记录、待办清单、技术笔记、读书笔记模板中的双重转义换行改为真实换行转义。
- 同步修复中英文默认新建文档内容的双重转义，保证新建文本文件和标签栏新建入口行为一致。
- 增加模板回归测试，逐一验证所有语言的默认内容和示例模板不包含字面量 `\n`、具有真实换行，并可由内置和第三方 Markdown 引擎生成一级标题。

### 涉及文件
- `src/App.tsx`
- `src/i18n/locales/zh-CN.ts`
- `src/i18n/locales/en.ts`
- `tests/core.test.ts`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- 修复前新增测试可稳定复现字面量 `\n` 问题。
- `npm test -- tests/core.test.ts`：1 个测试文件、31 个测试全部通过。
- `npm test`：4 个测试文件、75 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `fix: 修复开始写作浮层和模板渲染` 一并提交。

---
## 21. 修复 Dev 更新包 SHA-256 获取失败

### 问题
Dev 构建检查到新版本后，点击下载会提示“更新包缺少 SHA-256 校验值”，自动下载被安全校验拒绝。

### 根因
- Dev 发布长期复用 `dev-latest` 固定标签，客户端请求 `dev.json` 和 `SHA256SUMS` 时可能命中上一轮发布的缓存，导致清单版本、安装包文件名和校验值不一致。
- 静态清单不可用并降级到 GitHub Release API 后，安装包资源能够正常识别，但校验文件仍通过普通 Release 下载地址获取；该地址请求失败时，API 资源本身提供的下载接口没有被使用，因此安装包信息缺少 `sha256`。

### 修改
- 为 Dev 通道的 `dev.json` 和 `SHA256SUMS` 请求增加基于当前时间的防缓存参数，确保固定标签始终读取本轮发布内容；正式版更新地址保持原有行为。
- 扩展 GitHub Release 资源类型，读取校验资源的 API 地址。
- GitHub API 降级路径优先通过资源 API 和 `application/octet-stream` 请求头读取 `SHA256SUMS`，再按安装包文件名回填 SHA-256。
- 保留下载前必须具有 SHA-256 的安全边界，没有降低或跳过完整性校验。
- 增加两项回归测试，覆盖 Dev 固定标签旧缓存和静态清单失败后的资源 API 校验值回填。

### 涉及文件
- `src/utils/updater.ts`
- `tests/updater.test.ts`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- 修复前两项新增测试分别复现旧 Dev 清单被读取、API 降级后校验值为空的问题。
- `npm test -- tests/updater.test.ts`：1 个测试文件、6 个测试全部通过。
- `npm test`：4 个测试文件、77 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `fix: 修复开发版更新包校验值获取` 一并提交。

---
## 22. 兼容不支持的 HTML 标签和 Front Matter 渲染

### 问题
Markdown 文档中包含 `<aside>` 等当前引擎不支持的 HTML 标签时，标签会被转义为普通字符显示；文档开头由 `---` 包裹的 YAML Front Matter 会被误解析为水平分隔线、段落和列表，破坏正文布局。

### 根因
- 第三方引擎出于安全考虑关闭原始 HTML，未知标签因此由 markdown-it 转义输出；内置引擎同样会把未识别标签作为普通文本转义。
- 两套引擎都没有在 Markdown 解析前识别文档头属性块，开头和结尾的 `---` 被当作普通水平分隔线。
- 实时编辑会把 TipTap HTML 重新转换为 Markdown，如果属性块没有稳定标记，编辑后会丢失原始 Front Matter 边界。

### 修改
- 增加共享 Markdown 输入兼容层，只在代码块和行内代码之外移除不支持的 HTML 标签，并保留标签内部文字和 Markdown 格式。
- 空的不支持标签不再产生可见内容；`<br>` 转换为换行，已有 `<u>` 语法保持原行为。
- 仅识别文档起始位置成对的 `---` 为 Front Matter，正文中的普通分隔线保持不变。
- 将 Front Matter 渲染为带 YAML 高亮和专用样式的可编辑属性块，并对其中 HTML 字符进行转义。
- 扩展编辑器代码块属性，确保 TipTap 编辑后仍保留 Front Matter 标记；HTML 转回 Markdown 时恢复原始 `---` 包裹格式。
- 内置和第三方 Markdown 引擎共用同一规则。
- 增加四项回归测试，覆盖未知标签解包、空标签、代码示例保护、Front Matter 专用渲染和往返保真。

### 涉及文件
- `src/utils/markdown.normalize.ts`
- `src/utils/markdown.third.ts`
- `src/utils/markdown.ts`
- `src/components/Editor.tsx`
- `src/styles/markdown.css`
- `tests/markdown.third.roundtrip.test.ts`
- `tests/markdown.roundtrip.test.ts`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- 修复前新增测试可稳定复现未知 HTML 标签字符显示和 Front Matter 被拆成分隔线、列表的问题。
- `npm test -- tests/markdown.third.roundtrip.test.ts tests/markdown.roundtrip.test.ts`：2 个测试文件、44 个测试全部通过。
- `npm test`：4 个测试文件、81 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `fix: 兼容 HTML 标签和文档头属性渲染` 一并提交。

---
## 23. 统一编辑器弹层互斥并优化链接编辑响应

### 问题
编辑器内容区的右键菜单、选择器和编辑弹窗可能同时显示。例如图片右键菜单打开后，再单击图片会出现图片编辑弹窗，但原右键菜单仍残留；单击已有超链接后，编辑弹窗还需要明显等待才显示。

### 根因
- 图片单击编辑使用捕获阶段事件并停止传播，编辑器容器原有的菜单关闭监听无法收到该次点击。
- 链接单击为了区分双击，固定使用 `500ms` 定时器延后打开编辑弹窗，造成可感知的响应延迟。
- 点击已有链接时还会先执行一次不必要的 TipTap 聚焦和选区事务；实际修改位置已经由独立的链接范围引用保存。
- 图片、表格、链接和工具栏选择器分别维护自身状态，缺少统一的“打开新交互层前关闭旧交互层”入口。

### 修改
- 增加编辑器交互层统一关闭入口，集中关闭链接弹窗、图片编辑弹窗、图片尺寸弹窗、图片/表格右键菜单、斜杠菜单、表格/有序列表/标题选择器、语法提示和代码块语言选择器。
- 图片、表格、链接及工具栏选择器在打开前先关闭已有交互层，确保编辑器范围内同一时间只显示一个菜单或弹窗。
- 图片单击编辑前显式关闭图片右键菜单，避免捕获阶段停止传播后菜单残留。
- 移除链接单击固定 `500ms` 延时，编辑模式下普通单击立即打开链接编辑弹窗。
- 移除点击已有链接时额外的聚焦和选区事务，继续通过保存的精确链接范围完成修改。
- 保留 `Ctrl/Cmd + 单击`及阅读模式单击打开外部链接的既有行为。
- 增加真实 `Editor` 组件交互回归测试，覆盖图片菜单与编辑弹窗互斥、链接单击即时响应。

### 涉及文件
- `src/components/Editor.tsx`
- `tests/editor.interactions.test.tsx`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- 修复前两个新增测试可稳定复现图片菜单重叠和链接弹窗延迟问题。
- `npm test -- tests/editor.interactions.test.tsx`：1 个测试文件、2 个测试全部通过。
- `npm test`：5 个测试文件、83 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `fix: 统一编辑器弹层并优化链接响应` 一并提交。

---
## 24. 工具栏支持悬浮/停靠与四向位置切换

### 需求
- 保留现有工具栏悬浮开关，并支持切换为占用编辑区布局的停靠模式。
- 悬浮和非悬浮两种模式都可以选择上、左、下、右四个位置。
- 上下方向保持横向工具栏，左右方向调整为纵向工具栏，布局需要与现有软件视觉风格一致。
- 新设置必须持久化，并兼容没有位置字段的旧版设置文件。

### 设计
- 将工具栏显示方式拆分为“是否悬浮”和“停靠方向”两个独立设置，不改变原有悬浮开关语义。
- 默认方向保持为顶部，旧设置缺少新字段时由前端默认配置与 Rust Serde 默认值共同回退到顶部。
- 仅实时编辑模式应用工具栏布局类，阅读、源码和分栏模式继续隐藏工具栏，避免改变既有视图结构。
- 非悬浮模式通过编辑器 Flex 布局占据对应边缘；悬浮模式使用绝对定位覆盖在编辑区上方，不压缩正文空间。

### 修改
- 在前端 `AppSettings` 与 Rust `AppSettings` 中增加 `toolbarPosition` / `toolbar_position` 字段，支持 `top`、`left`、`bottom`、`right`。
- 前端与 Rust 默认值统一为 `top`，增加旧设置反序列化回归测试，确认缺少新字段时仍可正常加载。
- 设置页外观区域增加“工具栏”分组：
  - 保留“工具栏悬浮”开关。
  - 增加“工具栏位置”四向切换按钮。
  - 设置搜索索引补充工具栏位置及中英文方向关键词。
- 编辑器根据悬浮状态和位置生成独立布局类：
  - 顶部、底部使用横向排列。
  - 左侧、右侧使用纵向排列，分隔线同步改为横向。
  - 底部、右侧停靠时调整 Flex 顺序，正文保持完整可用区域。
  - 悬浮顶部/底部居中，悬浮左侧/右侧垂直居中。
- 标题选择器根据工具栏方向调整展开位置：底部向上、左侧向右、右侧向左，避免弹层越出编辑区。
- 补充中英文工具栏分组、悬浮说明、位置说明和四个方向文案。
- 增加编辑器布局与多语言回归测试，覆盖四个非悬浮方向和右侧悬浮方向。

### 涉及文件
- `src/types/index.ts`
- `src/App.tsx`
- `src-tauri/src/settings.rs`
- `src/components/SettingsPanel.tsx`
- `src/components/Editor.tsx`
- `src/styles/editor.css`
- `src/i18n/locales/zh-CN.ts`
- `src/i18n/locales/en.ts`
- `tests/core.test.ts`
- `tests/editor.interactions.test.tsx`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- 定向 Vitest：2 个测试文件、39 个测试全部通过。
- 真实浏览器布局测量通过：顶部/底部非悬浮工具栏横向占据对应边缘，左侧/右侧非悬浮工具栏纵向占据对应边缘，右侧悬浮工具栏不压缩正文宽度。
- `npm test`：5 个测试文件、89 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `cargo check`：通过。
- `cargo test`：6 个测试全部通过。
- `rustfmt --edition 2021 --check src/settings.rs`：本次 Rust 修改格式检查通过。
- 全仓 `cargo fmt -- --check` 仍会报告 `src-tauri/src/updater.rs` 的既有格式差异；该文件与本次需求无关，未进行顺带格式化。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `feat: 支持工具栏四向布局切换` 一并提交。

---
## 25. 项目发布版本统一升级至 0.1.5

### 需求
- 将 FkeMark 当前项目版本统一调整为 `0.1.5`。
- 发布配置、应用内版本兜底值和本地构建产物名称需要保持一致，避免安装包、关于页面和更新检查显示不同版本。

### 版本边界
- 应用主项目的前端包、Tauri 配置和 Rust 包版本统一为 `0.1.5`。
- 更新器测试中的 `0.1.1 → 0.1.2` 仅用于模拟升级场景，不代表当前应用版本，保持不变。
- `doc/package.json` 是独立的私有文档站包，不随桌面应用版本联动。

### 修改
- 将根目录 `package.json` 与 `package-lock.json` 的应用版本从 `0.1.2` 更新为 `0.1.5`。
- 将 `src-tauri/Cargo.toml`、活动 Tauri v2 配置和旧版根目录 Tauri 配置的版本统一为 `0.1.5`。
- Rust 检查过程中同步本地 Cargo 锁文件中的 FkeMark 包版本。
- 将应用初始化、关于页面和设置页面的版本兜底值调整为 `0.1.5`。
- 将 Windows 便携版压缩包名称更新为 `FkeMark-0.1.5-windows-portable.zip`。
- 更新 Dev 工作流中关于固定打包版本的注释，避免继续显示过期的 `0.1.0`。
- 未修改依赖版本、更新器测试夹具和独立文档站包版本。

### 涉及文件
- `package.json`
- `package-lock.json`
- `src-tauri/Cargo.toml`
- `src-tauri/tauri.conf.json`
- `tauri.conf.json`
- `scripts/build.cjs`
- `src/App.tsx`
- `src/components/AboutPage.tsx`
- `src/components/SettingsPanel.tsx`
- `.github/workflows/dev.yml`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- 版本一致性检查：前端包、npm 锁文件、Rust 包、Cargo 锁文件及两份 Tauri 配置均为 `0.1.5`。
- `npm test`：5 个测试文件、89 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过，构建命令显示当前包版本为 `0.1.5`。
- `cargo check`：通过，Rust 构建目标识别为 `fke-mark v0.1.5`。
- `cargo test`：6 个测试全部通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `chore: 升级项目版本至 0.1.5` 一并提交。

---
## 26. 项目发布版本统一升级至 0.2.0

### 需求
- 将 FkeMark 桌面应用当前发布版本从 `0.1.5` 升级为 `0.2.0`。
- 前端显示、安装包配置、Rust 包和本地构建产物必须使用同一版本，避免发布链路出现版本漂移。

### 版本边界
- 应用主项目的 npm、Tauri 与 Rust 版本统一调整为 `0.2.0`。
- 更新器测试中的历史版本仅用于模拟更新场景，保持不变。
- `doc/package.json` 是独立私有文档站包，不与桌面应用版本绑定。
- 不修改第三方依赖版本，不生成 Git Tag 或正式发布包。

### 修改
- 将 `package.json`、`package-lock.json` 的主项目版本更新为 `0.2.0`。
- 将 `src-tauri/Cargo.toml`、Tauri v2 配置和根目录旧版 Tauri 配置统一更新为 `0.2.0`。
- Rust 检查过程中同步本地 Cargo 锁文件中的 FkeMark 包版本。
- 将应用初始化、关于页面和设置页面中的版本兜底值更新为 `0.2.0`。
- 将 Windows 便携包名称更新为 `FkeMark-0.2.0-windows-portable.zip`。
- 将 Dev 工作流中的固定安装包版本说明同步为 `0.2.0`。

### 涉及文件
- `package.json`
- `package-lock.json`
- `src-tauri/Cargo.toml`
- `src-tauri/tauri.conf.json`
- `tauri.conf.json`
- `scripts/build.cjs`
- `src/App.tsx`
- `src/components/AboutPage.tsx`
- `src/components/SettingsPanel.tsx`
- `.github/workflows/dev.yml`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- 版本一致性检查：npm 清单、npm 锁文件、Rust 包、Cargo 锁文件和两份 Tauri 配置均为 `0.2.0`。
- `npm test`：5 个测试文件、89 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过，构建命令识别当前包版本为 `0.2.0`。
- `cargo check`：通过，Rust 构建目标识别为 `fke-mark v0.2.0`。
- `cargo test`：6 个测试全部通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `chore: 升级项目版本至 0.2.0` 一并提交。

---
## 27. P1：支持 Markdown 脚注与参考文献

### 需求
- 支持正文中的 `[^label]` 脚注引用和文末的 `[^label]: 内容` 定义语法。
- 支持同一脚注重复引用、中文标签及缩进续行的多行脚注内容。
- 点击脚注编号可以跳转到定义，点击定义后的返回标记可以回到对应引用。
- 内置 Markdown 引擎与第三方引擎行为保持一致，编辑、预览和保存往返后不得丢失原始脚注语义。

### 根因
- 当前两套 Markdown 渲染链路均未启用脚注扩展，解析器会把 `[^label]` 和定义行当作普通文本处理。
- 即使直接生成脚注 HTML，TipTap 默认也不会保留引用、定义列表、定义项及返回链接所需的专用属性，保存时会退化为普通链接和有序列表。
- 编辑器原有链接点击逻辑只区分内部编辑和外部链接，没有脚注锚点的局部跳转分支。

### 修改
- 新增共享脚注处理模块，在进入 Markdown 引擎前提取定义并保护引用标记，再统一生成带编号、定义列表和返回链接的语义化 HTML。
- 为内置与第三方 Markdown 引擎接入同一脚注渲染和 HTML 回转逻辑，保存时重新还原 `[^label]` 引用及缩进续行定义。
- 支持同一标签多次引用，为每次引用生成独立锚点，并在定义末尾生成对应的多个返回入口。
- 保护行内代码和围栏代码块中的脚注字面量，未定义的引用继续按原文本显示，避免误转换。
- 新增 TipTap 脚注元数据扩展，保留链接、脚注列表和定义项的 `data-*` 属性及锚点 ID。
- 编辑器实时视图、阅读视图和分栏预览统一拦截脚注内部链接，在当前内容区域内平滑跳转，不再按外部 URL 打开。
- 增加脚注上标编号、文末分隔线、定义列表和返回标记样式，使其与现有编辑器主题保持一致。

### 涉及文件
- `src/utils/markdown.footnotes.ts`
- `src/utils/markdown.ts`
- `src/utils/markdown.third.ts`
- `src/components/extensions/FootnoteMetadata.ts`
- `src/components/Editor.tsx`
- `src/styles/markdown.css`
- `tests/footnote.editor.test.ts`
- `tests/markdown.roundtrip.test.ts`
- `tests/markdown.third.roundtrip.test.ts`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- 新增内置与第三方 Markdown 引擎脚注测试，覆盖重复引用、中文标签、多行定义、定义内 Markdown 格式及代码字面量保护。
- 新增 TipTap 解析和序列化回归测试，确认脚注专用元数据在编辑器往返后仍可恢复为 Markdown 语法。
- `npm test`：6 个测试文件、95 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `feat: 支持 Markdown 脚注与参考文献` 一并提交。

## 28. P1：支持 YAML Front Matter 与正文标签

### 需求与边界
- 将文档开头的 YAML Front Matter 解析为结构化元数据，为后续搜索、筛选与分类提供统一数据入口。
- 支持正文中的 `#tag` 标签语法，并与 Front Matter 的 `tags` / `tag` 字段合并、去重。
- 内置与第三方 Markdown 引擎必须保持一致，编辑器加载、编辑和保存后不得丢失 Front Matter 或正文标签。
- 本次仅完成元数据解析、标签渲染与往返保真，不提前增加搜索索引、标签管理页或点击跳转等未要求功能。

### 根因
- 现有 Front Matter 兼容逻辑只识别文档开头的 `---` 区块，并将原文包装为 YAML 代码块用于显示和保存，没有真正解析成可复用的文档元数据。
- 正文 `#tag` 会被 Markdown 引擎当作普通文本，缺少独立语义标记；TipTap 也没有对应扩展来保留标签属性，因此无法稳定支撑后续检索与分类。
- 如果直接使用简单正则全局替换，会把标题、代码、链接地址和转义文本中的 `#` 误判为标签，两套引擎也容易产生不一致结果。

### 修改
- 新增共享文档元数据模块，使用 `yaml` 解析器读取完整 YAML 标量、数组、列表、布尔值和数字等常用结构；解析失败时保留原始 Front Matter，并通过 `parseError` 返回错误而不中断渲染。
- 提供 `extractDocumentMetadata` 公共接口，统一返回 `rawFrontMatter`、结构化 `frontMatter`、`frontMatterTags`、`inlineTags`、合并后的 `tags` 和 `parseError`。
- 同时兼容 Front Matter 顶层 `tags` 与 `tag` 字段，支持数组、标量和逗号分隔字符串；自动移除标签前导 `#`，并按首次出现顺序进行大小写不敏感去重。
- 在进入 Markdown 引擎前使用临时令牌保护有效正文标签，渲染后统一恢复为带 `data-doc-tag` 的语义化标签节点，保证内置与第三方引擎行为一致。
- 正文标签支持中英文、数字、下划线和连字符；排除 ATX 标题、行内代码、围栏代码块、缩进代码块、Markdown 链接目标及转义内容，避免误识别。
- 新增 TipTap `DocumentTag` 标记扩展，保留标签文本和 `data-doc-tag` 元数据，使编辑器 HTML 与 Markdown 保存往返保持一致。
- 增加跟随现有主题变量的胶囊标签样式，不引入新的主题配置或交互行为。
- 新增 `yaml` 运行时依赖，避免使用不完整的自定义 YAML 解析器。

### 涉及文件
- `package.json`
- `package-lock.json`
- `src/utils/markdown.metadata.ts`
- `src/utils/markdown.engine.ts`
- `src/utils/markdown.ts`
- `src/utils/markdown.third.ts`
- `src/components/extensions/DocumentTag.ts`
- `src/components/Editor.tsx`
- `src/styles/markdown.css`
- `tests/markdown.metadata.test.ts`
- `.workbuddy/memory/2026-07-22.md`

### 验证
- 先新增元数据回归测试并确认因缺少 `DocumentTag` 实现而失败，再完成实现使测试转绿。
- 定向测试：`npm test -- tests/markdown.metadata.test.ts`，1 个测试文件、7 个测试全部通过。
- 全量测试：`npm test`，7 个测试文件、102 个测试全部通过。
- `npm run build`：TypeScript 检查与 Vite 生产构建通过。
- `git diff --check`：通过，无空白符错误。

### 提交
- 随本次 `feat: 支持 YAML Front Matter 与正文标签` 一并提交。
---
## 今日提交汇总

| 提交 | 类型 | 内容 |
|---|---|---|
| `781889e` | fix | 修复 Markdown 渲染和导出注入风险 |
| `f9fd92b` | fix | 修复保存流程和更新安装安全边界 |
| `2c4f6da` | fix | 提示语接入多语言配置 |
| `28ce43a` | fix | 修复更新成功误报未生效 |
| `aec8bb9` | feat | 增加后台定时更新检查通知 |
| `5218895` | fix | 注册 Markdown 默认应用候选 |
| `10288ec` | fix | 防止链接和图片编辑弹窗误关闭 |
| `e799709` | style | 统一 Rust 源码格式 |
| `3ad7938` | fix | 修复拖拽图片初始化显示失败 |
| `75a4134` | fix | 修复系统打开方式文件定位 |
| `5eea293` | fix | 兼容更新清单缺失 SHA-256 校验值 |
| `84a8fd9` | fix | 修复编辑弹窗边界与链接交互 |
| `c92586a` | feat | 增加编辑器主题配色 |
| `e699ce0` | style | 优化侧栏文件树展示 |
| `18025b0` | fix | 补齐设置行为窗口关闭文案 |
| `86e69c8` | fix | 兼容第三方引擎松散表格渲染 |
| `62b63ce` | fix | 修复引用渲染高度和空引用分裂 |
| `8b004b6` | fix | 修复开发版变更日志获取范围 |
| `af2db69` | feat | 标签右键增加打开文件所在位置 |
| `78742f1` | fix | 修复开始写作浮层和模板渲染 |
| `069afa0` | fix | 修复开发版更新包校验值获取 |
| `e970c27` | fix | 兼容 HTML 标签和文档头属性渲染 |
| `cd839c4` | docs | 新增 FkeMark 文档站与墨纸主题 |
| `7e26e5d` | fix | 统一编辑器弹层并优化链接响应 |
| `7e0edd4` | feat | 支持工具栏悬浮/停靠与四向位置切换 |
| `ac35fa8` | chore | 升级项目版本至 0.1.5 |
| `8390471` | docs | 完善 VitePress 主题与 Cloudflare 部署说明 |
| `497c067` | chore | 升级项目版本至 0.2.0 |
| `1424541` | feat | 支持 Markdown 脚注与参考文献 |
| 本次提交 | feat | 支持 YAML Front Matter 与正文标签 |

## 今日最终验证结果

- 前端 TypeScript 与 Vite 生产构建通过。
- Vitest：7 个测试文件、102 个测试全部通过。
- Rust `cargo check` 通过。
- Rust `cargo test`：6 个测试通过。
- 各功能修改均使用规范 Git 提交，提交正文使用中文详细列出子项；主题配色、侧栏文件树、设置行为文案、第三方 Markdown 松散表格和引用渲染修复均已提交；Dev 流水线 changelog 范围、标签右键文件定位、开始写作浮层、模板渲染、Dev 更新包 SHA-256 获取、不支持 HTML 标签与 Front Matter 渲染兼容均已提交；编辑器弹层互斥与链接编辑响应优化已提交；工具栏悬浮/停靠、四向位置切换和设置兼容已提交；应用发布配置、界面版本兜底和本地构建产物此前已统一至 0.1.5；VitePress 主题与 Cloudflare 部署说明已完善；桌面应用发布链路已统一升级至 0.2.0；脚注与参考文献语法、双向跳转及编辑保存往返保真支持已提交；本次新增 YAML Front Matter 结构化元数据、正文标签渲染与编辑保存往返保真支持。
