# 2026-07-19 工作日志

## 新窗口 splash 启动画面闪烁修复

### 问题描述
新建窗口时会短暂出现"初始化界面"（splash 启动画面：FM 图标 + "FkeMark" 标题 + loading spinner），闪一下后才进入实际编辑器界面。期望：新建窗口应直接显示实际界面，不出现闪烁。

### 根因
`index.html` 的 `#root` 内预置了 splash 启动画面（静态 HTML）：
```html
<div id="root">
  <div class="splash-screen">
    <div class="splash-icon">FM</div>
    <h1 class="splash-title">FkeMark</h1>
    <div class="splash-loading"></div>
  </div>
</div>
```

新窗口加载流程：
1. WebView2 加载 index.html → **splash 立即显示**（静态 HTML，无需 JS）
2. JS 加载、React 挂载（`ReactDOM.createRoot(#root).render(<App />)`）→ **覆盖 #root 内容**，splash 被替换为实际界面

从窗口创建到 React 挂载完成的时间窗口内（几十到几百毫秒），splash 短暂可见——这就是"闪一下"。

主窗口启动时 splash 是预期的加载画面（用户预期看到应用启动加载），但新窗口是用户主动触发的，预期立即看到编辑器，先看到 splash 就显得突兀。

### 修复方案
**新窗口延迟显示**：窗口创建时 `visible(false)`，前端 React 渲染完成后再调用 `window.show()`。这样 splash 在窗口不可见时就已经被替换，用户看到的就是最终界面。

### 修改清单

| 文件 | 改动 |
|---|---|
| `src-tauri/src/lib.rs` | `new_window`：`.center()` 后添加 `.visible(false)` |
| `src-tauri/src/lib.rs` | `new_window_with_config`：`.center()` 后添加 `.visible(false)` |
| `src/App.tsx` | 导入 `getCurrentWebviewWindow`（from `@tauri-apps/api/webviewWindow`） |
| `src/App.tsx` | `showOnboarding` 初始状态：secondary 窗口强制 `false`（避免首启引导闪现） |
| `src/App.tsx` | `loadSettings` 的 useEffect：`.finally()` 后，secondary 窗口用**双 RAF** 调用 `getCurrentWebviewWindow().show()` 显示窗口 |

### 关键代码
```typescript
// App.tsx — loadSettings 完成后显示新窗口
useEffect(() => {
  loadSettings().finally(() => {
    if (!isSecondaryWindow || !isTauri()) return
    // 双 RAF：确保 React 完成首屏渲染并绘制后再显示窗口
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        getCurrentWebviewWindow()
          .show()
          .catch((e) => console.error('Failed to show secondary window:', e))
      })
    })
  })
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [])
```

### 踩坑点
- `getCurrentWebviewWindow()` 在 Tauri v2 中**同步返回 `WebviewWindow` 实例**（非 Promise），不能 `.then()`，直接 `.show().catch(...)` 调用。`show()` 方法返回 Promise。
- 双 RAF（嵌套 `requestAnimationFrame`）确保至少经过一次浏览器绘制，React 重渲染完成。单 RAF 可能不够。
- 即使 `loadSettings` 失败（invoke 报错），`.finally()` 仍会执行 show，保证窗口不会永远不可见。

### 校验
- `npx tsc --noEmit`：零错误通过
- `cargo check`：零错误零警告通过（2.28s）

### 测试建议
`npm run tauri dev` 后点击"新建窗口"：
1. 新窗口应**直接显示编辑器界面**，不再先闪现 splash 启动画面
2. 新窗口不应显示首启引导（Onboarding）
3. 新窗口显示略有延迟（React 渲染完成才显示），但无闪烁，体验更佳

## macOS 打包修复：`transparent` 方法不存在

### 问题
macOS CI 打包（`tauri build --target x86_64-apple-darwin --bundles dmg`）报错：
```
error[E0599]: no method named `transparent` found for struct `WebviewWindowBuilder<'a, R, M>`
  --> src/lib.rs:163:6  (new_window)
  --> src/lib.rs:206:6  (new_window_with_config)
```
Windows 上 `cargo check` 通过，但 macOS 编译失败。

### 根因
`WebviewWindowBuilder::transparent()` 方法的平台标注为：
```rust
#[cfg(any(not(target_os = "macos"), feature = "macos-private-api"))]
pub fn transparent(mut self, transparent: bool) -> Self
```
即 **Windows/Linux 始终可用，但 macOS 上只有启用 `tauri` 的 `macos-private-api` feature 时该方法才被编译进来**。macOS 透明窗口需调用私有 API，Tauri 默认不暴露该方法。之前为修复 Windows 白屏给新窗口加了 `.transparent(true)`（Windows 可用），但 macOS 编译时该 method 不存在 → 报错。

### 修复（两步，保持 macOS 新窗口与主窗口 `transparent: true` 配置一致）
1. `src-tauri/Cargo.toml`：`tauri` 依赖追加 `macos-private-api` feature：
   ```toml
   tauri = { version = "2", features = ["tray-icon", "devtools", "macos-private-api"] }
   ```
   （非 macOS 平台该 feature 为空操作，不影响 Windows/Linux 构建）
2. `src-tauri/tauri.conf.json`：`app` 下加 `"macOSPrivateApi": true`：
   ```json
   "app": { "withGlobalTauri": true, "macOSPrivateApi": true, "windows": [...] }
   ```
   否则 `tauri-build` 的 feature allowlist 校验会报 "Cargo.toml features does not match allowlist defined under tauri.conf.json"。

### 校验
- Windows `cargo check`：零错误通过（allowlist 校验也过）
- macOS 编译需 CI 验证（本地无法跑 macOS target）；逻辑已确认：`macos-private-api` 启用后 `transparent` 方法在 macOS 存在

### 注意
- 启用 `macos-private-api` 会让 macOS 构建依赖私有 API，**无法上架 Mac App Store**（GitHub 分发 dmg 不受影响）——若未来要上架需改用条件编译排除 macOS 的 `transparent` 调用（但会失去 macOS 新窗口透明）。

---

## 三项进程/交互修复（dev 流水线标题、更新通道、关闭按钮）

### 1. dev 流水线 release 标题统一格式
- `src-tauri/...` → 改 `.github/workflows/dev.yml` 的 `Compute release metadata` 步骤：
  `echo "release_name=🧪 Dev Build (${DEV_VERSION})"`（`DEV_VERSION`=dev-<SHA7>，故标题形如 `🧪 Dev Build (dev-d0a6a97)`）
- `release_name` 输出被「Create release」与「Upload assets to release」两处 `name:` 共用，统一生效。
- tag 仍为固定 `dev-latest`（按用户要求不变）。

### 2. 关于页更新通道不再持久化
- 根因：之前 `SettingsPanel` 的"更新通道"单选调用 `update({ updateChannel })` 写入设置配置（持久化）。
- 修复：
  - `SettingsPanel` 新增本地状态 `detectedChannel = getBuildChannel()`，并在 `activeSection === 'about'` 时重新检测（每次打开关于页自动选中本机构建类型 dev/stable/release）。
  - 单选按钮 `onClick` 改为 `setDetectedChannel(...)`，**不再持久化**。
  - "检查更新"按钮调用 `onCheckUpdate?.(detectedChannel)`，App.tsx 改为 `(ch) => doCheckUpdate(ch, true)`。
  - 启动自动检查 `doCheckUpdate(settings.updateChannel)` → `doCheckUpdate(BUILD_CHANNEL, false)`，彻底脱离持久化值。
- 验证：`grep settings.updateChannel` 已无业务读取点；`npx tsc --noEmit` 零错误。

### 3. 关闭按钮首次"隐藏至托盘"无效
- 根因：`ConfirmDialog` 的 `closeAction` 模式只有「取消/确定(关闭)」+「不再提示」复选框，复选框文案"默认隐藏至托盘"。第一次勾选并点确定 → `result.action==='close'` → **直接 closeWindow() 退出**，而非隐藏到托盘；第二次才因 `skipClosePrompt` 生效走 `minimize` 分支。
- 修复：
  - `ConfirmDialog` 的 `closeAction` 改为三按钮：**取消 / 隐藏至托盘 / 直接关闭**，并新增 `DialogResult.closeAction: 'minimize' | 'close'`、`showCloseActionDialog` 返回 `{ action: 'minimize'|'close'|'cancel', dontAskAgain }`。
  - `handleCloseWindow` 的 `ask` 分支：用户选择的具体行为**立即执行**；若勾选"不再提示"则 `handleSettingsChange({ ...settings, closeAction: result.action, skipClosePrompt: true })` 持久化为默认行为。
  - i18n：`window.closePrompt.message` 改中性问句、`dontAskAgain` 改"以后不再提示（按本次选择）"，新增 `window.closePrompt.close`。
- 验证：`npx tsc --noEmit` 零错误。

### 校验（总）
- `npx tsc --noEmit`：零错误通过。
- 无 Rust 改动，无需 cargo check。
- 建议本地 `npm run tauri dev` 实测：① 点关闭按钮出现三按钮、首次选"隐藏至托盘"立即最小化；② 关于页更新通道随构建类型自动选中、切换不写配置。

---

## 应用内更新功能（下载/校验/安装/回滚完整闭环）

### 背景与选型
- 此前更新仅"检查+跳网页手动下载"（`updater.ts` 的 `checkForUpdate` + `openExternalUrl`），Rust 无任何下载/安装命令。
- 需求：完整性校验、版本信息、下载进度、**断点续传**、自动安装、失败**回滚**、状态一致性避免数据丢失。
- 选型：**自写 Rust 命令（reqwest）** 而非官方 `tauri-plugin-updater`——官方插件不支持断点续传与版本回滚。

### 后端（新增 `src-tauri/src/updater.rs`，已在 lib.rs 注册 7 个命令）
- 依赖新增：`reqwest`(default-features=false, features=stream+rustls-tls，避免 Windows OpenSSL)、`sha2`、`futures-util`。
- 命令：`download_update`(断点续传+进度事件+校验)、`get_download_state`(查续传)、`cancel_download`、`verify_update_package`、`install_update`、`finalize_update`(启动自愈)、`rollback_update`。
- **断点续传**：`.partial` 文件 + `Range: bytes=<existing>-` 请求；服务器返回 206 才 append，否则从头覆盖。下载状态持久化到 `<app_cache>/updates/download-state.json`，跨重启可续传。
- **完整性校验**：下载完成校验大小 + SHA-256（清单提供时）；失败即删除，绝不留下损坏包。
- **原子性（核心回滚保证）**：校验通过后才 `rename .partial → <version>__<name>` 正式包；任一步失败当前安装完好无损。
- **进度事件**：`window.emit("update://download-progress", {version,downloaded,total,percent,speed})`，每 200ms 上报一次。
- **安装**：写 `pending-update.json`(prev/new 版本+安装包路径) → `spawn_installer`(Win: `.msi`→`msiexec /i /passive /norestart`；nsis `-setup.exe`→`/S`；用 `CREATE_NO_WINDOW|DETACHED_PROCESS`) → sleep 600ms → `app.exit(0)` 释放文件占用。
- **启动自愈** `finalize_update`：比对当前版本 vs pending.new_version → success(记录 rollback.json + 清理旧包) / failed(旧版本完好=等效回滚) / none。
- **手动回滚** `rollback_update`：用保留的上一版本安装包静默重装（依赖上次经应用内更新留下的旧包）。
- 跨边界 struct 加 `#[serde(rename_all="camelCase")]`(DownloadState/FinalizeResult)。
- 踩坑：`install_update` 参数名 `installer_path` 与同名 helper fn 冲突→函数体内为参数遮蔽，OK（未在体内调 helper）。

### 前端
- `src/utils/updater.ts`：新增 `DownloadAsset`(含 sha256)/`DownloadProgress`/`DownloadState`/`FinalizeResult` 类型 + `downloadUpdate/cancelDownload/getDownloadState/verifyUpdatePackage/installUpdate/finalizeUpdate/rollbackUpdate/listenDownloadProgress/getPlatformDownload` 函数。
- 新增 `src/hooks/useUpdater.ts`：状态机 `idle→downloading⇄paused→ready→installing`(+error)；封装进度监听、暂停(cancel 保留 partial)、续传、安装(带 onBeforeInstall)、回滚。导出 `Updater` 类型。
- `App.tsx`：`saveAllForUpdate`(安装前保存所有"已修改+有磁盘路径"标签，保证数据一致性) 作为 onBeforeInstall；启动调 `finalizeUpdate` 显示自愈 toast + 设置 rollbackAvailable；Toast"下载更新"改为打开关于页并直接 start 下载。
- `SettingsPanel.tsx`：关于页更新区新增进度条、下载/暂停/继续/重试/立即安装/回滚按钮（安装与回滚用 `showConfirm` 二次确认），保留"打开下载页面"兜底。
- i18n：zh-CN/en 各新增 16 个 `update.*` 文案（downloadInstall/pause/resume/installNow/installConfirm/rollback 等）。
- CSS：`components.css` 新增 `.update-progress*` / `.update-action-row` / `.update-error-msg` / `.update-rollback-*` 及 primary/ghost/disabled 变体。

### CI
- `release.yml` + `dev.yml` 的清单生成脚本加 `sha256_of()`，每个 download entry 增加 `sha256` 字段（供应用内下载校验）。

### 校验
- `cargo check`：零错误零警告（35s，含 reqwest/sha2 编译）。
- `npx tsc --noEmit`：零错误。
- `npm run build`：成功（仅既有 dynamic import 混用告警，无碍）。

### 待实测（需 tauri dev/打包环境）
- 真实 GitHub Release 下载链路（跨主机重定向时 Range 是否保留→续传）。
- 静默安装退出→安装器接管→重启后 finalize 判定成功/失败。
- 回滚仅在"曾用应用内更新留下旧包"时可用（首次更新无回滚包，属预期限制）。
