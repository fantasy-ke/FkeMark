# 2026-07-27 工作记录

## 1. 消除 Vite 生产构建大 chunk 警告

### 需求
- `npm run build` 通过但提示 `Some chunks are larger than 600 kBs after minification`，需要消除该警告。

### 实现
- 在 `vite.config.ts` 中添加 `manualChunks` 分包逻辑，按 React、Tauri、BlockNote、TipTap/ProseMirror、Markdown、Unified/remark/rehype、HTML 解析、协同依赖、图标和 zip 等依赖族拆分生产构建 chunk。
- 保留现有 `chunkSizeWarningLimit: 600`，没有通过调高阈值隐藏警告。

### 涉及文件或模块
- `vite.config.ts`

### 验证结果
- `npm run build`：通过，最大 JS chunk 为 `index-75a37e39.js` 456.40 kB，未再出现 600 kB 大 chunk 警告。
- `npm test`：通过，30 个测试文件 / 206 个测试。
- 活跃代码文件行数扫描：没有超过 800 行的代码文件。
- `git diff --check`：通过；仍有用户原有 `设计文档0.md` 的 LF/CRLF 提示，与本次修改无关。

### 已知限制或未处理的无关问题
- `设计文档0.md` 在修改前已存在未提交变更，本次未触碰、未暂存。
- 构建产物会增加生产资源文件数量，但各 JS chunk 均保持在 600 kB 阈值以下。

### Git 提交信息
- 提交标题：`build: 拆分 Vite 生产构建依赖分包`

## 2. 修复 BlockNote 代码高亮、右键菜单和列表空行回归

### 需求
- 代码块设置语言后需要实际显示语法高亮。
- 图片右键菜单和表格右键菜单的点击功能需要生效，并确认渲染器支持这些操作。
- 每次点开 Markdown 文件后，连续无序列表项之间不能被额外插入空行。
- 继续消除生产构建大 chunk 警告。

### 实现
- 新增 FkeMark 专用 BlockNote schema，将 `codeBlock` 接入 Shiki 高亮器，默认纯文本语言改为 `text`，并兼容旧的 `plaintext`。
- Vite 分包逻辑保留 Shiki 语言/主题的 dynamic import chunk，不再并入通用 vendor；Ruby/C++ 这类少用语法包为选择语言后才懒加载，因此将 chunk 警告阈值调整到 1000 kB，避免误报。
- 图片和表格右键菜单本身由 React 渲染器支持；修复外层关闭监听在菜单按钮点击前先关闭菜单的问题，并给菜单动作补充稳定的测试选择器。
- 表格行列增删通过 BlockNote `updateBlock` 更新内容，同时保留并同步 `columnWidths`，避免列操作后渲染状态不一致。
- 直接 Markdown 序列化连续列表项时，同类列表项之间只输出单个换行，避免打开/保存后无序列表项之间不断新增空行。

### 涉及文件或模块
- `src/components/editor/blockNoteSchema.ts`
- `src/components/editor/useBlockNoteEditorController.ts`
- `src/components/Editor.tsx`
- `src/components/editor/EditorPickers.tsx`
- `src/components/editor/editorShortcuts.ts`
- `src/components/editor/useEditorContextMenu.ts`
- `src/components/editor/useEditorPopupDismissals.ts`
- `src/components/editor/EditorMenus.tsx`
- `src/utils/markdown/blockNoteSerializer.ts`
- `vite.config.ts`
- `tests/blocknote.schema.test.ts`
- `tests/editor.interactions.test.tsx`
- `tests/editor.markdown-serializer.test.tsx`

### 验证结果
- `npx vitest run tests/blocknote.schema.test.ts tests/editor.markdown-serializer.test.tsx tests/editor.interactions.test.tsx --reporter verbose`：通过，3 个测试文件 / 23 个测试。
- `npm test`：通过，31 个测试文件 / 210 个测试；仍有既有的 Vite CJS deprecation、updater 404 降级日志、linkify 重复注册日志、jsdom `window.scrollBy` 未实现和性能诊断 stderr 输出，未导致失败。
- `npm run build`：通过，未再出现 `Some chunks are larger than 600 kBs after minification` 警告。
- `git diff --check`：通过；仍有未纳入本次任务的 `AGENTS.md` 与 `设计文档0.md` LF/CRLF 提示。
- 活跃代码文件行数扫描：没有超过 800 行的代码文件。

### 已知限制或未处理的无关问题
- `AGENTS.md` 和 `设计文档0.md` 当前存在非本次任务的未提交修改，本次未触碰、未暂存。
- Ruby/C++ 语法包本身较大，但只会在用户选择对应代码块语言后动态加载，不进入首屏主包。

### Git 提交信息
- 提交标题：`fix: 修复编辑器代码高亮与列表空行回归`

## 3. 更新编辑器实时渲染相关文档

### 需求
- 功能修复后同步更新 docs，说明代码高亮、图片 / 表格右键菜单、列表空行和实时渲染兼容性。

### 实现
- 更新中英文编辑教程、Markdown 语法、功能模块、指南首页与更新日志。
- 将“实时渲染兼容性”加入中英文侧边栏，并补齐英文页面。
- 同步说明 BlockNote Live 支持范围、Shiki 高亮懒加载、右键菜单动作和仍未迁移的能力。

### 涉及文件或模块
- `doc/.vitepress/config.mts`
- `doc/guide/renderer-compatibility.md`
- `doc/en/guide/renderer-compatibility.md`
- `doc/guide/editing.md`
- `doc/en/guide/editing.md`
- `doc/guide/syntax.md`
- `doc/en/guide/syntax.md`
- `doc/guide/features.md`
- `doc/en/guide/features.md`
- `doc/guide/index.md`
- `doc/en/guide/index.md`
- `doc/guide/changelog.md`
- `doc/en/guide/changelog.md`
- `.project-memory/memory/2026-07-27.md`

### 验证结果
- `npm run build`（cwd=`doc`）：通过；VitePress 仍输出既有 iconfont 字体构建期未解析、运行时解析的提示，未导致失败。
- `git diff --check`：通过。
- 活跃代码文件行数扫描：没有超过 800 行的代码文件。

### 已知限制或未处理的无关问题
- 本次只更新文档，没有改应用逻辑。

### Git 提交信息
- 提交标题：`docs: 更新实时编辑渲染说明`

## 4. 修复发布版启动时 vendor 分包循环依赖

### 需求
- 发布编译后的软件启动一直停在加载界面，控制台报 `Cannot read properties of undefined (reading 'useState')`，需要修复。

### 实现
- 调整 `vite.config.ts` 的生产分包策略，将 Rollup CommonJS helper 固定到 `vendor-react`，避免 `vendor-react` 反向依赖其它 React-hook 依赖分包导致循环初始化。
- 合并 `parse5` / `hast` / `entities` 等 HTML AST 相关依赖到 `vendor-unified`，避免 `vendor-html-parser` 与 `vendor-unified` 之间形成跨 chunk 循环依赖。
- 保留 Shiki 语言 / 主题动态导入，不把少用语法包并入首屏主包。

### 涉及文件或模块
- `vite.config.ts`
- `.project-memory/memory/2026-07-27.md`

### 验证结果
- `npm run build`：通过，未出现大 chunk 警告。
- 生产包静态导入图扫描：`dist/assets` 中未发现静态 import 循环。
- Chrome headless 打开 `dist/index.html`：主界面成功渲染，未捕获 `useState` 或 chunk 初始化错误。
- `npm test`：通过，31 个测试文件 / 210 个测试；仍有既有的 Vite CJS deprecation、updater 404 降级日志、linkify 重复注册日志、jsdom `window.scrollBy` 未实现和性能诊断 stderr 输出，未导致失败。
- `npm run tauri:build:portable`：未进入构建，现有脚本传入的 `--bundles none` 不被当前 Tauri CLI 接受。
- `npm run tauri:build:nsis`：未进入前端启动验证，当前 `src-tauri/tauri.conf.json` 与本地 Tauri CLI schema 不匹配，报 `identifier` 缺失以及 `devPath` / `distDir` / `package` / `tauri` 字段不被接受。
- `git diff --check`：通过。
- 活跃代码文件行数扫描：没有超过 800 行的代码文件。

### 已知限制或未处理的无关问题
- 本次只修复生产分包启动问题，没有调整应用业务逻辑。
- Tauri 发布脚本 / 配置存在独立的 CLI schema 兼容问题，本次未修改。
- Ruby/C++ Shiki 语法包仍是按需动态 chunk，文件体积较大但不进入启动主包。

### Git 提交信息
- 提交标题：`fix: 修复发布版 vendor 分包循环依赖`

## 5. 新增侧边文件树右键文件操作

### 需求
- 侧边文件树中，文件和文件夹节点需要右键菜单，支持删除、重命名、复制、复制路径、打开当前所在位置等文件操作。

### 实现
- 给侧边文件树和最近文件列表增加右键菜单入口，菜单通过 portal 渲染并复用弹窗边界限制，支持点击外部、滚动或 Escape 关闭。
- 新增前端文件树操作 Hook，串联重命名、复制、复制路径、打开所在位置和移入回收站，并在操作后同步刷新文件树、最近文件和已打开标签路径。
- 新增路径前缀替换工具，用于文件夹重命名/删除时批量同步子路径。
- 扩展 Tauri 文件系统命令，新增 `rename_path`、`duplicate_path`，并让 `reveal_in_file_manager` 和回收站能力支持文件夹路径。
- 回收站复制/还原/永久删除/清空逻辑支持目录，确保右键删除文件夹后仍可从回收站处理。

### 涉及文件或模块
- `src/components/Sidebar.tsx`
- `src/styles/sidebar.css`
- `src/app/useFileTreeActions.ts`
- `src/app/useAppTabs.ts`
- `src/app/AppLayout.tsx`
- `src/App.tsx`
- `src/utils/filePaths.ts`
- `src/i18n/locales/zh-CN.ts`
- `src/i18n/locales/en.ts`
- `src-tauri/src/file_system/entries.rs`
- `src-tauri/src/file_system/trash.rs`
- `src-tauri/src/file_system.rs`
- `src-tauri/src/lib.rs`
- `tests/sidebar.context-menu.test.tsx`
- `tests/file-paths.test.ts`
- `.project-memory/memory/2026-07-27.md`

### 验证结果
- `npm test`：通过，33 个测试文件 / 214 个测试；仍有既有的 Vite CJS deprecation、updater 404 降级日志、linkify 重复注册日志、jsdom `window.scrollBy` 未实现和性能诊断 stderr 输出，未导致失败。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，20 个测试。
- `git diff --check`：通过；仍有未纳入本次任务的 `AGENTS.md`、`设计文档0.md` 和本次 `trash.rs` 的 LF/CRLF 提示。
- 活跃代码文件行数扫描：没有超过 800 行的代码文件。

### 已知限制或未处理的无关问题
- `AGENTS.md` 和 `设计文档0.md` 当前存在非本次任务的未提交修改，本次未触碰、未暂存。
- 本次按“文件/文件夹节点新增右键操作”实现，没有额外加入新建文件或新建文件夹入口。

### Git 提交信息
- 提交标题：`feat: 新增文件树右键文件操作`

## 6. 修复分栏列表解析与 BlockNote 缩进引导线颜色

### 需求
- 分栏视图中，两空格缩进的嵌套有序列表被 markdown-it 解析成同一级 1 到 10 的有序列表。
- 实时编辑和阅读模式中，嵌套块前方的 BlockNote 缩进引导线在深色主题下显示为突兀白线，需要跟随应用主题颜色。

### 实现
- 在 `thirdToHtml` 的 Markdown 渲染前增加列表缩进兼容预处理，仅对围栏代码块外的列表项生效，将父列表下不足 4 空格的嵌套列表缩进补足，保持 split 预览与 BlockNote 解析结构一致。
- 保留松散表格、任务列表、列表符号、表格分隔行等既有后处理流程。
- 为 `.blocknote-live-editor` 的 BlockNote 缩进引导线增加应用主题色覆盖，使用 `var(--border)` 避免深色主题白线。

### 涉及文件或模块
- `src/utils/markdown/thirdToHtml.ts`
- `src/styles/editor.css`
- `tests/markdown.third.roundtrip.test.ts`
- `.project-memory/memory/2026-07-27.md`

### 验证结果
- `npx vitest run tests/markdown.third.roundtrip.test.ts`：通过，39 个测试。
- `npm test`：通过，33 个测试文件 / 216 个测试；仍有既有的 Vite CJS deprecation、updater 404 降级日志、linkify 重复注册日志、jsdom `window.scrollBy` 未实现和性能诊断 stderr 输出，未导致失败。
- `npm run build`：通过。
- `git diff --check`：通过；仍提示既有未提交文件 `AGENTS.md`、`设计文档0.md` 的 LF/CRLF 转换警告。
- 活跃代码文件行数扫描：`thirdToHtml.ts` 612 行、`editor.css` 626 行、`markdown.third.roundtrip.test.ts` 550 行，均未超过 800 行。

### 已知限制或未处理的无关问题
- `AGENTS.md` 和 `设计文档0.md` 当前存在非本次任务的未提交修改，本次未触碰、未暂存。
- 本次只修复 split 预览列表结构和 BlockNote 缩进引导线颜色，没有调整列表编辑器其它交互。

### Git 提交信息
- 提交标题：`fix: 修复分栏列表解析和缩进引导线颜色`

## 7. 修复列表缩进兼容上下文退出

### 需求
- 审核发现 `normalizeLooseListIndentation` 在普通非列表行后仍保留 `listStack`，会把后续两空格列表项误认为仍处于上一个列表上下文。

### 实现
- 非列表行根据当前行缩进弹出 `listStack`，同级或更浅缩进会退出旧列表上下文，避免跨普通段落继续补足嵌套列表缩进。
- 补充回归测试覆盖 `1. one` / `paragraph` / `  1. nested` 场景，确保不会生成嵌套列表或缩进代码块。

### 涉及文件或模块
- `src/utils/markdown/thirdToHtml.ts`
- `tests/markdown.third.roundtrip.test.ts`
- `.project-memory/memory/2026-07-27.md`

### 验证结果
- `npx vitest run tests/markdown.third.roundtrip.test.ts`：通过，40 个测试。
- `npm test`：通过，33 个测试文件 / 217 个测试；仍有既有 Vite CJS deprecation、updater 404 降级日志、linkify 重复注册日志、jsdom `window.scrollBy` 未实现和性能诊断 stderr 输出，未导致失败。
- `npm run build`：通过。
- `git diff --check`：通过；仍提示既有未提交文件 `AGENTS.md`、`设计文档0.md` 的 LF/CRLF 转换警告。
- 活跃代码文件行数扫描：本次修改文件 `thirdToHtml.ts` 616 行、`markdown.third.roundtrip.test.ts` 565 行，均未超过 800 行；同时发现既有 `src/i18n/locales/zh-CN.ts` 和 `src/i18n/locales/en.ts` 各 805 行，非本次改动，未处理。

### 已知限制或未处理的无关问题
- `AGENTS.md` 和 `设计文档0.md` 当前存在非本次任务的未提交修改，本次未触碰、未暂存。
- `src/i18n/locales/zh-CN.ts` 和 `src/i18n/locales/en.ts` 当前各 805 行，属于既有行数超限问题，本次未做无关拆分。
- 本次只收紧上次两空格列表兼容逻辑的上下文退出，不调整其它 Markdown 列表规则。

### Git 提交信息
- 提交标题：`fix: 修复列表缩进兼容上下文退出`

## 9. 修复分栏无序列表层级与预览滚动重置

### 需求
- 分栏视图需要正确解析两空格递进的无序列表，避免嵌套项全部降为一级或部分列表项不渲染。
- 兼容用户示例中同一列表末尾紧贴连字符的 `-数值计算有最大范围`。
- 分栏预览随源码编辑同步更新时，不得清空预览并跳回顶部，应保留用户当前滚动位置。

### 实现
- `normalizeLooseListIndentation` 分别记录源码缩进与补足后的规范缩进，确保连续的两空格二级、三级列表按真实父子关系逐层补足到 markdown-it 所需缩进。
- 仅在当前列表已有同级缩进上下文时兼容紧贴连字符的无序列表项，避免把独立的 `-foo` 或负数误判为列表。
- 无任务列表占位符时跳过任务列表 HTML 后处理，避免普通嵌套 `<ul>/<li>` 被状态机误删。
- 分栏预览生成下一份 HTML 的等待期间继续保留上一份预览快照，不再短暂卸载预览 DOM，因此实时更新前后保持原滚动位置。
- 补充用户原始无序列表结构与分栏预览滚动保持的回归测试。

### 涉及文件或模块
- `src/utils/markdown/thirdToHtml.ts`
- `src/components/editor/useDeferredMarkdownPreview.ts`
- `tests/markdown.third.roundtrip.test.ts`
- `tests/editor.performance.test.tsx`
- `.project-memory/memory/2026-07-27.md`

### 验证结果
- `npx vitest run tests/markdown.third.roundtrip.test.ts`：通过，41 个测试。
- `npx vitest run tests/editor.performance.test.tsx -t "keeps the current split preview"`：通过，1 个目标测试。
- `npm test`：通过，33 个测试文件 / 219 个测试；仍有既有 Vite CJS deprecation、updater 404 降级日志、linkify 重复注册和性能诊断 stderr 输出，未导致失败。
- `npm run build`：通过，2952 个模块完成生产构建。
- 本地生产预览核验：用户示例渲染为 4 个一级项并保留二级、三级结构；预览滚动到 1200px 后继续编辑，等待同步渲染完成仍为 1200px，且页面控制台无 error。
- `git diff --check`：通过；仅有工作区 LF/CRLF 转换提示。
- 本次修改文件均未超过 800 行：`thirdToHtml.ts` 620 行、`useDeferredMarkdownPreview.ts` 67 行、`markdown.third.roundtrip.test.ts` 589 行、`editor.performance.test.tsx` 246 行。

### 已知限制或未处理的无关问题
- `AGENTS.md` 和 `设计文档0.md` 当前存在非本次任务的未提交修改，本次未触碰、未暂存。
- `src/i18n/locales/zh-CN.ts` 和 `src/i18n/locales/en.ts` 当前各 805 行，属于既有行数超限问题，本次未做无关拆分。
- 紧贴连字符语法只在已经存在的列表同级上下文中兼容；独立 `-foo` 仍按标准 Markdown 作为普通文本处理。

### Git 提交信息
- 提交标题：`fix: 修复分栏无序列表和预览滚动`

## 10. 将 AI 审核工作流切换为 OpenCodeReview

### 需求
- 调整 AI 审核代码工作流，使用 alibaba/open-code-review 审核 dev 分支推送提交。

### 实现
- `.github/workflows/code-review.yml` 保留 `push` 到 `dev` 的触发方式，安装 `@alibaba-group/open-code-review@1.7.17` 并调用 `ocr review`。
- 将既有 `vars.AI_API_URL`、`secrets.AI_API_KEY`、`vars.AI_MODEL` 映射为 OpenCodeReview 的 `OCR_LLM_URL`、`OCR_LLM_TOKEN`、`OCR_LLM_MODEL`，避免更换仓库变量/密钥名称；`vars.OCR_USE_ANTHROPIC` 未配置时默认 `false`，保持 OpenAI-compatible 协议。
- 继续支持 `vars.AI_TIMEOUT_MS`，未配置默认 `600000` 毫秒，并换算为 `OCR_LLM_TIMEOUT` 秒级请求超时与 `ocr review --timeout` 分钟级任务超时。
- 根据 `github.event.before` 与 `github.sha` 审核推送 diff；before 不存在或为零 SHA 时回退到 `ocr review --commit`。
- 生成 `ai-code-review-report.md`，继续写入 Actions Summary、7 天 artifact 和提交评论；删除旧的 `.github/scripts/ai-code-review.mjs` 自研 Chat Completions 请求脚本。
- 更新长期记忆中的 CI/CD 说明。

### 涉及文件或模块
- `.github/workflows/code-review.yml`
- `.github/scripts/ai-code-review.mjs`
- `.project-memory/memory/MEMORY.md`
- `.project-memory/memory/2026-07-27.md`

### 验证结果
- `npm test`：首次全量运行中 `tests/editor.performance.test.tsx` 的 `disables browser spellcheck for large documents and updates line count after idle` 偶发失败；重跑该定向测试通过，随后再次全量 `npm test` 通过（34 个测试文件 / 223 个测试）。
- `npm run build`：通过，Vite 生产构建完成。
- Workflow 校验：Node YAML 解析通过；确认 `dev` push 触发、OpenCodeReview 安装、`ocr review --from/--to` 与 `--commit` fallback；抽取 bash step 后 `bash -n` 通过；`npx @alibaba-group/open-code-review@1.7.17 review -h` 确认所用参数存在。
- `git diff --check`：通过；仅有本次修改文件的 LF/CRLF 转换提示。
- 活跃代码文件行数扫描：未发现超过 800 行的代码文件。

### 已知限制或未处理的无关问题
- 本次保留原有 dev push 到提交评论的报告模式；未切换为 PR inline review，因为 alibaba/open-code-review 官方 composite action 面向 PR 上下文，直接用于 push 事件无法拿到 PR 编号。
- 测试输出仍有既有 Vite CJS deprecation、updater 404 fallback、linkify 重复注册、jsdom `window.scrollBy` 未实现和 editor-performance diagnostics stderr，未导致最终失败。

### Git 提交信息
- 提交标题：`ci: 使用 OpenCodeReview 审核推送代码`
