# 2026-07-24 工作日志

## 1. 工具栏配置布局扁平化与响应式调整

### 需求
- 工具栏配置页每个按钮都使用独立卡片，视觉堆叠严重、窄窗口布局僵硬；改为更灵活紧凑的布局。

### 实现
- 将按钮配置改为单一连续列表，移除逐项卡片边框、圆角、背景和重复说明。
- 每行采用“按钮标识 / 放置位置 / 前置分隔符”网格，隐藏项弱化显示，重置操作改为轻量文本按钮。
- 720px 以下切换为两列并将分隔符控制换行，520px 以下切换为单列。
- 保留原有位置选择、分组、隐藏、分隔符及恢复默认逻辑，不增加拖拽排序或新的配置模型。

### 涉及文件或模块
- `src/components/settings/SettingsAppearanceSection.tsx`
- `src/styles/components/settings-panel.css`

### 验证结果
- `npx vitest run tests/toolbar.settings.test.ts`：通过，3 个测试。
- `npm test`：通过，21 个测试文件 / 179 个测试。
- `npm run build`：通过，1949 个模块完成构建。
- `git diff --check`：通过，仅有项目记忆文件的 Windows 换行提示。
- 本地生产预览：默认桌面宽度与 700×760 窄窗口均已核验；下拉菜单可正常展开，窄窗口正确换列。
- 本次修改的代码文件分别为 208 行和 404 行；仓库已有 `src/components/SettingsPanel.tsx` 为 835 行，本次未修改。

### 已知限制
- 本次只调整配置布局与视觉密度，未加入拖拽排序；现有按钮顺序和配置语义保持不变。
- `FkeMark-roadmap.md` 与反向链接修复仍是工作区既有未提交修改，本次未覆盖或暂存。

### Git 提交信息
- `优化：改进工具栏配置布局`
## 2. 长文档实时编辑停止自动整篇序列化

### 需求
- 超长 Markdown 在实时编辑视图输入后仍会反复出现长时间无响应；阅读、分栏和源码视图不受影响。

### 实现
- 保留 100000 字符的长文档阈值；长文档输入时只记录待处理编辑器并立即标记 dirty，不再在停止输入 300ms 后自动执行整篇 `getHTML()` 与 `htmlToMarkdown()`。
- 保存、自动保存、导出、切换模式及标签页等现有 `EditorHandle.getContent()` 边界仍会强制刷新最新内容，避免输入丢失。
- 短文档继续即时同步父级内容，行为不变。

### 涉及文件或模块
- `src/components/editor/useDeferredEditorChange.ts`
- `tests/editor.performance.test.tsx`
- `.project-memory/memory/MEMORY.md`

### 验证结果
- `npm test -- --run tests/editor.performance.test.tsx`：通过，4 个测试。
- `npm run build`：通过，1949 个模块完成构建。
- `git diff --check`：通过，仅有 Git 的 Windows 换行提示。
- 回归测试覆盖连续输入后停顿 10 秒仍不触发整篇序列化，以及 `getContent()` 会同步最新待保存输入。
- 本次修改的代码文件分别为 75 行和 155 行，均未超过 800 行限制。

### 已知限制
- 长文档输入期间，依赖父级 Markdown 文本的统计、目录和 AI 文档上下文保持为最近一次同步内容，直到保存、自动保存、切换模式或标签页等读取边界刷新。
- `FkeMark-roadmap.md` 为工作区既有未提交修改，本次未覆盖或暂存。

### Git 提交信息
- `修复：消除长文档实时输入的重复阻塞`

## 3. AI 对话侧栏停止抢回文档焦点

### 需求
- AI 对话侧栏保持打开时，用户回到文档输入第一个字符后，焦点会自动跳回 AI 输入框。

### 实现
- 将“侧栏打开时聚焦输入框”与 Escape 键监听拆为两个 effect。
- AI 输入框只在 `open` 状态变化时执行打开聚焦；父组件因文档内容更新而生成新的 `onClose` 回调时，只重新绑定键盘监听，不再触发 `focus()`。
- 保留新会话、清空、选择历史、发送完成和快捷动作等显式 AI 操作后的原有聚焦行为。

### 涉及文件或模块
- `src/components/ai/AiChatSidebar.tsx`
- `tests/ai.chat.test.tsx`
- `.project-memory/memory/MEMORY.md`

### 验证结果
- `npm test -- --run tests/ai.chat.test.tsx`：通过，6 个测试。
- 在仅包含本次焦点修复的干净分离工作树中执行 `npm run build`：通过，1949 个模块完成构建。
- 干净工作树 `git diff --check`：通过，仅有 Git 的 Windows 换行提示。
- 本地生产预览实际核验：打开 AI 时输入框正常获得焦点；点击实时编辑器并输入字符后，字符写入文档且焦点保持在编辑器，AI 输入框未重新获得焦点。
- 本次修改的代码文件分别为 347 行和 214 行，均未超过 800 行限制。

### 已知限制
- 本次只修复父组件重渲染导致的非预期抢焦点；显式 AI 操作仍会按原交互聚焦 AI 输入框。
- 主工作区同时存在未完成的工具栏配置调整，当前 `npm run build` 会被其 `EditorLayout.tsx` 类型错误阻塞；相关文件未被本次修改或暂存，干净工作树构建已通过。

### Git 提交信息
- `修复：避免 AI 对话窗口抢回编辑焦点`

## 4. 工具栏双容器拖拽布局与扩展按钮

### 需求
- 将工具栏配置改为上下两个小图标容器，可在容器内精确排序，也可在显示与隐藏区域之间移动；分割线同样作为可拖拽项。
- 将原本固定在工具栏尾部的片段、拼写检查和演示模式纳入配置，默认放在下方隐藏区域，并为图标提供悬浮说明。

### 实现
- 新增独立的工具栏布局编辑器，显示区与隐藏区均支持原生拖放；使用同步拖放目标引用，避免快速移动或嵌套 drop 冒泡导致落点漂移，并提供键盘跨区切换与左右排序。
- 将三处分割线升级为显式配置项，默认位置保持现有工具栏分组间隔；新增配置会保存按钮、分割线、显示状态和精确顺序。
- 将片段、拼写检查和演示模式加入工具栏配置，默认隐藏；显示后复用现有按钮和面板行为，不扩展 AI 上下文入口。
- 保留旧版下拉分组和 separatorBefore 设置的读取迁移，避免旧设置文件失效；新界面只负责显示、隐藏和排序。
- 同步前端默认值、Rust 反序列化默认值、中英文文案和编辑器渲染顺序。

### 涉及文件或模块
- `src/components/settings/ToolbarLayoutEditor.tsx`
- `src/components/settings/SettingsAppearanceSection.tsx`
- `src/components/editor/EditorLayout.tsx`
- `src/utils/toolbar.ts`
- `src/types/index.ts`
- `src/app/appDefaults.ts`
- `src-tauri/src/settings.rs`
- `src/styles/components/settings-panel.css`
- `src/i18n/locales/`
- `tests/toolbar.settings.test.ts`
- `tests/toolbar.layout-editor.test.tsx`
- `tests/editor.interactions.test.tsx`
- `tests/core.test.ts`

### 验证结果
- `npm test`：通过，22 个测试文件 / 184 个测试。
- `npm run build`：通过，1950 个模块完成生产构建。
- `cargo test --manifest-path src-tauri/Cargo.toml settings::tests`：通过，5 个设置兼容测试。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `rustfmt --edition 2021 --check src-tauri/src/settings.rs`：通过。
- 本地浏览器实际核验：显示区 19 项、隐藏区 3 项；键盘跨区移动、恢复默认布局及图标 title/aria 描述正常。
- 拖放组件测试覆盖跨容器精确落点、事件冒泡稳定性、同区排序和默认数组不被修改。
- 本次涉及的代码文件均未超过 800 行。

### 已知限制
- 旧版下拉分组配置继续兼容渲染；当用户在新布局界面拖动旧分组成员时，该成员会转换为普通工具栏项。
- 本次只纳入原本固定在编辑器工具栏尾部的三个功能，不调整 AI 选择入口或其他编辑器浮层。
### Git 提交信息
- `功能：支持拖拽配置工具栏布局`


## 2026-07-24 Line number display and live editing performance

- Request: fix the Show line numbers option when numbering stopped after about 800 lines; show line numbers in live, read, source and split modes; optimize slow live-mode typing for large Markdown documents.
- Implementation:
  - Changed `LineNumbers` from one React child per line to one `<pre>` text node, with line counting by scanning newline characters.
  - Added line-number gutters to live, read, source, split source and split preview paths in `EditorLayout`; source textarea gutters follow `scrollTop`.
  - Set TipTap `shouldRerenderOnTransaction: false` in `Editor` so live-mode ProseMirror edits update the DOM without forcing the full React editor layout to rerender on every transaction. Existing content sync and dirty-state logic remains in place.
- Files: `src/components/Editor.tsx`, `src/components/editor/EditorLayout.tsx`, `src/components/editor/LineNumbers.tsx`, `src/styles/overlays.css`, `tests/editor.interactions.test.tsx`.
- Verification:
  - `npm test -- tests/editor.interactions.test.tsx tests/editor.performance.test.tsx` passed (16 tests).
  - `npm test` passed (23 files / 189 tests; updater test still prints the expected GitHub 404 fallback log).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
  - `cargo test --manifest-path src-tauri/Cargo.toml` passed (16 tests).
  - `git diff --check` passed, with only LF/CRLF warnings from other currently modified files.
  - Local Vite + Chrome component QA confirmed 805-line gutters in live/read/source/split and one text-node gutter output. A live-mode smoke test with about 228k characters reflected a typed character in about 31ms.
- Known limitation: read mode and split preview show Markdown source line numbers; complex rendered block heights are not pixel-perfect visual-line mapping.
- Git commit: `修复：完善各模式行号显示并优化长文档输入`。
## 2026-07-24 本地版本快照与差异对比

- 需求：为 Markdown 文档增加本地版本快照、历史列表、差异对比和恢复入口。
- 实现：
  - Markdown 文件保存前自动记录旧内容，并支持从编辑器工具栏手动创建当前内容快照；快照按规范化文档路径隔离，相同内容去重，每个文件保留最近 50 份。
  - 新增本地历史列表与逐行差异界面，显示快照时间、大小、双侧行号和新增/删除标记；大文档差异采用前后缀降级，界面最多渲染 5000 行差异。
  - 恢复历史版本前显式确认，恢复后只回填编辑器并沿用现有未保存状态与保存流程；快照初始化或自动创建失败不会阻断正文保存。
  - 同步工具栏默认布局、旧设置补齐、拖拽配置入口及中英文资源。
- 涉及文件：`src-tauri/src/file_system/version_history.rs`、`src-tauri/src/file_system.rs`、`src-tauri/src/lib.rs`、`src-tauri/src/settings.rs`、`src/components/Editor.tsx`、`src/components/editor/EditorLayout.tsx`、`src/components/editor/VersionHistoryMenu.tsx`、`src/components/settings/ToolbarLayoutEditor.tsx`、`src/utils/versionHistory.ts`、`src/utils/toolbar.ts`、`src/i18n/locales/versionHistory.ts`、`src/styles/version-history.css`、相关语言资源与测试。
- 验证：版本历史定向前端测试和 Rust 测试通过；完整 `npm test`、`npm run build`、`cargo check`、`cargo test`、`git diff --check` 与代码文件行数检查在提交前完成并记录。
- 界面核验：使用实际组件、样式和中英文资源的本地 Vite 页面在 Edge 中检查工具栏浮层边界和差异弹窗；浮层已复用 `clampPopupPosition` 避免贴近窗口边缘时被裁切。
- 已知限制：文档移动或重命名后不会自动迁移旧路径对应的历史；历史仅对 `.md` 和 `.markdown` 保存自动创建快照。
- Git 提交信息：`功能：增加本地版本快照与差异对比`。


## 2026-07-24 Diff navigation, snapshot retention setting, line numbers, and docs

### Request
- Add Previous difference / Next difference buttons to the version diff dialog so users do not need to scroll manually.
- Add a setting for the maximum number of local version snapshots kept per file.
- Fix Show line numbers under View when live-mode line numbers stopped increasing after about 800 lines.
- Complete docs for recently added feature modules and toolbar buttons.

### Implementation
- Added diff navigation over rendered changed lines only, with previous/next buttons, position text, active-line highlighting, and scroll-to-current-difference behavior.
- Added snapshot retention options 10 / 25 / 50 / 100 with default 50; frontend defaults, settings persistence, Tauri settings compatibility, manual snapshots, normal saves, close-tab saves, update-before-install saves, and export writes now pass the selected limit.
- Updated Rust version-history pruning to use the provided limit and keep a backend safety clamp; older settings files missing the field still deserialize to the default 50.
- Updated LineNumbers to subscribe to TipTap update events and count lines from the current ProseMirror document, while keeping the content-prop fallback. This avoids stale line numbers when large live-mode documents no longer rerender the parent per transaction.
- Extracted the View settings section to src/components/settings/SettingsViewSection.tsx to keep SettingsPanel below the 800-line limit.
- Added Chinese and English docs for feature modules, toolbar buttons, version diff navigation, line numbers, and snapshot retention settings.

### Files or modules
- src/components/editor/VersionHistoryMenu.tsx
- src/components/editor/LineNumbers.tsx
- src/components/editor/EditorLayout.tsx
- src/components/SettingsPanel.tsx
- src/components/settings/SettingsViewSection.tsx
- src/utils/versionHistory.ts
- src/app/appDefaults.ts
- src/App.tsx
- src/app/useAppTabs.ts
- src/app/useAppUpdates.ts
- src/utils/importExport.ts
- src/types/index.ts
- src-tauri/src/file_system/version_history.rs
- src-tauri/src/lib.rs
- src-tauri/src/settings.rs
- src/i18n/locales/zh-CN.ts
- src/i18n/locales/en.ts
- src/i18n/locales/versionHistory.ts
- src/styles/version-history.css
- tests/version.history.test.tsx
- tests/editor.interactions.test.tsx
- doc/guide/features.md
- doc/en/guide/features.md
- doc/guide/toolbar.md and doc/en/guide/toolbar.md
- doc/guide/settings.md and doc/en/guide/settings.md
- doc/guide/editing.md and doc/en/guide/editing.md
- doc/guide/usage.md and doc/en/guide/usage.md
- doc/.vitepress/config.mts
- doc/index.md and doc/en/index.md
- doc/README.md

### Verification
- npm test -- --run tests/version.history.test.tsx tests/editor.interactions.test.tsx tests/export.formats.test.ts: passed, 22 tests.
- npm test: passed, 23 test files / 191 tests; updater test still prints the expected GitHub 404 fallback log.
- npm run build: passed.
- cargo fmt --manifest-path src-tauri/Cargo.toml -- --check: passed.
- cargo check --manifest-path src-tauri/Cargo.toml: passed.
- cargo test --manifest-path src-tauri/Cargo.toml: passed, 17 tests.
- git diff --check: passed, with only Git LF/CRLF warnings for this memory file and two Rust files.
- Code-file line scan: no TS/TSX/JS/JSX/MTS/CTS/RS/CSS/SCSS file under src, src-tauri/src, or tests exceeds 800 lines.
- Local Vite + Chrome UI check: Settings -> Behavior showed Version snapshot retention and options 10 / 25 / 50 / 100.
- doc npm run build: passed.

### Known limitations or unrelated issues
- Diff navigation only jumps within the currently rendered diff rows; very large diffs still use the existing 5000-rendered-line fallback.
- When snapshot content is identical to the latest snapshot, existing dedupe behavior returns the old snapshot. If the user lowered the retention limit, pruning occurs on the next newly created snapshot.

### Git commit information
- Commit title: 功能：完善版本差异导航、快照设置与文档

## 7. dev 分支推送代码审核工作流

### 需求
- 为 GitHub 仓库增加自建代码审核工作流，只在向 `dev` 分支推送时触发。

### 实现
- 新增独立的 `Code Review` GitHub Actions 工作流，触发条件限定为 `push.branches: [dev]`，不监听 Pull Request，也不需要 API 密钥。
- 前端检查执行 `npm ci`、`npm test`、`npm run build`，并对本次推送范围执行 `git diff --check`；首次创建分支或找不到前置提交时退回检查当前提交。
- Rust 检查安装 Tauri v2 所需的 Linux 系统依赖，执行 `cargo fmt --check`、`cargo check` 和 `cargo test`。
- 工作流只授予 `contents: read`，并启用同分支并发取消，避免旧检查占用 CI 资源。

### 涉及文件或模块
- `.github/workflows/code-review.yml`
- `.project-memory/memory/MEMORY.md`

### 验证结果
- 使用项目已有 `yaml` 依赖解析工作流成功，确认仅包含 `push -> dev` 触发及 `frontend`、`rust` 两个任务。
- `npm test`：通过，23 个测试文件 / 191 个测试。
- `npm run build`：通过，1954 个模块完成构建。
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，17 个测试。

### 已知限制
- 本工作流属于测试、构建和静态质量门禁，不包含 AI 对代码语义生成审核评论。
- 实际 GitHub Actions 运行将在本次文件推送到 `dev` 后首次触发。

### Git 提交信息
- 未单独提交；随后由 AI 审核报告实现替换。

## 8. 将 dev 代码审核替换为 AI 审核报告

### 需求
- 原工作流只验证编译和测试，不符合“AI 审核代码并发送报告”的目标。
- 保持 `dev` 分支 push 触发，AI URL、Key 和 Model 通过环境变量配置。

### 实现
- 移除 `code-review.yml` 中的前端和 Rust 编译检查，改为单一 AI 审核任务。
- 新增无第三方运行时依赖的 `.github/scripts/ai-code-review.mjs`，读取 `github.event.before` 到当前提交的 Git diff；首次分支推送或前置提交不可用时审核当前提交。
- 从仓库变量 `AI_API_URL`、`AI_MODEL` 和仓库 Secret `AI_API_KEY` 注入同名环境变量；`AI_API_URL` 使用完整的 OpenAI-compatible Chat Completions 端点。
- 审核提示要求只根据 diff 查找正确性、回归、安全、数据丢失、并发、边界、性能和测试缺口，并忽略代码差异中的提示注入指令。
- 报告写入 `ai-code-review-report.md`，同时发布到 Actions Summary、保留 7 天的 Actions Artifact，并使用 `GITHUB_TOKEN` 评论到本次 push 的提交。
- AI 调用失败或缺少配置时仍生成失败报告，后续 `always()` 步骤继续发布报告，同时保持工作流失败状态。
- 单次发送的 diff 上限为 70000 个字符；超过时在报告中明确标记截断。

### 涉及文件或模块
- `.github/workflows/code-review.yml`
- `.github/scripts/ai-code-review.mjs`
- `.project-memory/memory/MEMORY.md`

### 验证结果
- 工作流 YAML 解析通过，确认仅由 `push -> dev` 触发，且配置 `AI_API_URL`、`AI_API_KEY`、`AI_MODEL`。
- `node .github/scripts/ai-code-review.mjs --self-test`：通过，覆盖常见审核响应文本提取格式。
- 本地模拟 OpenAI-compatible HTTP 接口集成验证：请求模型、Bearer/API-Key 请求头、Git diff 消息、成功报告均符合预期。
- 缺少三个 AI 环境变量的失败路径验证：脚本以失败状态退出并生成包含缺失项的报告。
- 脚本 167 行，工作流 79 行，均未超过 800 行限制。

### 已知限制
- 当前接口契约为 OpenAI-compatible Chat Completions，即请求体使用 `model` 与 `messages`；不自动把 Base URL 拼接为具体端点。
- 超过 70000 个字符的差异只审核前半部分；完整覆盖超大推送需要后续按文件分批审核。
- 报告按提交评论，不生成逐行 GitHub Review 注释，也不发送邮件或外部 IM 通知。

### Git 提交信息
- 提交标题：功能：增加 dev 分支 AI 代码审核

## 9. 修复长文档行数与设置项间距

### 需求
- 修复开启行号后，超过 800 行的长文档在编辑视图和状态栏中最多只显示到 800 行的问题。
- 统一设置页每一项的上下边距和分割线表现，即使一行内左右控件高度不同也保持一致。

### 实现
- 新增共享的文档行数统计函数，状态栏不再直接使用 `split('\n')`。
- 将 TipTap 文档行数统计抽到 `editorLineCount`，行号组件挂载时立即从当前编辑器状态同步一次，避免在大文档延迟序列化期间重新开启行号时仍显示旧内容行数。
- 编辑器更新时向 App 回传实时行数；大文档编辑只标记 dirty、不立即 Markdown 序列化时，状态栏仍使用当前 TipTap 文档行数。
- 设置项行样式统一通过 `.settings-row` 控制上下 padding、最小高度、横向间距和分割线；直接属于设置组的最后一项也保留分割线，嵌套行保持原来的内部收尾处理。

### 涉及文件或模块
- `src/App.tsx`
- `src/app/AppLayout.tsx`
- `src/components/Editor.tsx`
- `src/components/editor/LineNumbers.tsx`
- `src/components/editor/useDeferredEditorChange.ts`
- `src/components/editor/editorLineCount.ts`
- `src/utils/documentStats.ts`
- `src/styles/components/settings-panel.css`
- `tests/editor.interactions.test.tsx`
- `tests/statusbar.stats.test.ts`

### 验证结果
- `npm test -- --run tests/editor.interactions.test.tsx tests/statusbar.stats.test.ts`：通过，2 个测试文件 / 19 个测试。
- `npm test`：通过，23 个测试文件 / 192 个测试。
- `npm run build`：通过。
- `git diff --check`：通过，仅有 Git LF/CRLF 提示。
- 代码文件行数扫描：本次涉及的 TS/TSX/CSS 测试文件均未超过 800 行。
- 本地 Vite + Chrome UI 核验：设置行的 `paddingTop` / `paddingBottom` 均为 10px，直接设置项分割线为 1px solid。

### 已知限制或未处理的无关问题
- 本次只修复行数显示与设置行视觉一致性，没有处理此前记录的长 Markdown 输入性能专项问题。

### Git 提交信息
- 未提交。

## 10. AI 审核请求超时环境变量

### 需求
- 将 AI 代码审核接口的固定超时时间改为环境变量配置，未配置时默认 10 分钟。

### 实现
- 工作流从 GitHub Actions Repository variables 读取可选的 `AI_TIMEOUT_MS`，并传给审核脚本。
- 审核脚本将空值回退为 `600000` 毫秒；自定义值必须是大于 0 的安全整数，否则生成包含明确原因的失败报告。
- `AbortSignal.timeout` 使用解析后的超时值，不改变现有 API 地址、密钥和模型配置方式。

### 涉及文件或模块
- `.github/workflows/code-review.yml`
- `.github/scripts/ai-code-review.mjs`
- `.project-memory/memory/MEMORY.md`

### 验证结果
- `node --check .github/scripts/ai-code-review.mjs`：通过。
- `node .github/scripts/ai-code-review.mjs --self-test`：通过，覆盖默认值、自定义值、零值和非法文本。
- 使用项目已有 `yaml` 依赖解析工作流：通过，确认仍仅由 `push -> dev` 触发且已注入 `AI_TIMEOUT_MS`。
- `git diff --check`：通过（见最终检查）。

### 已知限制或未处理的无关问题
- `AI_TIMEOUT_MS` 单位为毫秒；工作流本身仍有 15 分钟的任务上限，因此配置超过该上限时会先被 GitHub Actions 终止。
- 本次不增加自动重试，避免重复请求和额外 API 消耗。

### Git 提交信息
- 未提交。

## 11. 小地图跟随实际视图显示

### 需求
- 小地图需要跟随当前实际视图显示；源码模式显示源码，阅读/实时预览和分栏右侧预览不应继续显示 Markdown 源码。

### 实现
- `Minimap` 增加渲染态小地图分支：源码模式保留逐行源码缩略显示，非源码视图使用 Markdown 渲染后的 HTML 显示。
- 分栏模式右侧小地图绑定右侧预览滚动容器，并复用分栏预览 HTML；左侧小地图仍跟随源码栏。
- 增加渲染态小地图的紧凑样式，保持小地图不可交互、只作为视图缩略辅助。
- 补充回归测试覆盖源码、阅读和分栏右侧小地图的显示差异。

### 涉及文件或模块
- `src/components/editor/Minimap.tsx`
- `src/components/editor/EditorLayout.tsx`
- `src/styles/overlays.css`
- `tests/editor.interactions.test.tsx`

### 验证结果
- `npm test -- --run tests/editor.interactions.test.tsx`：通过，14 个测试。
- `npm test`：通过，23 个测试文件 / 193 个测试。
- `npm run build`：通过。
- 本地 Vite + Chrome UI 核验：源码模式小地图显示 `# QA Minimap` 源码；阅读模式与分栏右侧小地图均显示渲染后的 `h1`，不再显示 `#` 源码标记。
- 代码文件行数扫描：本次修改的 TSX/CSS/测试文件均未超过 800 行。
- `git diff --check`：见最终提交前检查。

### 已知限制或未处理的无关问题
- 本次只调整小地图与当前视图的显示一致性，没有改变小地图滚动比例算法或长 Markdown 性能策略。

### Git 提交信息
- 提交标题：修复小地图跟随实际视图显示
