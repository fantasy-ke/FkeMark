# 2026-08-03 工作记录

## 更新项目版本至 0.2.5

- 需求：将 FkeMark 项目版本统一调整为 `0.2.5`。
- 实现：同步更新根 npm 包及锁文件、独立 MCP npm 包、Tauri v2 配置、Rust 包清单、仓库根目录兼容配置、自动更新与“关于”页降级版本、更新信息示例和 Windows 便携包文件名；保留第三方依赖 `isomorphic.js` 的 `^0.2.4` 版本范围。
- 涉及文件或模块：`package.json`、`package-lock.json`、`packages/fkemark-mcp-server/package.json`、`tauri.conf.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`scripts/build.cjs`、`src/app/useAppUpdates.ts`、`src/components/settings/SettingsAboutSection.tsx`、`src/utils/updater.ts`。
- 验证结果：`npm test` 通过；`npm run build` 通过并显示 `fke-mark@0.2.5`；`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml` 通过，24 个 Rust 测试成功且本地忽略的 `src-tauri/Cargo.lock` 已刷新为 `0.2.5`；`npm pack --dry-run --ignore-scripts` 生成预检包名 `fkemark-mcp-server-0.2.5.tgz`；`node --check scripts/build.cjs`、`git diff --check` 和活跃代码文件 800 行限制检查通过。
- 已知限制或未处理的无关问题：本次仅更新版本元数据，不创建 `v0.2.5` 标签、不推送远端、不触发发布；前端全量测试仍输出既有的 jsdom `window.scrollBy` 未实现、linkify 重复初始化和性能诊断日志，但测试命令通过。
- Git 提交信息：`build: 更新项目版本至 0.2.5`。

## 缩小 Markdown 渲染标题上边距

- 需求：优化 Markdown 渲染后标题上方间距过大的问题，使标题与上一段内容之间更紧凑。
- 实现：将 HTML 渲染区域 H1/H2/H3 上边距从 40/36/28px 缩小为 28/24/20px，并为 H4/H5/H6 补充 18/16/16px 的明确上边距；保留原有下边距、字号和标题层级关系。
- 涉及文件或模块：`src/styles/editor.css`、`tests/editor.typography-scope.test.ts`。
- 验证结果：新增回归测试在修改前失败、修改后通过；`npm test -- --run tests/editor.typography-scope.test.ts`、`npm test`、`npm run build` 通过；在本地应用分栏模式中渲染 H1-H6 实际核验，计算样式分别为 28/24/20/18/16/16px，界面显示间距更紧凑；`git diff --check` 和活跃代码文件 800 行限制检查通过。
- 已知限制或未处理的无关问题：本次仅调整 HTML 渲染预览的标题间距，不修改 BlockNote 实时编辑块间距；全量测试仍输出既有的 jsdom `window.scrollBy` 未实现、linkify 重复初始化和性能诊断日志，但测试命令通过。
- Git 提交信息：`fix: 缩小 Markdown 标题上边距`。
