# 2026-08-03 工作记录

## 更新项目版本至 0.2.5

- 需求：将 FkeMark 项目版本统一调整为 `0.2.5`。
- 实现：同步更新根 npm 包及锁文件、独立 MCP npm 包、Tauri v2 配置、Rust 包清单、仓库根目录兼容配置、自动更新与“关于”页降级版本、更新信息示例和 Windows 便携包文件名；保留第三方依赖 `isomorphic.js` 的 `^0.2.4` 版本范围。
- 涉及文件或模块：`package.json`、`package-lock.json`、`packages/fkemark-mcp-server/package.json`、`tauri.conf.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`scripts/build.cjs`、`src/app/useAppUpdates.ts`、`src/components/settings/SettingsAboutSection.tsx`、`src/utils/updater.ts`。
- 验证结果：`npm test` 通过；`npm run build` 通过并显示 `fke-mark@0.2.5`；`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml` 通过，24 个 Rust 测试成功且本地忽略的 `src-tauri/Cargo.lock` 已刷新为 `0.2.5`；`npm pack --dry-run --ignore-scripts` 生成预检包名 `fkemark-mcp-server-0.2.5.tgz`；`node --check scripts/build.cjs`、`git diff --check` 和活跃代码文件 800 行限制检查通过。
- 已知限制或未处理的无关问题：本次仅更新版本元数据，不创建 `v0.2.5` 标签、不推送远端、不触发发布；前端全量测试仍输出既有的 jsdom `window.scrollBy` 未实现、linkify 重复初始化和性能诊断日志，但测试命令通过。
- Git 提交信息：`build: 更新项目版本至 0.2.5`。

## 缩小 Markdown 渲染标题上边距

- 需求：优化 Markdown 渲染后标题上方间距过大的问题，使标题与上一段内容之间更紧凑。
- 实现：将 HTML 渲染区域 H1/H2/H3 上边距从 40/36/28px 缩小为 28/24/20px，并为 H4/H5/H6 补充 18/16/16px 的明确上边距；保留原有下边距、字号和标题层级关系。
- 涉及文件或模块：`src/styles/editor.css`、`tests/editor.typography-scope.test.ts`。
- 验证结果：新增回归测试在修改前失败、修改后通过；`npm test -- --run tests/editor.typography-scope.test.ts`、`npm test`、`npm run build` 通过；在本地应用分栏模式中渲染 H1-H6 实际核验，计算样式分别为 28/24/20/18/16/16px，界面显示间距更紧凑；`git diff --check` 和活跃代码文件 800 行限制检查通过。
- 已知限制或未处理的无关问题：本次仅调整 HTML 渲染预览的标题间距，不修改 BlockNote 实时编辑块间距；全量测试仍输出既有的 jsdom `window.scrollBy` 未实现、linkify 重复初始化和性能诊断日志，但测试命令通过。
- Git 提交信息：`fix: 缩小 Markdown 标题上边距`。

## 新增 GPU 渲染加速开关

- 需求：增加 GPU 渲染功能开关，提升页面滚动、切换和拖拽等交互的流畅度。
- 实现：新增持久化设置 `gpuRenderingEnabled`，前端与 Rust 默认开启并兼容旧设置；在高级实验性设置中增加中英文开关和搜索索引；通过根容器类名仅为侧栏、标签栏和编辑区滚动区域启用 `translateZ(0)` 与 `will-change` 合成层提示，关闭后立即恢复默认样式。
- 涉及文件或模块：`src/types/index.ts`、`src/app/appDefaults.ts`、`src-tauri/src/settings.rs`、`src/components/SettingsPanel.tsx`、`src/app/AppLayout.tsx`、`src/styles/layout.css`、`src/i18n/locales/zh-CN.ts`、`src/i18n/locales/en.ts`、`tests/gpu-rendering.settings.test.tsx`。
- 验证结果：新增回归测试修改前失败、修改后通过；定向 GPU 测试 2 项、前端全量测试 309 项、Rust 全量测试 25 项、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、定向 Rust 格式检查、`git diff --check` 和活跃代码文件 800 行限制检查均通过；本地页面实际核验默认开启、关闭/开启即时切换、根容器类名和侧栏计算样式，浏览器无错误日志。
- 已知限制或未处理的无关问题：该开关控制 WebView 可控的局部合成层提示，不修改系统 GPU 驱动或 WebView 启动参数；部分设备可能增加显存占用；更新检查仍输出既有网络 403/请求失败警告，但不影响本地页面核验。
- Git 提交信息：`feat: 新增 GPU 渲染加速开关`。

## 修正 GPU 渲染实验功能默认策略

- 需求：根据代码审查将实验性 GPU 渲染开关改为默认关闭，并移除可能造成冗余合成层和层叠上下文的样式。
- 实现：前端与 Rust 默认值统一改为关闭，旧设置缺少字段时保持关闭；同步更新兼容测试名称和断言；根容器类名改为无多余空格的条件拼接；移除 `translateZ(0)` 与无效的 `will-change: width`，滚动区域仅保留 `will-change: scroll-position`，侧栏仅保留 `opacity, transform` 提示。
- 涉及文件或模块：`src/app/appDefaults.ts`、`src-tauri/src/settings.rs`、`src/app/AppLayout.tsx`、`src/styles/layout.css`、`tests/gpu-rendering.settings.test.tsx`。
- 验证结果：定向 GPU 前端测试 2 项与 Rust 设置测试 11 项通过；`npm test` 共 309 项、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml` 共 25 项、定向 `rustfmt --check`、`git diff --check` 和活跃代码文件 800 行限制检查均通过；本地页面实际核验默认未勾选，手动开启和关闭可即时切换根容器类名及侧栏优化提示，且无浏览器错误日志。
- 已知限制或未处理的无关问题：滚动容器样式由自动化测试覆盖；未打开文档时页面不存在编辑区滚动节点，因此实际页面核验聚焦默认状态、开关交互、根容器类名和侧栏计算样式；全量测试仍输出既有更新接口 404、linkify 重复初始化和性能诊断日志，但命令通过。
- Git 提交信息：`fix: 默认关闭 GPU 渲染加速`。

## 增加代码块下边距

- 需求：代码块无论折叠还是展开都保留少量下边距，避免与后续内容紧贴。
- 实现：在 BlockNote 实时编辑代码块的共享容器上增加 `12px` 下边距；规则不依赖折叠状态属性，因此折叠和展开时一致生效；阅读模式与分栏预览继续沿用已有的 `20px` 垂直间距。
- 涉及文件或模块：`src/styles/editor.css`、`tests/code-block.theme-style.test.ts`。
- 验证结果：新增回归测试在修改前失败、修改后通过；定向测试 16 项、前端全量测试 310 项、`npm run build`、`git diff --check` 和活跃代码文件 800 行限制检查均通过；本地页面实际核验可折叠代码块在折叠与展开状态下的计算下边距均为 `12px`，浏览器无错误日志。
- 已知限制或未处理的无关问题：本次只调整实时编辑区代码块间距，不重复修改已有预览间距；全量测试仍输出既有更新接口 404、linkify 重复初始化和性能诊断日志，但命令通过。
- Git 提交信息：`fix: 增加代码块下边距`。
