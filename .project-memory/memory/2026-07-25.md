# 2026-07-25 工作记录

## 1. 修复渲染视图行号定位与空行编号

### 需求
- 修复实时编辑、阅读和分栏预览中，标题等高块渲染后占用多个连续行号、后续行号错位的问题。
- 修复长文档滚动到底前行号已经结束的问题。
- Markdown 空行也必须保留并占用独立行号。

### 实现
- 保留源码视图原有的固定行高单文本节点行号，避免影响源码编辑性能。
- 为渲染视图新增基于实际 DOM 几何位置的行号布局：标题、段落、列表、表格、代码块、分隔线和块级公式按真实顶部位置生成锚点，标题无论视觉高度多大都只对应一个源码行号。
- 将渲染锚点映射回 Markdown 源码行；非空行使用渲染块定位，空行在相邻锚点之间插值或在文档末尾外推，因此空行仍有独立编号。
- 行号容器跟随渲染根节点在滚动区域中的真实偏移，兼容标题外边距折叠；容器高度至少覆盖最终渲染内容和最后一个行号。
- 使用 ResizeObserver、MutationObserver 和 TipTap update 事件重新测量，兼容窗口宽度变化、内容更新及大文档延迟序列化。
- 渲染视图只挂载视口附近的行号，未取得视口尺寸时最多渲染前 200 个标记，避免长文档产生大量 React 节点。

### 涉及文件或模块
- `src/components/editor/EditorLayout.tsx`
- `src/components/editor/LineNumbers.tsx`
- `src/components/editor/renderedLineNumbers.ts`
- `src/styles/overlays.css`
- `tests/editor.interactions.test.tsx`
- `tests/rendered.line-numbers.test.ts`

### 验证结果
- 修改前基线：`npm test` 通过，23 个测试文件 / 193 个测试；`npm run build` 通过。
- 定向回归：`npx vitest run tests/rendered.line-numbers.test.ts tests/editor.interactions.test.tsx` 通过，2 个测试文件 / 20 个测试。
- 隔离工作树全量验证：`npm test` 通过，24 个测试文件 / 199 个测试；`npm run build` 通过。
- 当前工作区最终验证：`npm test` 通过，25 个测试文件 / 205 个测试；`npm run build` 通过。
- 实际界面核验：使用 323 行长 Markdown 在阅读模式和分栏预览中检查；第 1 行与一级标题顶部一致，第 2 行空行位于标题与正文之间，滚动到最大位置时第 323 行与最后一个段落顶部一致，末行行号可见；视口虚拟化在阅读模式顶部/底部分别仅挂载 8/18 个标记，在分栏预览顶部/底部分别仅挂载 4/11 个标记。
- `git diff --check` 通过，仅提示未纳入本次提交的 Tauri schema 文件存在 LF/CRLF 转换警告。
- 本次涉及代码文件均未超过 800 行，最大为 `EditorLayout.tsx` 的 713 行。

### 已知限制或未处理的无关问题
- 实现期间工作区出现另一组 AI 设置、类型、Tauri 配置和测试修改，本次未覆盖、未暂存、未提交这些文件。
- 仓库原有 `src/App.tsx` 为 810 行，已超过 800 行限制，但本次未修改该文件，未进行无关拆分。
- 本次仅涉及前端渲染与测试，不涉及 Rust 代码，因此未运行 Cargo 检查。

### Git 提交信息
- 提交标题：修复：同步渲染视图行号位置


## 2. AI 助手多上游格式与连接配置

### 需求
- 支持 Chat Completions、Responses、Anthropic Messages 三种上游请求格式。
- 新增默认关闭的“完整 URL”开关；关闭时由应用自动补全格式对应的请求路径。
- 新增连接测试、模型列表获取，并将模型型号改为可搜索的下拉输入。

### 实现
- 新增上游格式与完整 URL 持久化设置，并同步 TypeScript 默认值、Rust 默认值及旧设置缺失字段的反序列化兼容测试。
- 统一在 AI 请求入口按格式生成请求 URL、鉴权头、请求体、非流式响应和流式增量解析；兼容旧设置中已包含 `/chat/completions` 的地址，避免重复追加路径。
- Chat Completions、Responses、Anthropic Messages 分别自动追加 `/chat/completions`、`/responses`、`/messages`；完整 URL 开启时直接使用输入地址。
- 设置页新增上游格式选择、完整 URL 开关、连接测试按钮、模型列表获取按钮和基于 `datalist` 的可搜索模型选择框，并补充中英文文案与状态提示。
- 模型列表统一从请求地址推导标准 `/models` 路径；连接测试发送一次最小的真实非流式请求。

### 涉及文件或模块
- `src/types/index.ts`
- `src/app/appDefaults.ts`
- `src-tauri/src/settings.rs`
- `src/utils/aiAssistant.ts`
- `src/components/settings/SettingsAiSection.tsx`
- `src/styles/components/settings-panel.css`
- `src/i18n/locales/ai.ts`
- `tests/ai.assistant.test.ts`
- `tests/ai.settings.test.tsx`
- `tests/core.test.ts`

### 验证结果
- `npm test` 通过：25 个测试文件、205 个测试全部通过。
- `npm run build` 通过。
- `rustfmt --edition 2021 --check src-tauri/src/settings.rs` 通过。
- `cargo check --manifest-path src-tauri/Cargo.toml` 通过。
- `cargo test --manifest-path src-tauri/Cargo.toml` 通过：17 个 Rust 测试全部通过；仅有现存 Windows 链接器信息警告。
- `git diff --check` 通过。
- 实际界面核验通过：三种格式可切换，完整 URL 开关默认关闭且提示自动路径；模型输入呈可搜索组合框，连接测试与获取模型按钮布局无溢出；Anthropic 模式温度上限同步为 1。
- 本次修改涉及的代码文件均未超过 800 行。

### 已知限制或未处理的无关问题
- 获取模型列表按兼容接口的标准 `/models` 路径推导；使用非标准模型列表地址的自定义网关无法自动发现该地址。
- 连接测试会向当前配置的模型发送一次最小真实请求，可能产生极少量 API 用量。
- 仓库原有 `src/App.tsx` 为 810 行，已超过 800 行限制，但本次未修改该文件，未进行无关拆分。

### Git 提交信息
- 提交标题：新增 AI 助手多上游格式配置


## 3. 调整新建文档快速开始流程

### 需求
- 点击新建文件时不再立即创建包含“未命名文档 / 开始编写...”默认内容的 `未命名.md` 标签。
- 先显示模板快速开始弹窗，用户选择模板后再创建新的 Markdown 文档。
- 快速开始弹窗背景透明并在编辑区域居中。

### 实现
- 抽出 `useNewDocument` 管理快速开始弹窗状态；所有现有新建入口只负责打开弹窗，不再预先创建标签。
- 选择模板时一次性创建未保存的 `未命名.md` 标签并写入所选模板；选择空白文档时创建空内容标签且不再次弹出快速开始。
- 为 `createTab` 增加初始修改状态参数，确保新建模板文档直接显示“待同步”并进入正常保存流程。
- 将快速开始组件改为可关闭的模态弹窗，支持关闭按钮和 Escape；弹窗整体与内容层均保持透明背景，在编辑区域水平和垂直居中。
- 提升弹窗层级至编辑器工具栏之上，确保模板卡片和关闭按钮可以正常点击。
- 移除旧的默认内容空状态检测和异步模板覆盖逻辑；通过抽取当前功能将 `src/App.tsx` 从 810 行降至 790 行。

### 涉及文件或模块
- `src/App.tsx`
- `src/app/AppLayout.tsx`
- `src/app/useAppTabs.ts`
- `src/app/useNewDocument.ts`
- `src/components/EmptyState.tsx`
- `src/styles/onboarding.css`
- `tests/new.document.test.tsx`

### 验证结果
- 修改前基线：`npm test` 通过，25 个测试文件 / 205 个测试；`npm run build` 通过。
- 定向回归：`npx vitest run tests/new.document.test.tsx` 通过，2 个测试全部通过。
- 隔离工作树全量验证：`npm test` 通过，26 个测试文件 / 207 个测试；`npm run build` 通过。
- 当前混合工作区最终验证：`npm test` 通过，27 个测试文件 / 211 个测试；`npm run build` 通过。
- 实际界面核验：点击新建后标签数仍为 0，直接显示快速开始弹窗；弹窗及内部背景均为透明，水平和垂直中心偏移均为 0；选择日记模板后创建一个未保存的 `未命名.md` 标签并显示模板内容；关闭按钮不会创建标签，选择空白文档后创建空标签且弹窗关闭。
- 弹窗最终层级为 150，关闭按钮在命中测试中位于编辑器工具栏之上。
- 本次任务文件的 `git diff --check` 通过；所有涉及的代码文件均未超过 800 行，`src/App.tsx` 为 790 行。

### 已知限制或未处理的无关问题
- 验证期间工作区出现另一组编辑器性能相关未提交修改；本次未覆盖、未暂存、未提交这些文件。
- 本次仅涉及前端与测试，不涉及 Rust 代码，因此未运行 Cargo 检查。

### Git 提交信息
- 提交标题：调整新建文档快速开始流程

## 4. 重构长文档实时编辑渲染链路

### 需求
- 修复 Markdown 内容过多时，实时编辑继续输入后渲染响应可能阻塞至少 10 秒的严重性能问题。
- 重构输入事务的渲染与统计方式，避免每个按键重复遍历、序列化或布局整篇文档，并提交到 Git。

### 实现
- 将 TipTap 默认的 `CodeBlockLowlight` 替换为基础 `CodeBlock` 与自定义增量高亮插件；通过 `getChangedRanges()` 获取 transaction 变更范围，映射既有 Decoration，仅重新扫描和高亮受影响的代码块，普通段落输入不再遍历全文代码块。
- 长文档输入的状态栏行数统计改为 300ms 合并延迟，连续 transaction 不再同步执行整篇 ProseMirror 文档的 `textBetween()`。
- 长文档渲染行号先保留源 Markdown 行数兜底布局，再以 400ms 合并延迟执行实际 DOM 几何测量；MutationObserver、ResizeObserver 和 TipTap update 不再在每次输入时立即触发全文布局读取。
- 仅在超过 100000 字符的实时编辑模式启用 `content-visibility: auto` 与 `contain-intrinsic-size`，让 WebView2 优先布局视口附近块；同时关闭原生全文拼写扫描，避免浏览器在每次输入时检查整篇内容。短文档、阅读模式和已有保存/刷新语义保持不变。
- 将不再使用的 `@tiptap/extension-code-block-lowlight` 依赖替换为直接使用的 `@tiptap/extension-code-block`。

### 涉及文件或模块
- `package.json`
- `package-lock.json`
- `src/components/Editor.tsx`
- `src/components/editor/EditorLayout.tsx`
- `src/components/editor/LineNumbers.tsx`
- `src/components/editor/editorExtensions.ts`
- `src/components/editor/incrementalLowlight.ts`
- `src/components/editor/useDeferredEditorChange.ts`
- `src/styles/editor.css`
- `tests/editor.performance.test.tsx`
- `tests/editor.interactions.test.tsx`
- `tests/incremental.lowlight.test.ts`

### 验证结果
- 修改前基线：`npm test` 通过，25 个测试文件 / 205 个测试；`npm run build` 通过。
- 类型检查：`npx tsc --noEmit` 通过。
- 定向回归：增量代码高亮、编辑器性能、编辑器交互和渲染行号共 4 个测试文件 / 28 个测试全部通过。
- 最终全量验证：`npm test` 通过，27 个测试文件 / 211 个测试；`npm run build` 通过。
- 实际界面核验：在本地页面向实时编辑器载入 5000 行、218892 字符内容，长文档视口优化类和 `spellcheck=false` 正确生效；继续输入单个字符的自动化观测耗时约 70ms，内容立即出现在文档末尾，未复现 10 秒级阻塞。
- `git diff --check` 无空白错误，仅提示部分 Markdown 与测试文件后续可能发生 LF/CRLF 转换；所有活跃代码文件均未超过 800 行，本次最大文件为 793 行的 `src/components/Editor.tsx`。

### 已知限制或未处理的无关问题
- 精确渲染行号的全量 DOM 几何校准会在停止输入约 400ms 后执行；这是为保持标题、空行和长内容行号定位准确而保留的延迟校准，不再占用连续输入的关键路径。
- 超过阈值的实时编辑模式会暂时关闭浏览器原生拼写检查；切换到短文档或非实时编辑模式后恢复设置中的拼写检查行为。
- 本次仅涉及前端渲染与测试，不涉及 Rust/Tauri 代码，因此未运行 Cargo 检查。

### Git 提交信息
- 提交标题：修复：优化长文档实时编辑响应

## 5. 修复 AI 代码审核响应头超时

### 需求
- 修复 GitHub Actions 执行 `.github/scripts/ai-code-review.mjs` 时，Node 22 的 `fetch` 因 `UND_ERR_HEADERS_TIMEOUT` 提前失败的问题。
- 保持现有 OpenAI-compatible Chat Completions 请求、失败报告和 `AI_TIMEOUT_MS` 配置语义，不通过自动重试重复消耗 API。

### 实现
- 将 AI 审核脚本的 `fetch` 请求替换为 Node 原生 `http.request` / `https.request`，消除 Undici 独立响应头超时早于 `AI_TIMEOUT_MS` 触发的问题。
- 请求继续发送原有 Bearer、`api-key`、JSON 请求体，并显式设置 `Content-Length`；HTTP 状态、JSON 解析和审核文本提取行为保持不变。
- 继续使用单个 `AbortSignal.timeout(timeoutMs)` 控制完整请求，总超时后输出明确的毫秒值；其他网络失败保留底层错误信息。
- 未新增依赖，未增加自动重试。
- 扩展脚本自测，使用本地 HTTP 服务验证请求鉴权、消息体和响应提取。

### 涉及文件或模块
- `.github/scripts/ai-code-review.mjs`
- `.project-memory/memory/MEMORY.md`

### 验证结果
- `node --check .github/scripts/ai-code-review.mjs`：通过。
- `node .github/scripts/ai-code-review.mjs --self-test`：通过，本地 HTTP 集成请求成功。
- `npm test`：通过，27 个测试文件、211 个测试全部通过。
- `npm run build`：通过。
- `git diff --check`：通过，仅有 Git 的 LF/CRLF 转换提示。
- 活跃代码文件扫描：没有文件超过 800 行；修改后的审核脚本为 263 行。

### 已知限制或未处理的无关问题
- 尚未在真实 GitHub Actions 与实际 AI 上游上执行一次请求；需要推送后由 CI 验证外部网络与上游服务状态。
- 工作流任务仍有 15 分钟上限；`AI_TIMEOUT_MS` 配置超过该上限时，GitHub Actions 会先终止任务。
- 本次仅修改 Node.js CI 脚本，不涉及 Rust/Tauri，因此未运行 Cargo 检查。

### Git 提交信息
- 提交标题：修复 AI 审核响应头超时

## 6. 统一所有视图模式的行号处理

### 需求
- 源码、实时编辑、阅读和分栏四种视图模式都需要正确显示行号。
- 标题或长内容渲染为多行时，行号仍应对应 Markdown 逻辑行，不能按视觉行重复占号；空行也必须占用行号。
- 长文档滚动到底部时必须能显示最后一行行号，并避免行号更新重新引入输入卡顿。

### 实现
- 源码与分栏源码侧的行号改为按视口虚拟化，只创建可见区及缓冲区标记；使用固定逻辑行高计算位置，保留末尾空行，十万行文档也不再创建十万个行号 DOM 节点。
- 源码输入区统一关闭物理自动换行，并让搜索高亮层同步横向与纵向滚动，确保每个 Markdown 逻辑行只对应一个行号；长物理行通过横向滚动查看。
- 实时编辑、阅读和分栏预览统一启用长文档延迟几何测量；停止输入约 400ms 后校准标题、空行和换行内容的实际位置，连续输入期间使用源行映射兜底。
- 渲染行号的可见区筛选由全量遍历改为两次二分查找，只切片生成视口附近标记，降低长文档滚动时的计算量。
- 修复模式切换后源码输入区恢复了行号状态、但文本滚动位置未同步导致的错位；重新挂载源码或分栏输入区时恢复横纵滚动位置。

### 涉及文件或模块
- `src/components/editor/EditorLayout.tsx`
- `src/components/editor/LineNumbers.tsx`
- `src/components/editor/SearchHighlightOverlay.tsx`
- `src/components/editor/renderedLineNumbers.ts`
- `src/components/editor/sourceLineNumbers.ts`
- `src/styles/overlays.css`
- `tests/editor.interactions.test.tsx`
- `tests/editor.performance.test.tsx`
- `tests/rendered.line-numbers.test.ts`
- `tests/source.line-numbers.test.tsx`

### 验证结果
- 修改前基线：`npm test` 通过，27 个测试文件 / 211 个测试；`npm run build` 通过。
- 定向回归：编辑器交互、编辑器性能、源码行号和渲染行号共 4 个测试文件 / 32 个测试全部通过。
- 最终全量验证：`npm test` 通过，28 个测试文件 / 217 个测试；`npm run build` 通过。
- `git diff --check` 无空白错误，仅提示部分文件后续可能发生 LF/CRLF 转换。
- 实际界面核验：载入 168907 字符、5005 个逻辑行的 Markdown，源码、实时编辑、阅读和分栏模式均显示 5005 行；空白的第 2 行有独立行号，源码滚动到底部可见第 5005 行，行号 DOM 始终只保留约 30 个视口标记，模式切换后文本与行号滚动位置一致。
- 本次涉及的活跃代码文件均未超过 800 行；不涉及 Rust/Tauri，因此未运行 Cargo 检查。

### 已知限制或未处理的无关问题
- 为保证源码逻辑行号严格对应 Markdown 行，源码与分栏源码输入区不再自动折行；超长物理行需要横向滚动查看。
- 长文档渲染视图的精确 DOM 几何位置会在停止输入约 400ms 后校准；输入过程中优先保证响应速度，短暂使用逻辑行兜底位置。

### Git 提交信息
- 提交标题：修复：统一所有视图的行号处理
