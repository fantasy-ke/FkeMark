# 2026-07-26 工作记录

## 1. 删除行号并解除大文档保存阻塞

### 需求
- 删除设置中的“显示行号”功能，同时删除行号渲染实现和对应测试。
- 排查 800 行以上 Markdown 编辑后保存长时间无响应的问题，确认是否存在死循环，并补充能够定位卡顿阶段的日志。

### 实现
- 删除 `showLineNumbers` 设置字段、默认值、设置界面与中英文文案；移除实时、阅读、源码、分栏模式的行号组件、测量、虚拟化样式和专用测试。
- 未发现无界循环。前端原保存链路虽然先让出一次浏览器任务，但随后仍会同步遍历整篇 ProseMirror 文档；改为按最多 64 个顶层块或约 8ms 时间片执行 Markdown 序列化，并在时间片之间让出主线程。
- 分片序列化期间继续输入会使旧修订结果失效；旧结果只记录日志，不覆盖规范 Markdown，再自动处理最新修订，避免保存或切换视图使用陈旧内容。
- 将 Tauri `write_file_command` 改为异步命令，并通过 `spawn_blocking` 执行旧文件读取、版本快照和最终文件写入，避免同步磁盘操作阻塞 WebView 命令主线程。
- 版本快照由两次目录扫描减少为一次，创建快照后复用同一列表完成数量裁剪。
- 保存日志新增 `save.started`、`save.content-flush.started`、`save.disk-write.started` 和 8 秒阶段卡顿记录；Rust 返回历史目录初始化、旧文件读取、快照、最终写入和总耗时。超过 8 秒时提示用户导出性能日志，不自动强制关闭软件，避免未保存内容丢失。

### 涉及文件或模块
- `src/app/appDefaults.ts`
- `src/app/useDocumentSave.ts`
- `src/components/SettingsPanel.tsx`
- `src/components/editor/EditorLayout.tsx`
- `src/components/editor/useEditorMarkdownPipeline.ts`
- `src/components/settings/SettingsViewSection.tsx`
- `src/utils/markdown/proseMirrorSerializer.ts`
- `src-tauri/src/file_system/version_history.rs`
- `src-tauri/src/lib.rs`
- `src/i18n/locales/zh-CN.ts`
- `src/i18n/locales/en.ts`
- `src/styles/editor.css`
- `src/styles/overlays.css`
- `tests/document.save.test.tsx`
- `tests/editor.markdown-serializer.test.tsx`
- 删除 `src/components/editor/LineNumbers.tsx`、`renderedLineNumbers.ts`、`sourceLineNumbers.ts` 及对应测试。

### 验证结果
- `npm test`：通过，30 个测试文件 / 221 个测试。
- `npm run build`：通过，1961 个模块完成生产构建。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，18 个 Rust 测试；包含 900 行文档保存、版本快照和阶段指标覆盖。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- 真实界面核验：载入 900 段、1801 个源码行后可继续输入；切换分栏约 292ms，源码区和预览区正常挂载；设置页已无“显示行号”或“行号”文本。
- `git diff --check`：通过，仅有已有 LF/CRLF 提示；行号相关引用搜索为空；所有活跃代码文件均未超过 800 行。

### 已知限制或未处理的无关问题
- 浏览器界面无法覆盖 Tauri 原生文件选择与磁盘 I/O；原生保存路径由 Rust 全量测试、组件测试和 `cargo check` 覆盖。
- 8 秒保存监视器用于保持可操作并记录阶段，不会主动终止进程。若仍发生慢保存，应导出性能日志，重点查看 `save.content-flush.stalled` 或 `save.disk-write.stalled`，以及 `save.disk-write` 中的后端分阶段耗时。
- 既有自定义 Markdown 支持边界不变：非十进制有序列表样式会规范为数字列表；未完成上传的图片节点不写入 Markdown；未登记的第三方节点或标记按顶层块回退到 HTML→Markdown，并记录类型。
- `AGENTS.md` 存在用户预先修改，本次未暂存、未提交。

### Git 提交信息
- 提交标题：`perf: 移除行号渲染并解除大文档保存阻塞`

## 2. 按 Tolaria 架构重写实时编辑渲染

### 需求
- 研究并按 refactoringhq/tolaria 的 Markdown 编辑架构重写实时编辑渲染，解决约 800 行文档输入、切换分栏和保存时主线程长时间无响应的问题。
- 保留可定位解析、分块应用、序列化和保存阶段的性能日志，并明确新渲染器暂不兼容的自定义能力。

### 实现
- 将实时编辑器替换为应用生命周期内复用的 BlockNote 实例；实时、阅读、源码和分栏切换时不销毁编辑器 DOM。
- 输入事件只标记脏状态，停止输入 1500ms 或保存时才执行一次直接块序列化；保存刷新前先让出一个浏览器任务，避免同步阻塞点击和视图切换。
- 320 行或 16KB 以上 Markdown 优先交给 Worker 快速解析；快速解析器不支持的语法回退到 BlockNote 解析器。
- 320 个块以上先应用 48 个块，再按每帧 120 个块追加，应用期间临时禁止编辑，并可中止过期任务。
- 增加文档块缓存和基于块对象的序列化缓存，标准块不再经过 HTML 中转；Front Matter 与 Wiki 链接继续往返保存。
- 增加 BlockNote 解析、分块应用、序列化和 Markdown 刷新日志；既有保存日志继续区分内容刷新、磁盘写入和总耗时。
- 大文档应用完成后再次同步 DOM 属性，确保从源码切回实时编辑时浏览器拼写检查仍保持关闭。

### 涉及文件或模块
- `src/components/Editor.tsx`
- `src/components/editor/EditorLayout.tsx`
- `src/components/editor/useBlockNoteEditorController.ts`
- `src/components/editor/blockNoteMarkdown.ts`
- `src/components/editor/blockNoteFastMarkdown*.ts`
- `src/components/editor/blockNoteContentSwap.ts`
- `src/components/editor/blockNoteDocumentCache.ts`
- `src/components/editor/useEditorMarkdownPipeline.ts`
- `src/utils/markdown/blockNoteSerializer.ts`
- `doc/guide/renderer-compatibility.md`
- 编辑器交互、序列化、性能和 Wiki 链接测试。

### 验证结果
- `npm test`：通过，30 个测试文件 / 218 个测试。
- `npm run build`：通过，2869 个模块完成生产构建，并生成独立快速解析 Worker。
- 真实界面核验：800 行、9383 字符文档进入实时编辑约 693ms；输入事件约 89ms；切换分栏约 596ms；保存触发约 326ms，操作后界面仍可点击。
- 真实界面确认大文档 `spellcheck=false`，分栏时 BlockNote DOM 保持挂载但隐藏，源码区包含实时编辑结果。
- `git diff --check`：通过，仅有仓库既有 LF/CRLF 提示；所有活跃代码文件均未超过 800 行。

### 已知限制
- 新实时编辑区中的公式暂按 Markdown 文本插入；阅读和分栏预览仍可按原渲染器显示。
- 高亮、下划线、交互式脚注、图片尺寸调整和上传进度、自定义有序列表样式，以及部分旧 TipTap 2 浮层和右键菜单尚未迁移为 BlockNote 自定义 schema。
- 引入 BlockNote 与 Mantine 后生产构建仍有 vendor chunk 超过 600KB 的非阻断警告；后续可单独做按需加载和分包，不在本次性能故障修复范围内。
- 浏览器核验无法覆盖 Tauri 原生文件对话框和真实磁盘写入；原生保存阶段由既有保存日志与测试继续覆盖。

### Git 提交信息
- 提交标题：`perf: 按 Tolaria 架构重写 Markdown 渲染`
