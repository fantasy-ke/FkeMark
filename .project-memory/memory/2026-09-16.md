# 2026-09-16 工作记录

## 优化设置页布局、选项尺寸与下拉适配

- 需求：设置窗口搜索框原先用 sticky 悬浮在内容头部，且因负 top 不满顶；同时需要收紧各组件布局、选项尺寸，并改善下拉框在滚动区域内被裁切、宽度不适配的问题。
- 实现：搜索栏改为右侧内容区顶部固定条，高度与左侧品牌栏对齐，内容区独立滚动；下拉菜单通过 portal 挂到 document.body，按触发器定位、宽度适配长选项，空间不足时向上翻开并夹紧在设置窗口内。
- 涉及文件或模块：`src/components/SettingsPanel.tsx`、`src/components/Select.tsx`、`src/styles/components/settings-page.css`、`src/styles/components/select.css`、`src/utils/popupPosition.ts`、`tests/settings.section-spacing.test.ts`、`tests/popup-position.test.ts`。
- 验证结果：定向前端测试 5 个套件、15 个用例通过；完整前端测试 57 个套件、341 个用例通过；`npm run build` 通过；`git diff --check` 通过；活跃代码文件未超过 800 行。
- 已知限制或未处理的无关问题：AI 设置中的上游格式仍使用原生 `<select>`，本轮未改为自定义 Select；设置页仍可拖动，下拉定位在拖动过程中会随滚动/resize 更新。
- Git 提交信息：`feat: 优化设置页布局与下拉适配`。

---

## 修复图片上传方式无法切换

- 需求：设置页「图片上传」中的上传方式下拉无法选择更改。
- 实现：自定义 Select 改为在 click 时选中并关闭菜单，mousedown 只阻止默认行为并停止冒泡，避免 portal 到 body 后菜单先卸载、后续 click 穿透设置遮罩；设置遮罩仅在点击背景本身时关闭。
- 涉及文件或模块：`src/components/Select.tsx`、`src/components/SettingsPanel.tsx`、`tests/image.upload.settings.test.tsx`。
- 验证结果：`tests/image.upload.settings.test.tsx` 2 个用例通过；完整前端测试 58 个套件、343 个用例通过；`npm run build` 通过；活跃代码文件未超过 800 行。
- 已知限制或未处理的无关问题：工作区原有未跟踪的 `Temp/` 目录未纳入本次提交；`git diff --check` 因 Windows LF/CRLF 警告产生非空白错误输出。
- Git 提交信息：`fix: 修复设置页图片上传方式无法切换`。

---

## 验收并修复实时编辑器块级悬停操作

- 需求：实时编辑模式下，块前显示操作/添加图标；操作菜单支持删除与块格式转换；添加按钮在当前块下方插入空段落；源码/分栏/只读不显示。
- 实现：复用已有 `BlockActionRail`；按钮改为 `lucide-react` 的 `MoreVertical`/`Plus`；定位计入 `scrollTop`/`scrollLeft` 避免滚动错位；插入段落后关闭菜单；补齐悬停、添加、转换、删除、菜单关闭、嵌套块与滚动对齐的交互测试。
- 涉及文件或模块：`src/components/editor/BlockActionRail.tsx`、`tests/block-action-rail.test.tsx`（替换 `tests/block-action-rail.test.ts`）。
- 验证结果：定向测试 11 个用例通过；完整前端测试 58 个套件、353 个用例通过；`npm run build` 通过；`git diff --check` 通过；`BlockActionRail.tsx` 285 行，测试文件 312 行。
- 已知限制或未处理的无关问题：工作区原有未跟踪的 `Temp/` 目录未纳入本次提交；操作菜单仍用按钮右侧绝对定位，未接入 `popupPosition.ts`；删除最后一个块仍直接调用 `removeBlocks`。
- Git 提交信息：`fix: 修复块操作轨道滚动错位并补齐交互测试`。

---

## 将 Temp 目录加入 gitignore

- 需求：项目本地倾倒场 `Temp/` 不应出现在 Git 状态中。
- 实现：在 `.gitignore` 增加 `Temp/`。
- 涉及文件或模块：`.gitignore`。
- 验证结果：`git check-ignore -v Temp` 命中 `.gitignore:46:Temp/`；`git status` 不再列出 `Temp/`。
- 已知限制或未处理的无关问题：目录本身仍可留在工作区，只是不再被跟踪。
- Git 提交信息：`chore: 将 Temp 目录加入 gitignore`。

---

## 源码模式语法高亮改为符号字体颜色

- 需求：源码编辑器的 Markdown 特殊语法高亮不应把彩色字叠在原文上，而应改变符号本身的字体颜色。
- 实现：源码文本域改为透明字色并保留光标；语法覆盖层绘制正文与标记颜色，去掉加粗以免错位；搜索高亮只保留背景，不再覆盖字体颜色。
- 涉及文件或模块：`src/styles/overlays.css`、`tests/overlays.style.test.ts`。
- 验证结果：覆盖层样式测试 2 个用例通过；完整前端测试 58 个套件、354 个用例通过；`npm run build` 通过。
- 已知限制或未处理的无关问题：源码模式仍用 textarea + 覆盖层，无法在同一层直接给 textarea 混排着色；`git diff --check` 在 Windows 上仍可能因 LF/CRLF 产生非空白警告。
- Git 提交信息：`fix: 源码模式语法高亮改为符号字体颜色`。

---

## 更新项目版本至 0.2.6

- 需求：将 FkeMark 项目版本统一调整为 `0.2.6`。
- 实现：同步更新根 npm 包及锁文件、独立 MCP npm 包、Tauri v2 配置、Rust 包清单、便携包文件名、自动更新与“关于”页降级版本；保留第三方依赖 `isomorphic.js` 的 `0.2.5`。
- 涉及文件或模块：`package.json`、`package-lock.json`、`packages/fkemark-mcp-server/package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`scripts/build.cjs`、`src/app/useAppUpdates.ts`、`src/components/settings/SettingsAboutSection.tsx`、`src/utils/updater.ts`、`tests/app.updates.test.tsx`。
- 验证结果：完整前端测试 58 个套件、354 个用例通过；`npm run build` 显示 `fke-mark@0.2.6`；`cargo check --manifest-path src-tauri/Cargo.toml` 编译 `fke-mark v0.2.6` 通过。
- 已知限制或未处理的无关问题：本次仅更新版本元数据，不创建 `v0.2.6` 标签、不推送远端、不触发发布。
- Git 提交信息：`build: 更新项目版本至 0.2.6`。

---

## 优化文档站布局并补充内容

- 需求：文档前端布局过重，需要更干净的排版，并补充近期功能说明。
- 实现：去掉 Teek 卡片导航、站点统计和阅读高亮；正文改为暖纸直排；首页特征卡片收束为四项；中英文补充块级操作、源码语法颜色、WebDAV 同步和 0.2.6 更新说明。
- 涉及文件或模块：`doc/.vitepress/config.mts`、`doc/.vitepress/theme/custom.css`、`doc/.vitepress/theme/home.css`、`doc/index.md`、`doc/en/index.md`、`doc/guide/editing.md`、`doc/guide/features.md`、`doc/guide/settings.md`、`doc/guide/changelog.md` 及对应英文页。
- 验证结果：`doc` 目录 `npm run build` 完成页面渲染；前端全量测试 354 项通过。
- 已知限制或未处理的无关问题：VitePress Theme Teek 构建仍输出 iconfont 字体未解析警告，属既有问题。
- Git 提交信息：`docs: 优化文档站布局并补充 0.2.6 内容`。

---
