# 2026-07-30 工作记录

## 新增独立 MCP 设置与外部 Agent 服务

- 需求：将 MCP 从 AI 助手设置中拆成独立设置菜单；新增可供外部 Agent 调用的 MCP 服务；把原数据库权限文案改为适配 Markdown 文件操作的中英文文案。
- 实现：
  - 新增 `settings.nav.mcp` 独立菜单和 `SettingsMcpSection`，包含服务开关、允许 Markdown 文件夹、三档执行权限、能力对照和外部 Agent 配置示例。
  - 新增无依赖 stdio MCP Server `scripts/fkemark-mcp-server.cjs` 与 `npm run mcp:stdio`，支持列出、读取、搜索、提取大纲、写入、追加以及在完全访问模式下删除 `.md/.markdown` 文件，并限制在允许文件夹内。
  - 同步前端和 Rust 设置默认值与旧设置兼容字段，迁移 MCP 文案到 `src/i18n/locales/mcp.ts`，更新中英文设置文档和回归测试。
- 涉及文件或模块：`src/components/SettingsPanel.tsx`、`src/components/settings/SettingsMcpSection.tsx`、`scripts/fkemark-mcp-server.cjs`、`src/i18n/locales/mcp.ts`、`src/i18n/locales/zh-CN.ts`、`src/i18n/locales/en.ts`、`src/types/index.ts`、`src/app/appDefaults.ts`、`src-tauri/src/settings.rs`、`doc/guide/settings.md`、`doc/en/guide/settings.md`、`tests/mcp.settings.test.tsx`、`tests/mcp.server.test.ts`、`tests/core.test.ts`。
- 验证结果：
  - `npm test -- --run tests/ai.settings.test.tsx tests/mcp.settings.test.tsx tests/mcp.server.test.ts tests/core.test.ts`：通过，4 个测试文件、44 个测试。
  - `cargo test --manifest-path src-tauri/Cargo.toml settings::tests`：通过，8 个测试。
  - `node -c scripts/fkemark-mcp-server.cjs`：通过。
  - `node -e "const fs=require('fs'); JSON.parse(fs.readFileSync('package.json','utf8')); console.log('package.json ok')"`：通过。
  - `npm test`：通过，39 个测试文件、256 个测试；保留既有 Vite CJS 弃用提示、updater 预期日志、jsdom `window.scrollBy` 未实现提示和编辑器性能日志。
  - `npm run build`：通过。
  - `npm --prefix doc run build`：通过；保留既有 VitePress iconfont 运行时解析提示。
  - `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
  - `cargo test --manifest-path src-tauri/Cargo.toml`：通过，22 个测试。
  - `git diff --check`：通过。
  - 活跃代码文件行数检查：通过，未发现超过 800 行的代码文件。
- 已知限制或未处理的无关问题：MCP 服务由外部 Agent 以 stdio 方式启动，应用内开关只作为授权配置，不在桌面应用内常驻监听；本次只暴露 Markdown 文件工具，未新增非 Markdown 工作区工具；未进行真实 Tauri 界面手动核验，设置页渲染和交互由组件测试覆盖。
- Git 提交信息：`feat: 新增独立 MCP 设置与服务`
