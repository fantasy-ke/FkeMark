# 2026-07-28

## 代码块对比度、csharp 语言标识和 OCR 报告格式

- 需求：修复明亮主题下难以辨认的代码关键字与标记颜色，使用 `csharp` 替代 `C#`，并阻止 OpenCodeReview 报告将 Markdown 列表项错误包裹为 `diff` 代码块。
- 实现：
  - 将 BlockNote/Shiki 明亮主题切换为 `github-light-high-contrast`，加深明亮主题下 highlight.js 的注释与元信息标记颜色，同时保留代码块独立背景色及顶部、底部边框颜色。
  - 新增共享 Markdown 代码语言规范化逻辑，使 `C#`、`c#` 和 `CSharp` 在斜杠围栏创建、语言选择器输入、快速解析器、后备解析器输出及序列化流程中统一存储和输出为 `csharp`。
  - 收紧 `.github/workflows/code-review.yml` 的 OCR 报告格式化规则，使普通 Markdown `- item`、`+ item` 和任务列表保持正文或列表格式，仅将具有代码特征的差异行放入 `diff` 代码块。
- 涉及文件或模块：`.github/workflows/code-review.yml`、`src/components/editor/blockNoteSchema.ts`、`src/styles/variables.css`、`src/utils/markdown/codeLanguage.ts`、BlockNote 解析与序列化流程、编辑器语言处理路径及相关测试。
- 验证结果：
  - `npm test -- tests/blocknote.schema.test.ts tests/code-block.theme-style.test.ts tests/code-review.workflow.test.ts tests/editor.markdown-serializer.test.tsx` 通过。
  - `npm test` 通过：36 个测试文件、234 个测试。
  - `npm run build` 通过。
  - 已使用 Chrome 对构建后的 CSS 进行冒烟检查，确认明亮主题颜色：注释为 `rgb(71, 85, 105)`、关键字为 `rgb(124, 58, 237)`、元信息为 `rgb(51, 65, 85)`、代码块背景为 `rgb(241, 245, 249)`、顶部与底部边框为 `rgb(203, 213, 225)`。
  - `git diff --check` 通过，修改的代码、样式、测试和工作流文件均低于 800 行。
- 已知限制：OCR 格式化对于只有一个空格的含糊 `+/-` 行仍采用启发式判断，但普通列表和任务列表已经有测试覆盖。
- Git 提交：`fix: 修复代码高亮与 OCR 报告格式`。

## FkeMark 无字母品牌图标

- 需求：围绕项目主题重新设计 Logo，不使用 `F` 字母造型，并提交完整品牌资产。
- 实现：
  - 新增 512 像素扁平 SVG 标识，以折角本地文档、Markdown `#` 和陶土色输入光标组成，沿用应用配色（`#1F2937`、`#FAF9F6`、`#C96442`）。
  - 应用图标、启动页、网站图标、文档站导航与首页、桌面图标、macOS ICNS、Windows ICO 和 Windows Store 磁贴共用同一图形源。
  - 将手写光栅 `F` 图标生成器替换为基于 `public/logo.svg` 的 Tauri CLI 生成流程，只复制仓库跟踪的桌面端和 Windows 图标目标。
  - 新增品牌资产回归测试，确保应用与文档站的 Logo 源文件保持一致。
- 涉及文件或模块：`public/logo.svg`、`doc/public/logo.svg`、`index.html`、`src/splash.css`、`scripts/gen-icons.cjs`、`icons/`、`src-tauri/icons/` 和 `tests/branding.assets.test.ts`。
- 验证结果：
  - `npm run gen:icons` 通过。
  - `npm test -- tests/branding.assets.test.ts` 通过：3 个测试。
  - `npm test` 通过：37 个测试文件、237 个测试。
  - `npm run build` 通过。
  - `npm --prefix doc run build` 通过。
  - 已在构建后的文档站核验 24 像素导航图标和 320 像素首页图标；16 至 256 像素的明暗背景预览均清晰可辨。
  - `git diff --check` 通过，修改的文本文件均低于 800 行。
- 已知限制：安装器或系统外壳可能缓存旧图标，需要重新安装应用或等待操作系统图标缓存刷新。
- Git 提交：`feat: 统一 FkeMark 品牌图标`。

## 渲染器遗留代码与依赖清理

- 需求：清理当前渲染器不再使用的代码文件、过时测试和仅由旧实现使用的前端依赖，同时保持现有 BlockNote 编辑、Markdown 往返、图片、数学公式、标签和脚注能力不变。
- 实现：
  - 从 `src/main.tsx` 建立静态依赖图，并结合测试引用与当前 BlockNote 链路核对，删除 15 个不可达的旧 TipTap/ProseMirror 源码文件和 2 个只验证旧实现的测试文件。
  - 删除旧的 `AboutPage`、TipTap 扩展集合、增量 lowlight、高亮注册、图片/数学节点视图、可缩放图片、Typora 渲染插件、主题 Provider、旧 ProseMirror 序列化器和无引用 Markdown 桶入口。
  - 从 `package.json` 与锁文件移除 20 个不再直接使用的依赖，包括旧 TipTap 扩展、`lowlight`、前端 `@tauri-apps/plugin-fs` 和直接 `prosemirror-model`；保留当前仍由编辑器辅助逻辑使用的 `@tiptap/core`、`@tiptap/react`、`prosemirror-state` 与 `prosemirror-view`。
  - 删除 Vite 中失效的 lowlight/highlight.js 分包规则，将致谢中的 lowlight 更新为当前使用的 Shiki。
  - 将“关于”中英文资源拆分到 `src/i18n/locales/about.ts`，使两份主语言文件从 807 行降至 779 行，符合 800 行限制。
- 涉及文件或模块：旧渲染器组件与扩展、`package.json`、`package-lock.json`、`vite.config.ts`、`src/i18n/locales/about.ts`、中英文主语言文件及 Markdown 元数据测试。
- 验证结果：
  - `npm ci --ignore-scripts --dry-run` 通过，包锁与依赖声明一致。
  - `npm test` 通过：35 个测试文件、233 个测试。
  - `npm run build` 通过，生产构建继续生成 BlockNote Markdown Worker、Shiki 主题和动态语言包。
  - 静态依赖图复查后仅剩由 `index.html` 直接加载的 `src/splash.css` 与两个 TypeScript 声明文件不从 `src/main.tsx` 可达，均按用途保留。
  - `git diff --check HEAD` 通过；活跃 TS、TSX、JS、JSX、Rust、CSS、SCSS 文件均未超过 800 行。
- 已知限制：`src/styles/markdown.css` 和导出 HTML 样式仍保留 `.hljs-*` 兼容选择器，用于兼容已有或外部生成的 highlight.js HTML；本次仅删除运行时 lowlight 实现与依赖。
- Git 提交：`chore: 清理旧渲染器代码与依赖`。

## 项目版本更新至 0.2.2

- 需求：将 FkeMark 项目版本从 `0.2.0` 统一更新为 `0.2.2`。
- 实现：
  - 同步更新 `package.json`、`package-lock.json`、Tauri v2 配置、Rust 包清单和仓库根目录兼容配置中的项目版本。
  - 将更新模块和“关于”页的前端降级版本更新为 `0.2.2`，避免无法读取运行时版本时显示旧版本。
  - 将 Windows 便携包文件名更新为 `FkeMark-0.2.2-windows-portable.zip`，并同步更新更新信息类型中的版本示例。
  - 保留 `package-lock.json` 中依赖 `universalify@0.2.0` 的版本，不将依赖版本误改为项目版本。
- 涉及文件或模块：`package.json`、`package-lock.json`、`tauri.conf.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`scripts/build.cjs`、`src/app/useAppUpdates.ts`、`src/components/settings/SettingsAboutSection.tsx` 和 `src/utils/updater.ts`。
- 验证结果：
  - 项目各版本入口一致性检查通过，均为 `0.2.2`；忽略的生成文件 `src-tauri/Cargo.lock` 也由 Cargo 更新为 `0.2.2`。
  - `cargo check --manifest-path src-tauri/Cargo.toml` 通过，编译目标为 `fke-mark v0.2.2`。
  - `npm test -- tests/editor.performance.test.tsx` 通过：7 个测试。
  - `npx vitest run --maxWorkers=1 --minWorkers=1` 通过：35 个测试文件、233 个测试。
  - `npm run build` 通过，构建日志显示 `fke-mark@0.2.2`。
  - `git diff --check` 通过，活跃代码文件均未超过 800 行。
- 已知限制：默认并行执行 `npm test` 时，`tests/editor.performance.test.tsx` 中等待 80 毫秒的长文档样式断言偶发超时；该用例单独运行和全套测试单工作线程运行均通过，本次版本更新未修改相关编辑器实现。
- Git 提交：`build: 更新项目版本至 0.2.2`。

## 长代码块折叠与展开

- 需求：当渲染后的代码块高度超过指定高度时，在折叠区域底部中央显示带阴影的箭头，用于展开和再次收起；在编辑器设置中提供可持久化的启用开关。
- 实现：
  - 新增共享的代码块折叠控制器，实时编辑和阅读模式直接处理 BlockNote 代码块，分栏预览为普通 `pre` 创建临时包裹层；超过 320 像素时自动折叠，展开后保留底部收起按钮。
  - BlockNote 代码块渲染器追加不可编辑的按钮节点，并忽略折叠状态与按钮属性引发的 ProseMirror DOM 变更，避免编辑器重建节点或反复同步。
  - 使用主题变量绘制底部渐隐阴影、圆形箭头、悬停和键盘焦点状态，同时适配明亮、黑暗主题与减少动画偏好。
  - 新增 `codeBlockCollapseEnabled` 持久化设置，前端与 Rust 默认开启；旧设置文件缺少字段时自动使用默认值，并补充中英文设置说明与无障碍标签。
- 涉及文件或模块：`src/components/editor/useCodeBlockCollapse.ts`、`src/components/editor/blockNoteSchema.ts`、`src/components/editor/EditorLayout.tsx`、`src/styles/editor.css`、`src/components/settings/SettingsEditorSection.tsx`、中英文语言资源、前后端设置模型及相关测试。
- 验证结果：
  - `npm test -- --maxWorkers=1 --minWorkers=1` 通过：36 个测试文件、238 个测试。
  - `npm run build` 通过。
  - `cargo check --manifest-path src-tauri/Cargo.toml` 通过。
  - `cargo test --manifest-path src-tauri/Cargo.toml` 通过：21 个测试。
  - `rustfmt --edition 2021 --check src-tauri/src/settings.rs` 与 `git diff --check` 通过；活跃代码文件扫描未发现超过 800 行的文件。
  - 已在生产构建预览中核验实时编辑、阅读共用的 BlockNote 渲染路径和分栏预览：长代码块折叠到 320 像素，箭头可展开与收起；明亮和黑暗主题均可见；关闭设置后立即恢复完整高度并隐藏按钮，重新开启后恢复折叠。
- 已知限制：折叠阈值固定为 320 像素，设置项仅控制是否启用自动折叠；源码模式保留原始 Markdown 文本，不对围栏代码执行视觉折叠。
- Git 提交：`feat: 增加长代码块折叠功能`。

## 编辑区宽度档位调整

- 需求：重新定义设置中的编辑区宽度档位，使“窄”采用原“标准”宽度，“标准”采用原“宽”宽度，“宽”占实际可用编辑视图的 90% 并水平居中。
- 实现：
  - 将 `editorWidth` 的宽度映射调整为 `narrow = 800px`、`medium = 960px`、`wide = 90%`。
  - 将根样式中的编辑区默认最大宽度同步为 `960px`，避免编辑器初始化前后出现默认宽度跳变。
  - 新增编辑器交互测试，覆盖三档设置写入 `--editor-max-w` 的实际值。
- 涉及文件或模块：`src/components/Editor.tsx`、`src/styles/variables.css` 和 `tests/editor.interactions.test.tsx`。
- 验证结果：
  - `npm test -- tests/editor.interactions.test.tsx --maxWorkers=1 --minWorkers=1` 通过，1 个测试文件、18 个测试。
  - `npm test -- --maxWorkers=1 --minWorkers=1` 通过，36 个测试文件、239 个测试。
  - `npm run build` 通过。
  - 生产构建预览中实测：窄档内容宽度为 800px，标准档为 960px，宽档为可用编辑视图的 90%；宽档左右留白均为 51px，确认水平居中。
  - `git diff --check` 通过，活跃代码文件扫描未发现超过 800 行的文件。
- 已知限制：分栏模式继续沿用左右半栏各自铺满的既有布局，避免在已经受限的半栏内再次缩窄并造成源码栏与预览栏不对称。
- Git 提交：`fix: 调整编辑区宽度档位`。

## 标签栏溢出显示方式

- 需求：新增标签栏溢出处理，可选择单行水平滚动或多行自动换行平铺。
- 实现：
  - 新增 `tabOverflowMode` 持久化设置，默认为 `scroll`，旧设置文件缺少该字段时保持单行滚动。
  - 标签栏在 `scroll` 模式继续使用水平滚动；`wrap` 模式使标签自动换行并根据行数扩展栏高。
  - 在“设置 - 视图”新增“标签栏溢出”单选项，并补齐中英文文案与前端交互测试。
- 涉及文件或模块：`TabBar`、`SettingsViewSection`、应用设置类型与默认值、Tauri `settings.rs`、标签模式测试。
- 验证结果：
  - `npm test -- --maxWorkers=1 --minWorkers=1` 通过（240 项测试）。
  - `npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、`rustfmt --edition 2021 --check src-tauri/src/settings.rs` 和 `git diff --check` 通过。
  - 在当前生产构建预览中新建 20 个标签验证：单行模式保持 36px 高度且水平滚动；切换多行平铺后自动换行为 3 行、栏高 109px。
- 已知限制：标签名沿用现有最小宽度和文本截断规则，本次仅增加溢出布局方式。
- Git 提交：`feat: 增加标签栏溢出显示方式`。

## 标签栏滚轮横向滚动修复

- 需求：修复“单行滚动”模式下标签溢出后无法通过普通鼠标滚轮水平滚动的问题。
- 实现：仅当单行标签实际溢出时，将普通鼠标滚轮的纵向增量写入标签容器的 `scrollLeft`；保留触控板水平手势、Shift 滚轮和多行平铺的原生行为。
- 涉及文件或模块：`src/components/TabBar.tsx` 和 `tests/tabs.test.tsx`。
- 验证结果：
  - 新增溢出标签的滚轮交互测试；`npm test -- tests/tabs.test.tsx --maxWorkers=1 --minWorkers=1` 通过（4 项测试）。
  - `npm test -- --maxWorkers=1 --minWorkers=1` 通过（241 项测试），`npm run build` 和 `git diff --check` 通过。
  - 在当前生产构建预览中新建 19 个标签后，对标签栏执行普通竖向滚轮，`scrollLeft` 从 1148 变为 948。
- 已知限制：工作区中存在本次任务无关的 `src-tauri/Cargo.toml` 与 `src-tauri/src/lib.rs` 未提交修改，已保持未暂存。
- Git 提交：`fix: 支持标签栏滚轮横向滚动`。

## 标签栏非被动滚轮监听修复

- 需求：修复 React 18 被动滚轮监听中 `preventDefault()` 失效，导致页面纵向滚动与标签横向滚动同时发生的问题。
- 实现：将 `TabBar` 的 React `onWheel` 替换为 `useEffect` 注册的原生 `wheel` 监听器，显式设置 `{ passive: false }`，并在卸载或模式变更时清理监听器。
- 涉及文件或模块：`src/components/TabBar.tsx` 和 `tests/tabs.test.tsx`。
- 验证结果：
  - 滚轮测试断言 `scrollLeft` 发生偏移且原生 `WheelEvent.defaultPrevented` 为 `true`。
  - `npm test -- tests/tabs.test.tsx --maxWorkers=1 --minWorkers=1`、`npm test -- --maxWorkers=1 --minWorkers=1`（241 项测试）、`npm run build` 与 `git diff --check` 通过。
  - 在当前生产构建预览中滚轮标签栏：`scrollLeft` 从 1148 变为 948，`window.scrollY` 保持 0。
- 已知限制：工作区中存在本次任务无关的 `src-tauri/Cargo.toml`、`src-tauri/src/lib.rs`、`src/App.tsx`、`src/components/TopBar.tsx` 和 `src/main.tsx` 未提交修改，已保持未暂存。
- Git 提交：`fix: 使用非被动标签栏滚轮监听`。


## 移动端适配与 Android 打包入口

- 需求：增加移动端运行时适配、Android 打包生成入口，并保持既有项目目录和桌面构建链路不变。
- 实现：
  - 新增移动端平台识别，在 Android、iPhone、iPad 与 767px 以下窄屏时启用紧凑布局；移动端首屏默认收起侧栏。
  - 保留现有组件结构，仅通过 src/styles/mobile.css 覆盖样式：侧栏改为抽屉、工具栏可横向滚动、分栏纵向排列、状态栏与 AI 侧栏收紧；移动端隐藏桌面窗口拖拽与窗口控制按钮。
  - 新增 tauri:android:init、tauri:android:dev 与 tauri:android:build 脚本，命令从 src-tauri 目录调用 Tauri v2，并提供 Android 专用配置和 README 使用说明。
- 涉及文件或模块：src/utils/platform.ts、src/main.tsx、src/App.tsx、src/components/TopBar.tsx、src/styles/mobile.css、src/index.css、scripts/tauri-android.cjs、src-tauri/tauri.android.conf.json、tests/mobile.platform.test.ts、package.json 与 README.md。
- 验证结果：
  - npm test -- tests/mobile.platform.test.ts 通过（4 项）。
  - npm run build 通过；生产预览以 390 × 844 视口检查，确认默认侧栏收起、窗口控制未渲染，点击后侧栏抽屉可正常展开。
  - npm run tauri:android:init 已确认从 src-tauri 的 Tauri v2 配置启动；当前机器未配置 JDK/Android SDK，JAVA_HOME 缺失，因而未生成原生 Android 工程、APK 或 AAB。
  - 并行全量 npm test 有 1 项既有的长文档性能断言偶发失败（tests/editor.performance.test.tsx:256），该文件单独运行通过（7 项）；本次未改动相关编辑器逻辑。
- 已知限制或未处理的无关问题：当前环境缺少 Android 构建前置工具，需安装 JDK 17 和 Android SDK/NDK 并设置环境变量后执行打包；工作区已有的 src-tauri/src/lib.rs 修改未纳入本次任务。
- Git 提交：feat: 增加 Android 移动端适配与构建入口。


## 移动端专属布局与 Android 流水线产物

- 需求：流水线需要支持产出移动端包；移动端 UI 不能只做响应式收缩，需要具备移动端专属操作入口和布局，同时保持现有项目结构框架不乱。
- 实现：
  - 在 `.github/workflows/dev.yml` 和 `.github/workflows/release.yml` 新增 `build-android` job，安装 Node、JDK 17、Rust Android targets，复用现有 `tauri:android:init` 与 `tauri:android:build` 脚本，收集 `src-tauri/gen/android` 下的 APK/AAB 并上传为独立 Android artifact。
  - 发布阶段继续聚合所有桌面端产物，并将 Android APK/AAB 写入更新清单的 `downloads.android`，优先选择 APK，确保移动端包有稳定下载入口。
  - 新增 `src/components/mobile/MobileActionBar.tsx` 与 `src/components/mobile/MobileDocumentBar.tsx`，移动端提供底部文件/新建/保存/AI/设置操作栏、文档状态栏和紧凑模式切换，不复用桌面状态栏硬塞响应式样式。
  - 在 `AppLayout` 中按 `isMobileRuntime()` 切换移动端 shell，侧栏和 AI 改为互斥抽屉，抽屉背景可点击关闭；桌面端状态栏在移动端隐藏。
  - 将移动端文案拆到 `src/i18n/locales/mobile.ts`，由 `zh-CN.ts` 和 `en.ts` 合并导出，避免语言主文件贴近 800 行。
- 涉及文件或模块：`.github/workflows/dev.yml`、`.github/workflows/release.yml`、`src/app/AppLayout.tsx`、`src/components/mobile/`、`src/styles/mobile.css`、`src/i18n/locales/mobile.ts`、`src/i18n/locales/zh-CN.ts`、`src/i18n/locales/en.ts`、`tests/mobile.platform.test.ts`、`tests/mobile.layout.test.tsx`。
- 验证结果：
  - `npm test -- tests/mobile.platform.test.ts tests/mobile.layout.test.tsx` 通过，覆盖 Android workflow 产物入口和移动端布局组件。
  - `npm test` 通过，38 个测试文件、249 项测试。
  - `npm run build` 通过。
  - `git diff --check` 通过；活跃代码文件行数扫描未发现超过 800 行的文件。
  - 生产预览以 390 × 844 视口核验：`html.mobile-runtime` 生效，底部操作栏显示文件/新建/保存/AI/设置；新建文档后显示移动端文档栏与编辑/分栏/阅读/源码切换；文件与 AI 抽屉互斥，背景遮罩可关闭；桌面状态栏与顶部重复入口在移动端隐藏。
- 已知限制或未处理的无关问题：本机未执行真实 Android APK/AAB 打包；该步骤依赖 CI runner 或本地环境具备 JDK、Android SDK/NDK。`npm test` 中仍有既有 Vite CJS、updater 404 降级、linkify 重复初始化和 jsdom `window.scrollBy` 提示，测试结果均通过，未在本次范围内处理。
- Git 提交信息：`feat: 新增移动端布局与 Android 流水线产物`。
