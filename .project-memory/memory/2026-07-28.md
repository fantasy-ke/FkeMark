# 2026-07-28

## Code block contrast, csharp language id, and OCR report formatting

- Requirement: fix unreadable light-theme code keyword/token colors, use `csharp` instead of `C#`, and stop OpenCodeReview reports from wrapping Markdown list items as `diff` code blocks.
- Implementation:
  - Switched the live BlockNote/Shiki light theme to `github-light-high-contrast` and darkened light-theme highlight.js comment/meta token variables while preserving dedicated code-block background and top/bottom border colors.
  - Added shared Markdown code-language normalization so `C#` / `c#` / `CSharp` are stored and serialized as `csharp`, including slash-fence creation, language picker input, fast parser, fallback parser output, and serializers.
  - Tightened `.github/workflows/code-review.yml` OCR report formatting so normal Markdown `- item`, `+ item`, and task-list items remain prose/list text, while code-looking diffs still get ```diff` fences.
- Files/modules: `.github/workflows/code-review.yml`, `src/components/editor/blockNoteSchema.ts`, `src/styles/variables.css`, `src/utils/markdown/codeLanguage.ts`, BlockNote parser/serializer/editor language paths, and related tests.
- Validation:
  - `npm test -- tests/blocknote.schema.test.ts tests/code-block.theme-style.test.ts tests/code-review.workflow.test.ts tests/editor.markdown-serializer.test.tsx` passed.
  - `npm test` passed: 36 files / 234 tests.
  - `npm run build` passed.
  - Chrome smoke check against built CSS confirmed light code colors: comment rgb(71, 85, 105), keyword rgb(124, 58, 237), meta rgb(51, 65, 85), code block bg rgb(241, 245, 249), top/bottom border rgb(203, 213, 225).
  - `git diff --check` passed; changed code/style/test/workflow files are below 800 lines.
- Known limits: OCR formatting is still heuristic for ambiguous one-space `+/-` lines, but normal and task Markdown list items are covered by tests.
- Git commit: `fix: 修复代码高亮与 OCR 报告格式`.
