# 2026-07-29 工作记录

## 支持 PR 自动代码审查评论

- 需求：保留 `dev` push 的 OpenCodeReview 提交报告，同时在有人提交 PR 时自动审查并向 PR 发送评论。
- 实现：
  - 在 `.github/workflows/code-review.yml` 新增 `pull_request_target` 的 `opened`、`reopened`、`synchronize` 和 `ready_for_review` 触发，草稿 PR 不执行审查。
  - 保留 push 场景的 commit comment，并把 job 权限收敛到 `contents: write`。
  - 新增 PR 审查 job，固定 OpenCodeReview `v1.7.17` 对应的 commit，仅授予 `contents: read` 和 `pull-requests: write`；Action 使用 Git base/head 差异审查，不检出或执行 PR 代码，适配 fork PR 的 AI 评论场景。
  - PR 审查复用 `AI_API_URL`、`AI_API_KEY`、`AI_MODEL`、`AI_TIMEOUT_MS`、`OCR_USE_ANTHROPIC` 和 `OCR_VERSION`，其中 `AI_TIMEOUT_MS` 从毫秒转换为 Action 需要的秒。
  - 增加工作流测试，覆盖 PR 触发、job 条件、最小权限、Action 固定版本、增量评论配置，以及未直接 checkout PR head。
- 涉及文件：`.github/workflows/code-review.yml`、`tests/code-review.workflow.test.ts`、`.project-memory/memory/2026-07-29.md`。
- 验证：
  - `npm test -- tests/code-review.workflow.test.ts`：6 个测试通过。
  - `npm test`：38 个测试文件、254 个测试通过。
  - `npm run build`：通过。
  - `actionlint v1.7.12 .github/workflows/code-review.yml`：通过。
  - `git diff --check`：通过；活跃代码文件没有超过 800 行。
- 已知限制：`pull_request_target` 工作流需要存在于默认分支 `main` 才会触发 PR 审查；本地无法直接验证 GitHub PR 评论实际发送效果，首次贡献者 PR 是否需要人工批准取决于仓库 Actions 设置。
- Git 提交信息：`ci: 支持 PR 自动代码审查评论`。

## 修复 PR 审查配置中的问号占位符

- 需求：检查并处理 PR 审查相关改动中出现的三个问号占位符。
- 实现：
  - 恢复 `.github/workflows/code-review.yml` 中上次新增注释的中文内容，避免 OpenCodeReview 在 PR diff 中审查到问号占位符。
  - 恢复本日项目记忆的中文记录，清理上次写入时产生的问号占位符和乱码。
  - 在 `tests/code-review.workflow.test.ts` 增加断言，防止工作流文件再次包含三个连续问号。
- 涉及文件：`.github/workflows/code-review.yml`、`tests/code-review.workflow.test.ts`、`.project-memory/memory/2026-07-29.md`。
- 验证：
  - `npm test -- tests/code-review.workflow.test.ts`：6 个测试通过。
  - `actionlint v1.7.12 .github/workflows/code-review.yml`：通过。
  - `git diff --check`：通过。
  - `npm run build`：通过。
  - 任务相关文件已检查，没有三个连续问号、`U+FFFD` 替换字符或四个问号占位符。
  - `npm test`：失败于既有编辑器性能用例 `tests/editor.performance.test.tsx` 的 `editor-inner--large` 断言；单独复跑同一文件仍失败，本次未修改该区域。
- Git 提交信息：`ci: 支持 PR 自动代码审查评论`。

## 收敛 PR 审查密钥环境变量

- 需求：处理 PR 审查提出的安全意见，避免 `AI_API_KEY` 通过顶层环境变量暴露给 `pull_request_target` job。
- 实现：
  - 从 `.github/workflows/code-review.yml` 顶层 `env` 移除 `AI_API_KEY`。
  - 仅在 push 场景的 `Generate OpenCodeReview report` step 级别注入 `AI_API_KEY`，供提交报告脚本转换为 `OCR_LLM_TOKEN` 使用。
  - PR 审查 job 继续只通过 `llm_auth_token` input 把 secret 传给固定版本的 OpenCodeReview Action，不再继承 `AI_API_KEY` 环境变量。
  - 增加工作流测试，覆盖顶层环境变量不包含 `AI_API_KEY`、push step 独立注入密钥、PR job 不定义该环境变量。
- 涉及文件：`.github/workflows/code-review.yml`、`tests/code-review.workflow.test.ts`、`.project-memory/memory/2026-07-29.md`。
- 验证：
  - `npm test -- tests/code-review.workflow.test.ts`：6 个测试通过。
  - `actionlint v1.7.12 .github/workflows/code-review.yml`：通过。
  - `git diff --check`：通过。
  - `npm run build`：通过。
  - 任务相关文件已检查，没有三个连续问号、`U+FFFD` 替换字符或四个问号占位符。
  - `npm test`：失败于既有编辑器性能用例 `tests/editor.performance.test.tsx` 的 `editor-inner--large` 断言；单独复跑同一文件仍失败，本次未修改该区域。
- Git 提交信息：`ci: 支持 PR 自动代码审查评论`。

## 处理 PR 审查权限和超时意见

- 需求：处理 PR 审查提出的 artifact 权限、draft PR 说明和超时上限意见。
- 实现：
  - PR 审查场景改为 `upload_artifacts: 'false'`，保留 `contents: read` 与 `pull-requests: write` 最小权限，不额外授予 `actions: write`。
  - 在 `pull-request-review` job 的 `if` 前增加中文注释，说明 draft PR 的 `synchronize` 会触发但会跳过，`ready_for_review` 后才开始审查。
  - 在 `Convert review timeout` 中把 `AI_TIMEOUT_MS` 上限收敛到 `1620000` 毫秒，超出时钳制，避免超过 30 分钟 job 超时造成无意义挂起。
  - 增加工作流测试，覆盖关闭 artifact 上传、未授予 `actions: write`、draft PR 注释和超时上限。
- 涉及文件：`.github/workflows/code-review.yml`、`tests/code-review.workflow.test.ts`、`.project-memory/memory/2026-07-29.md`。
- 验证：
  - `npm test -- tests/code-review.workflow.test.ts`：6 个测试通过。
  - `actionlint v1.7.12 .github/workflows/code-review.yml`：通过。
  - `git diff --check`：通过。
  - `npm run build`：通过。
  - 任务相关文件已检查，没有三个连续问号、`U+FFFD` 替换字符或四个问号占位符。
  - `npm test`：失败于既有编辑器性能用例 `tests/editor.performance.test.tsx` 的 `editor-inner--large` 断言；本次未修改该区域。
- Git 提交信息：`ci: 支持 PR 自动代码审查评论`。

## 修复编辑器排版与检查设置

- 需求：修复编辑器正文行高、字体和字号设置不生效的问题；保持 Markdown 视图可跟随编辑器或独立设置；检查并修复“显示 Markdown 标记”和拼写检查。
- 实现：
  - 将编辑器字体、字号和紧凑/标准/宽松三档行高传入 BlockNote 容器，并由实际正文节点消费，避免只修改外围变量而不影响编辑内容。
  - 阅读模式和分栏预览统一解析 Markdown 视图设置；`inherit` 与 `0` 继续表示跟随编辑器，自定义字体和字号则只作用于视图。
  - 新增 BlockNote/ProseMirror Markdown 标记扩展，在光标位于粗体、斜体、行内代码、删除线、下划线、高亮和链接内容时恢复对应分隔符；关闭设置时同时隐藏分隔符和语法提示。
  - 在编辑模式切换和文档内容应用前后同步 `spellcheck` 与编辑器 DOM 类，防止 BlockNote 重建可编辑节点后覆盖拼写检查状态，并保留长文档下关闭原生拼写检查的既有策略。
- 涉及文件：`src/components/Editor.tsx`、`src/components/editor/EditorLayout.tsx`、`src/components/editor/useBlockNoteEditorController.ts`、`src/components/editor/markdownMarkerExtension.ts`、`src/styles/editor.css`、`tests/editor.interactions.test.tsx`、`.project-memory/memory/2026-07-29.md`。
- 验证：
  - `npx vitest run tests/editor.performance.test.tsx tests/editor.interactions.test.tsx --reporter=verbose`：28 个测试通过。
  - `npm test`：36 个测试文件、245 个测试全部通过。
  - `npm run build`：通过。
  - `npx tsc --noEmit --pretty false`：通过。
  - `git diff --check`：通过；活跃代码文件没有超过 800 行。
  - 生产预览实际核验：实时编辑字体/字号可切换为 Georgia/23px；阅读模式可独立切换为 Courier New/18px；三档正文行高分别为 1.5、1.8、2.2；Markdown 标记和拼写检查开关会实时更新，浏览器控制台无错误。
- 已知限制：原生拼写检查的词典内容和提示效果由操作系统/WebView 提供；本次已验证实时编辑与源码模式的 `spellcheck` 状态同步，不改变既有长文档性能降级策略。
- Git 提交信息：`fix: 修复编辑器排版与检查设置`。

## 调整正文行高并完善工具栏左键拖拽

- 需求：将紧凑、标准、宽松三档正文行高调整为 1.2、1.5、2；在工具栏布局设置中支持按住鼠标左键拖动，根据指针位置调整按钮顺序并切换显示或隐藏状态。
- 实现：
  - 将 BlockNote 正文行高映射和 CSS 回退、兼容类统一为 1.2、1.5、2。
  - 工具栏布局从原生 HTML5 拖放改为 Pointer Events；仅鼠标左键按住并移动超过 4 像素后开始拖拽，根据指针坐标识别“已显示”和“已隐藏”区域、换行位置及按钮前后插入位置，松开时复用现有 `moveToolbarItem` 一次性保存。
  - 保留分割线拖动、Enter 或空格切换区域和左右方向键排序；中英文提示明确左键拖动可调整显示、隐藏和顺序。
  - 补充三档行高、跨区域拖拽、精确插入、非左键忽略和键盘操作回归测试。
- 涉及文件：`src/components/editor/EditorLayout.tsx`、`src/components/settings/ToolbarLayoutEditor.tsx`、`src/styles/components/settings-panel.css`、`src/styles/editor.css`、`src/i18n/locales/en.ts`、`src/i18n/locales/zh-CN.ts`、`tests/editor.interactions.test.tsx`、`tests/toolbar.layout-editor.test.tsx`、`.project-memory/memory/2026-07-29.md`。
- 验证：
  - `npm test -- --run tests/toolbar.layout-editor.test.tsx tests/toolbar.settings.test.ts tests/editor.interactions.test.tsx`：3 个测试文件、29 个测试全部通过。
  - `npm test`：36 个测试文件、247 个测试全部通过。
  - `npm run build`：通过，共转换 2957 个模块。
  - `git diff --check`：通过；活跃代码文件没有超过 800 行。
  - 生产预览实际核验：三档正文行高变量分别为 1.2、1.5、2，对应 16px 正文的计算行高为 19.2px、24px、32px；按住鼠标左键将“演示模式”从隐藏区拖至“粗体”之后，设置区顺序、显示数量和实际编辑器工具栏均同步更新，随后已恢复默认布局；浏览器控制台无错误。
- 已知限制：未新增拖拽时自动滚动设置面板；鼠标在两个布局区域之外松开会取消本次移动，不改变当前配置。
- Git 提交信息：`fix: 调整正文行高与工具栏拖拽`。

## 限定编辑器字体作用范围

- 需求：设置中的编辑器字体和字号只影响文档内容视图，不再作用于关于页、文件树、设置页和右键菜单等软件界面；Markdown 视图继续支持分别跟随编辑器字体、字号或单独设置。
- 实现：
  - 移除 `App` 向 `document.documentElement` 写入编辑器和 Markdown 字体变量的全局通道，BlockNote 正文仅消费编辑器实例上的 `--fkemark-content-font-*` 局部变量，分栏预览继续使用局部内联字体。
  - 删除 `body.read-mode` 和 `.ProseMirror` 对根级字体变量的覆盖，普通 `.editor-inner` 回退为应用正文设计字体，避免编辑器设置扩散到软件界面和 Portal 菜单。
  - 演示模式显式接收编辑器字体族，同时保留演示视图的响应式字号；源码模式继续使用固定等宽字体和 14px 字号。
  - 更新中英文视图设置说明，明确 Markdown 字体和字号同时作用于阅读模式与分栏预览，并补充跟随、独立及作用域回归测试。
- 涉及文件：`src/App.tsx`、`src/components/editor/EditorLayout.tsx`、`src/components/editor/PresentationMode.tsx`、`src/components/settings/SettingsViewSection.tsx`、`src/styles/editor.css`、`src/types/index.ts`、`src/i18n/locales/en.ts`、`src/i18n/locales/zh-CN.ts`、`tests/editor.interactions.test.tsx`、`tests/presentation.mode.test.tsx`、`tests/editor.typography-scope.test.ts`、`.project-memory/memory/2026-07-29.md`。
- 验证：
  - 定向 Vitest：3 个测试文件、28 个测试全部通过。
  - `npm test`：37 个测试文件、249 个测试全部通过。
  - `npm run build`、`npx tsc --noEmit --pretty false`、`git diff --check`：通过。
  - 活跃代码文件没有超过 800 行。
  - 本地 GUI 核验：编辑器设置为 Georgia/24 后，实时和跟随状态的阅读视图使用对应局部字体；`html` 仅保留圆角和编辑宽度变量，关于页保持应用界面字体；源码模式仍为固定等宽小字号。截图工具出现四宫格重复画面，但 DOM 属性与可视内容结论一致。
- 已知限制：浏览器开发环境刷新会清空未保存文档；分栏独立字体的真实界面路径由组件回归测试覆盖。本次未修改设置面板中既有的 `pt` 文案与实际 `px` 单位差异。
- Git 提交信息：`fix: 限定编辑器字体作用范围`。


## 新增 MCP 执行权限对照设置

- 需求：在 AI 设置中增加 MCP 执行权限配置，分为只读、数据读写（推荐）和完全访问，并提供能力明细对照。
- 实现：
  - 新增 `mcpPermissionMode` 前端默认值和 Rust 持久化字段，旧设置缺失时默认 `data-read-write`。
  - 在 `SettingsAiSection` 增加三档权限选择、推荐标识、能力矩阵和约束说明。
  - 补充中英文 i18n 资源与 AI 设置、i18n、Rust 默认值回归测试。
- 涉及文件或模块：`src/types/index.ts`、`src/app/appDefaults.ts`、`src-tauri/src/settings.rs`、`src/components/settings/SettingsAiSection.tsx`、`src/styles/components/settings-panel.css`、`src/i18n/locales/ai.ts`、`tests/ai.settings.test.tsx`、`tests/core.test.ts`。
- 验证结果：
  - `npm test -- --run tests/ai.settings.test.tsx tests/core.test.ts`：通过，2 个测试文件、38 个测试。
  - `cargo test --manifest-path src-tauri/Cargo.toml settings::tests`：通过，8 个测试。
  - `npm test`：通过，37 个测试文件、250 个测试。
  - `npm run build`：通过。
  - `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
  - `cargo test --manifest-path src-tauri/Cargo.toml`：通过，22 个测试。
  - `git diff --check`：通过。
  - 活跃代码文件行数检查：通过，未发现超过 800 行的代码文件。
  - 本地页面核验：通过，在 AI 设置中确认三档权限、推荐标识和 5 行能力矩阵可见。
- 已知限制或未处理的无关问题：本次新增的是 MCP 权限配置与对照展示，不包含真实 MCP Server 连接管理或执行拦截链；未处理无关问题。
- Git 提交信息：`feat: 新增 MCP 执行权限设置`


## 补充 MCP 配置案例与文档

- 需求：在 MCP 设置底部增加几种配置案例，并在文档站补充 MCP 执行权限内容。
- 实现：
  - 在 AI 设置的 MCP 权限区块底部增加只读、数据读写（推荐）和完全访问三类配置案例，分别说明场景、配置和保护建议。
  - 补充中英文 MCP 配置案例 i18n 资源，并更新 AI 设置回归测试与 i18n 完整性测试。
  - 在中英文设置文档中增加 MCP 执行权限说明、权限矩阵和三类配置案例。
- 涉及文件或模块：`src/components/settings/SettingsAiSection.tsx`、`src/styles/components/settings-panel.css`、`src/i18n/locales/ai.ts`、`tests/ai.settings.test.tsx`、`tests/core.test.ts`、`doc/guide/settings.md`、`doc/en/guide/settings.md`。
- 验证结果：
  - `npm test -- --run tests/ai.settings.test.tsx tests/core.test.ts`：通过，2 个测试文件、38 个测试。
  - `npm run build`：通过。
  - `npm --prefix doc run build`：通过；仅保留 VitePress 主题 iconfont 资源运行时解析提示。
  - 本地页面核验：通过，AI 设置中确认 3 个 MCP 配置案例均可见。
  - `npm test`：通过，37 个测试文件、250 个测试。
  - `git diff --check`：通过，仅有 LF 转 CRLF 提示。
  - 活跃代码文件行数检查：通过，未发现超过 800 行的代码文件。
- 已知限制或未处理的无关问题：本次补充的是 MCP 权限说明、配置案例和文档，不新增真实 MCP 执行拦截或连接管理逻辑；全量测试仍有既有的 Vite CJS 弃用提示、updater 404 预期日志、jsdom `window.scrollBy` 未实现提示和编辑器性能日志。
- Git 提交信息：`feat: 补充 MCP 权限配置案例`
