# 2026-07-16 工作日志

## 第十轮：小地图悬浮预览优化

### 需求
1. 小地图在左边时，悬浮预览框显示在右边；小地图在右边时，悬浮显示在左边。
2. 悬浮框样式调整为带小尖尖指向地图位置（类似消息气泡），内容区域渲染 markdown。
3. 悬浮预览根据视图模式显示内容：live/read 模式显示渲染后的 HTML，source 模式显示源码。

### 实现
**`src/components/Editor.tsx`** Minimap 组件重写：
- 新增 `side: 'left'|'right'` 和 `editorMode` props。
- `<Minimap>` 调用处传入 `side={minimapOnLeft ? 'left' : 'right'}` 和 `editorMode`。
- 悬浮预览改为提取以鼠标对应行为中心的 5 行片段：
  - `editorMode === 'source'`：用 escapeHtml 转义后包 `<pre class="minimap-tip-pre">` 显示源码。
  - `live`/`read` 模式：调 `markdownToHtml(fragment)` 渲染为 HTML，用 `dangerouslySetInnerHTML` 注入。
- tooltip 只存 `{ html, y }`，水平位置由 CSS class 决定（不再存 x 坐标）。
- 新增 `.minimap-tooltip-arrow` 元素作为小尖尖。
- 复用文件末尾已有的 `escapeHtml` 函数（删除重复定义）。

**`src/index.css`** tooltip 样式重写：
- `.minimap-tooltip` 改为固定 width 280px + `transform: translateY(-50%)` 垂直居中。
- `.minimap-tooltip-left` → `left: calc(100% + 14px)`（显示在右边）。
- `.minimap-tooltip-right` → `right: calc(100% + 14px)`（显示在左边）。
- `.minimap-tooltip-arrow` 三角形 + `::after` 内部白色覆盖留 1px 边框，左/右方向分别处理。
- `.minimap-tooltip-content` 完整 markdown 元素样式（h1-h4/ul/ol/code/pre/blockquote/table/hr/a/img）。
- `.minimap-tip-pre` 源码模式专用样式。
- 动画 keyframes 改为 `translateY(-50%)` 保持居中。

### 构建验证（第十轮）
- tsc 0 错误（修复 escapeHtml 重复定义后）、vite build 通过（CSS 46.50KB / JS 728.86KB gzip 228KB）。tauri build 后台打包中（任务 D2mM5W）。

---

## 第十轮修复：悬浮窗不显示（overflow:hidden 裁切问题）

### 问题
用户反馈"没有看见悬浮窗显示出来"。

### 根因
`.minimap-panel` 有 `overflow: hidden`（为了让长小地图内容不溢出 panel），但 tooltip 是 panel 的子元素，`position: absolute` 定位到 panel 外部时会被 hidden 裁切掉，导致不可见。

### 修复（React Portal 方案）
**`src/components/Editor.tsx`**：
- 新增 `import { createPortal } from 'react-dom'`（注意：createPortal 不在 react 命名导出，必须从 react-dom 导入）。
- Minimap hover state 改为 `{ html, y, left }`（left 改为视口绝对坐标）。
- handleMouseMove 计算 tooltip 的视口坐标：`side==='left'` 时 `left = rect.right + 14`，`side==='right'` 时 `left = rect.left - 14 - 280`。
- tooltip 渲染改用 `createPortal(<div>...</div>, document.body)`，脱离 panel 的 overflow 限制。
- tooltip 的 `top`/`left` 直接用视口坐标（配合 fixed 定位）。

**`src/index.css`**：
- `.minimap-tooltip` 从 `position: absolute` 改为 `position: fixed`，`z-index` 1000。
- 删除 `.minimap-tooltip-left { left: calc(100% + 14px) }` / `.minimap-tooltip-right { right: calc(100% + 14px) }`（位置改由 JS 设定）。
- 保留 `.minimap-tooltip-arrow` 左右方向的小尖尖 border 规则。
- 清理重复的 `.minimap-tooltip-left/right .minimap-tooltip-arrow` 定义。

### 关键经验
- 父元素 `overflow: hidden` 会裁切 `position: absolute` 的子元素。解决方案：用 React Portal 把 tooltip 渲染到 `document.body`，配合 `position: fixed` + 视口坐标，彻底脱离父元素 overflow 限制。
- `createPortal` 必须从 `react-dom` 导入，不是 `react` 的命名导出（TS 报 TS2724）。

### 构建验证（第十轮修复）
- tsc 0 错误、vite build 通过（CSS 46.41KB / JS 728.90KB gzip 228KB）。tauri build 后台打包中（任务 BL1bIU）。

---

## 第十轮修复 2：表格右键菜单不生效

### 问题
用户反馈"表格右键菜单没有效果"。

### 根因（双重问题）
1. **close useEffect 监听了 contextmenu 事件**：用户右键表格时，contextmenu 事件冒泡到 document → close 监听器立即 `setTableCtxMenu(null)` → 菜单刚弹出就被关闭，用户根本看不到。
2. **菜单容器未阻止 contextmenu 冒泡**：在菜单内右键会再次触发 contextmenu → 关闭菜单。

### 修复
1. `onScrollContextMenu` 加 `e.nativeEvent.stopImmediatePropagation()` 阻止 contextmenu 事件冒泡到 document。
2. close useEffect **移除 contextmenu 监听**（只保留 click 关闭）。
3. 表格菜单容器加 `onContextMenu={(e) => { e.preventDefault(); e.stopPropagation() }}` 防止菜单内右键触发关闭。

### 构建验证
- tsc 0 错误、vite build 通过（CSS 46.41KB / JS 728.91KB gzip 228KB）。tauri build 后台打包中（任务 NIoqy7）。

---

## 第十一轮：11 项编辑器改进的隐藏 bug 审查与修复

### 审查范围
遍历第九轮 11 项功能代码（列表符号 / 代码块语言选择器 / 斜杠菜单定位 / 小地图悬浮 /
表格右键菜单 / 工具栏悬浮 / 边角柔化 / tab圆角 / 模式切换文案 / 主题图标式 / 删迷你侧栏）。

### 发现的 bug 与修复
1. **[关键] 代码块语言输入框每输入一字符就失焦**：`onChange` 内 `editor?.chain().focus().updateAttributes(...)`
   会在每次按键时把焦点抢回 ProseMirror 编辑器，导致输入框只能输入一个字符。
   修复：改为 `editor?.commands.updateAttributes('codeBlock', { language: lang })`（去掉 `.focus()`）。
2. **[体验] 小地图悬浮框溢出视口**：`left`/`top` 未做边界钳制，屏幕底部/窄窗时悬浮框不可见。
   修复：水平 `Math.max(8, Math.min(rawLeft, innerWidth-280-8))`，垂直 `Math.max(80, Math.min(e.clientY, innerHeight-80))`。
3. **[体验] 悬浮工具栏压住首行**：`.editor-scroll` 顶部内边距 40px < 工具栏浮层(10+34px)，
   页面最顶部时首行被压 4px。修复：padding-top 40px → 56px。

### 确认的其它问题（未改，低优先级）
- 小地图 mousedown 拖出 panel 外部释放后 draggingRef 未复位（边缘情况）。

### 构建验证
- tsc 0 错误、vite build 通过（CSS 46.41KB / JS 729KB gzip 228KB）。tauri build 后台打包中（任务 qv9V9M）。

---

## 第十二轮：3 项遗留问题修复

### 1. 右键菜单屏幕右下角溢出（无边界钳制）
**`src/components/Editor.tsx`**：
- 新增 `clampMenuPos(x, y, estW, estH)` 工具函数：水平 `pad ~ innerWidth-estW-pad`，垂直 `pad ~ innerHeight-estH-pad`，Math.min/max 钳制。
- `onScrollContextMenu` 中 `setTableCtxMenu` 与 `setContextMenuPos` 均经 `clampMenuPos` 处理（表格菜单 estH=300，通用菜单 estH=180）。

### 2. TopBar 切换主题忽略 system 选项
**`src/App.tsx`**：
- `handleToggleTheme` 从 `isDark ? 'light' : 'dark'`（仅二态）改为三态循环：`light → dark → system → light`。
**`src/components/TopBar.tsx`**：
- props `isDark: boolean` → `theme: 'light'|'dark'|'system'`，菜单项图标按 theme 显示太阳/月亮/电脑，右侧 `menu-shortcut` 显示当前模式（明亮/黑暗/系统）。
- App.tsx 渲染处 `isDark={isDark}` → `theme={settings.theme}`。

### 3. 新建文件时编辑器内容区域宽度不按设置配置
**根因**：`applySettings` 把 maxWidth 直接设在 `.ProseMirror` 上，但父容器 `.editor-inner`（及 `.source-textarea`）用 `max-width: var(--editor-max-w)`（默认 800px）截断，导致 editorWidth 设置（680/800/960）始终被 800px 上限卡住、不生效；新建文件（从欢迎页挂载）时尤为明显。
**修复**：`applySettings` 不再设 `.ProseMirror` 内联 maxWidth，改为 `document.documentElement.style.setProperty('--editor-max-w', ewMap[editorWidth])`，父容器 `.editor-inner` 与 `.source-textarea` 均引用该变量，宽度设置对所有文件（含新建）生效。

### 构建验证
- tsc 0 错误、vite build 通过（CSS 46.41KB / JS 729.58KB gzip 228KB）。tauri build 后台打包中（任务 rA9204）。


---

## 第十四轮续：字体切换 + 多语言 i18n（用户要求"增加字体切换读取本机字体" + "设置增加多语言"）

### 字体切换（task #67，完成）
- `src/utils/fonts.ts`（新建，上轮）：`getAvailableFonts()` 用 canvas measureText 检测本机可用字体（中文/英文/代码分组，含 available 标记），`DEFAULT_FONT_FAMILY='system-ui'`。
- `src/types/index.ts`：AppSettings 加 `fontFamily: string`。
- `src/App.tsx`：DEFAULT_SETTINGS 加 `fontFamily: 'system-ui'`。
- `src/components/Editor.tsx`：`applySettings` 已写 `document.documentElement.style.setProperty('--font-sans', settings.fontFamily)`，全局 `--font-sans` 驱动正文/菜单/编辑区字体。
- `src/components/SettingsPanel.tsx`：编辑器分组新增字体下拉（原生 `<select>` + `<optgroup>` 分组，不可用项标"不可用"），`onChange` 调 `update({fontFamily})`；新增 `.settings-select` 样式。

### 多语言 i18n（task #68，完成）
- `src/i18n/locales.ts`（新建）：扁平 key 字典，zh-CN（默认）+ en 两套，支持 `{name}` 插值。含设置/快捷键/标题栏/状态栏/欢迎页/侧边栏/关于页/编辑器菜单/斜杠命令等全部 UI 文案。
- `src/i18n/index.tsx`（新建）：`I18nProvider`（受控 `language`/`setLanguage`）+ `useI18n()` hook + 纯函数 `translate(lang, key, params)`；键缺失时回退到 key 本身。
- `src/types/index.ts`：AppSettings 加 `language: 'zh-CN' | 'en'`。
- `src-tauri/src/settings.rs`：AppSettings 加 `pub language: String`，默认 `"zh-CN"`，serde camelCase 自动映射。
- `src/App.tsx`：包 `<I18nProvider language={settings.language} setLanguage={(l)=>handleSettingsChange({...settings, language:l})}>`；状态栏用 `translate()` 纯函数；`<html lang>` 随语言更新。
- 翻译接入：SettingsPanel、TopBar、WelcomeScreen、Sidebar（含相对时间）、AboutPage、Editor（右键/表格/链接弹窗/代码语言占位）、SlashMenu（命令+分组+提示）均用 `useI18n()`。
- SettingsPanel 新增"语言"分组，中文/English 分段选择，经 `setLanguage` 写回设置并持久化到 settings.json。

### 构建验证（第十四轮续）
- tsc 0 错误（修掉 SettingsPanel 未用导入 DEFAULT_FONT_FAMILY）、vite build 通过（CSS 46.94KB / JS 747KB gzip 233KB）、cargo check 通过（仅预存 dead_code 警告）。tauri build 后台打包中。

---

## 第十五轮：字体列表动态化 + i18n 文件拆分（用户反馈）

### 1) 字体列表改为动态读取本机字体（task #74/#75）
- 问题：原 fonts.ts 写死 CANDIDATE_FONTS 候选列表 + canvas 检测，并非真实本机字体枚举。
- 方案：WebView 无法枚举系统字体，改用 Tauri Rust 命令。
  - Cargo.toml 新增 font-kit = "0.11"。
  - main.rs 新增 get_system_fonts 命令（font_kit::source::SystemSource.all_families()），已注册 invoke_handler。
  - fonts.ts 重写：fetchSystemFontFamilies()（invoke 调 get_system_fonts，非 Tauri/失败回退 FALLBACK_FONTS）+ getAvailableFonts()（异步、去重排序、classify() 启发式分 default/cjk/latin/mono 组）。
  - SettingsPanel：字体 useEffect 异步加载（state fonts）+ 分组 optgroup + 翻译组名（font.group.*）+ 显示数量/加载中；移除 unavailable 标记渲染。

### 2) i18n 字典按语言拆文件（task #76）
- locales.ts（同时含两套）→ 拆为 types.ts + zh-CN.ts + en.ts + index.ts（聚合 LANG_LABELS/DICTS，re-export Lang/Dict）；删除 locales.ts。index.tsx/SettingsPanel 导入路径 ../i18n/locales 自动命中 index.ts。

### 验证
- tsc --noEmit 0 错误；vite build 通过。cargo check（含 font-kit 下载编译）后台 ruC2yL 进行中。

---

## 第十六轮：3 项修复（主题i18n/新建文件宽度/列表嵌套样式）+ 打包修复

### 1) 头部菜单主题项显示字典 key（task 修复）
- TopBar.tsx 主题菜单 shortcut 用了 `t('topbar.theme.light/dark/system')`，但字典里只有 `settings.theme.*`，key 缺失 → 回退显示原始 key。
- 改为复用 `settings.theme.light/dark/system`。

### 2) 新建文件编辑器宽度只有一点点宽
- 根因：`.editor-scroll` 是 flex+justify-center，EditorContent 渲染一个 `width:auto` 的包裹 div 作为 flex item，空文档时收缩到 max-content 变很窄；`.editor-inner` 的 width:100%/max-width 无法撑开包裹层。
- 修复：新增 `.editor-scroll > div { width:100%; max-width: var(--editor-max-w) }` 让包裹层占满允许宽度。

### 3) 列表嵌套标记不变 + 工具栏有序列表样式下拉
- CSS：ul/ol 按嵌套层级变化（ul: disc→circle→square；ol: decimal→lower-alpha→lower-roman→upper-alpha→upper-roman）。
- 自定义 StyledOrderedList（extend OrderedList，加 listStyle 属性渲染为 data-ls）；StarterKit 关闭 orderedList，改用 StyledOrderedList。
- 工具栏有序列表改为分体按钮：主按钮快速切换、▾ 打开样式选择器（decimal/lower-alpha/upper-alpha/lower-roman/upper-roman），applyOlStyle 未在 OL 中先 toggle 再 updateAttributes。
- 新增 i18n key toolbar.ol / toolbar.olStyle.*；新增 .ol-style-picker CSS。
- package.json 新增直接依赖 @tiptap/extension-ordered-list（2.27.2，原为 starter-kit 传递依赖）。

### 打包修复
- tauri build 从项目根跑会因 CLI 监视路径相对 cwd 解析 `../dist` 失败（watch path 报错）→ 改从 src-tauri 目录跑。
- msi 打包 rename fke-mark.exe→FkeMark.exe 失败（os error 2）→ Cargo.toml 加 `[[bin]] name="FkeMark"` 让产物名与 productName 一致，跳过 rename。

### 验证
- tsc 0 错误；vite build 通过（CSS 48.35KB / JS 749KB gzip 234KB）。tauri build（KHtWSL）后台进行中。
- 用户要求：不提交 git，只给 commit comment 信息；打包好供测试。

---

## 第十六轮：修复切换视图模式表格塌成 `|  |`

### 现象
切到实时/阅读模式后，表格源码变成一行行 `|  |`；再切回去已被永久改写成 `|  |`。

### 根因（Editor.tsx `tableToMd`）
TipTap 的 Table 扩展输出的 HTML **没有 `<thead>`**，表头单元格是 `<th>` 直接放在 `<tbody>` 的 `<tr>` 里。而旧 `tableToMd`：
- 只从 `<thead>` 取表头（thead 为 null → 表头丢失）
- `tbody.querySelectorAll('td')` 只匹配 `td`，表头行的 `<th>` 被跳过 → push `[]`
- rows[0]=[] → colCount=0 → `'| ' + [].join(' | ') + ' |'` = `'|  |'`，整表塌成 `|  |`

触发链：切到 live → `setContent(markdownToHtml(content))` → TipTap 渲染 → `onUpdate` → `htmlToMarkdown(editor.getHTML())` → tableToMd 丢 th → onChange 把内容改写成 `|  |`。

### 修复
重写 `tableToMd`：
- 按 thead（若有）→ tbody（若有）→ 直接取 tr 的顺序收集行
- 每行同时收集 `th` 与 `td`（仅取直接子元素，避免嵌套表格干扰）
- colCount 取所有行最大列数，按最大列数补齐每行
- 第一行作表头，对齐默认 `:---`，支持读 `align`/`style.textAlign`

### 验证
- tsc 0 错误；vite build 通过。
- 写最小 DOM mock 测试三种结构：TipTap 结构（无 thead/th 在 tbody）、标准结构（有 thead）、空单元格，全部正确输出。
- 打包（35pjIk）后台进行中；不提交 git，只给 commit comment，打包供测试。

## 第十七轮：五大新功能实现

### 1. 图片右键上下文菜单
**新增文件**：`src/components/plugins/ResizableImage.ts`
- 自定义 Image 扩展，支持 `width`/`height`/`widthUnit`/`heightUnit` 四个属性
- 像素值（如 300）渲染为 `style="width:300px"`，百分比（如 50）渲染为 `style="width:50%"`
- 新增 `updateImageSize` 命令，通过 ProseMirror 事务修改图片尺寸
- `parseInlineMd` 支持解析 `<!-- size:300pxx200px -->` 注释还原尺寸

**修改文件**：`src/components/Editor.tsx`
- 替换 `Image` 为 `ResizableImage`
- `onScrollContextMenu` 中检测图片右键，弹出图片操作菜单（调整尺寸/重置/50%宽度/100%宽度/删除）
- 新增 `imageCtxMenu` 和 `imageSizeDialog` 状态
- 图片尺寸弹窗支持宽高输入 + 像素/百分比切换 + 实时预览
- `htmlToMarkdown` 中图片节点保留尺寸信息（`<!-- size:WxH -->` HTML 注释）
- `markdownToHtml` 中 `parseInlineMd` 解析尺寸注释转为 style

### 2. 图片在列表中的排版
**修改文件**：`src/index.css`
- `.editor-inner li img` 设置 `max-width: calc(100% - 24px)` 确保列表缩进后图片不溢出
- 列表首尾段内图片的 margin 归零，保持视觉一致性

### 3. 专注模式 + 打字机模式
**修改文件**：
- `src/types/index.ts`：AppSettings 新增 `focusMode` 和 `typewriterMode` 字段
- `src/App.tsx`：
  - DEFAULT_SETTINGS 新增两个布尔字段
  - 新增专注模式/打字机模式 body class 切换 useEffect
  - 打字机模式通过 `selectionchange` 事件监听光标位置，自动滚动视口使光标居中
  - F11 切换专注模式，Ctrl+Shift+T 切换打字机模式
- `src/components/SettingsPanel.tsx`：视图组新增两个开关
- `src/index.css`：
  - `.focus-mode` 隐藏 titlebar/sidebar/statusbar/toolbar
  - `.typewriter-mode` 编辑区 min-height + padding-bottom: 50vh
- `src-tauri/src/settings.rs`：新增 `focus_mode`/`typewriter_mode` 字段

### 4. 导入导出系统
**新增文件**：`src/utils/importExport.ts`
- 支持导出 MD/HTML/TXT 三种格式
  - MD：原样输出
  - HTML：生成完整 HTML 文档（含样式）
  - TXT：去除 Markdown 标记
- 支持导入 MD/HTML/TXT/HTM 文件
  - 格式校验（`validateImportFile`）：检查扩展名和空文件
  - HTML→Markdown 简易转换（`htmlToMarkdownSimple`）：基于 DOM 解析
  - 导入冲突处理：未保存修改时确认覆盖
  - Tauri 环境使用系统对话框，浏览器环境使用 input/download
- TopBar 菜单新增「导入」「导出」按钮
- 导出格式选择器弹窗

**修改文件**：
- `src/components/TopBar.tsx`：新增 `onExport`/`onImport` props 和菜单项
- `src/App.tsx`：新增 `handleExport`/`handleImport` 函数和 `exportFormatPicker` 状态

### 5. 性能优化与测试
**新增文件**：
- `src/utils/performance.ts`：防抖/节流/分片解析/内存泄漏检测/性能标记工具
- `tests/core.test.ts`：Vitest 自动化测试用例（18 个测试）
  - HTML→Markdown 转换测试
  - 导入导出格式校验测试
  - 性能工具（debounce/throttle/splitLargeDocument/isLargeDocument）测试

**修改文件**：
- `package.json`：新增 `vitest`/`jsdom` 依赖和 `test`/`test:watch` 脚本
- `vitest.config.ts`：Vitest 配置文件
- `src/components/Editor.tsx`：
  - `onUpdate` 回调中对大文档（>100KB）使用 100ms 防抖更新
  - 导入 `debounce`/`isLargeDocument` 工具函数

### i18n 新增键值
`zh-CN.ts` / `en.ts`：
- 图片右键菜单：`image.resize`/`resetSize`/`halfWidth`/`fullWidth`/`delete`/`resizeTitle`/`width`/`height`/`auto`
- 专注/打字机模式：`focusMode.label`/`hint`/`typewriterMode.label`/`hint`
- 导入导出：`export.*`/`import.*` 系列

---

## 第十八轮：移除打字机模式 + 前端目录结构重构

### 1. 移除打字机模式功能（task #1）
**修改文件**：
- `src/App.tsx`：
  - 删除 `typewriterMode` 相关 state、useEffect（selectionchange 监听 + 光标居中滚动）、快捷键 Ctrl+Shift+T 处理
  - 删除 body class `typewriter-mode` 切换逻辑
- `src/components/SettingsPanel.tsx`：删除打字机模式开关 UI
- `src/types/index.ts`：删除 `typewriterMode: boolean` 字段
- `src-tauri/src/settings.rs`：删除 `typewriter_mode` 字段
- `src/index.css`：删除 `.typewriter-mode` 样式规则
- `src/i18n/locales/zh-CN.ts` / `en.ts`：删除 `typewriterMode.*` 键值

> **保留**：专注模式（F11）不受影响，继续正常工作。

### 2. CSS 模块化拆分（task #2）
将单体 `src/index.css`（原 ~1700 行）拆分为 13 个模块化文件，存放在 `src/styles/` 目录：

| 文件 | 职责 |
|------|------|
| `variables.css` | CSS 变量系统（颜色/间距/字体/圆角/z-index）+ light/dark 双主题 |
| `layout.css` | App 容器、主布局 flex/grid |
| `titlebar.css` | 标题栏、窗口控制按钮、应用菜单 |
| `sidebar.css` | 侧边栏、文件夹树、大纲 TOC、标签页 Tabs |
| `editor.css` | 编辑器区域、欢迎屏、工具栏、Markdown 渲染基础样式 |
| `statusbar.css` | 状态栏、状态按钮 |
| `components.css` | 设置面板、Toggle/Radio/Select 组件 |
| `overlays.css` | 阅读/源码/专注模式覆盖层、小地图 |
| `forms.css` | Range Slider、Number Input 表单控件 |
| `menus.css` | 斜杠命令菜单、链接对话框、图片右键菜单 |
| `markdown.css` | Markdown 扩展：任务列表、表格、代码高亮 |
| `about.css` | 关于页面 |
| `misc.css` | 全局滚动条、圆角工具类 |

**入口文件 `src/index.css`** 重写为纯导入文件，所有 `@import` 在顶部（CSS 规范要求），`@tailwind` 在底部。

### 3. Editor.tsx 组件拆分（task #3）
将超大组件 `src/components/Editor.tsx`（原 1865 行）按职责拆分为多个子组件和工具函数文件：

| 新文件 | 职责 | 行数 |
|--------|------|------|
| `src/components/Minimap.tsx` | 小地图组件（Portal 悬浮预览） | ~200 |
| `src/components/LineNumbers.tsx` | 行号显示组件 | ~60 |
| `src/components/EditorPickers.tsx` | 选择器类：表格选择器、有序列表样式选择器、代码块语言选择器 | ~180 |
| `src/components/EditorMenus.tsx` | 弹窗与菜单类：表格右键菜单、图片右键菜单/尺寸弹窗、链接弹窗 | ~350 |
| `src/utils/markdown.ts` | HTML↔Markdown 转换工具函数（htmlToMarkdown/markdownToHtml/tableToMd 等） | ~400 |

**重构后 `Editor.tsx`**：从 1865 行降至约 1000 行，通过 import 引入子组件。

### 关键技术细节
- **forwardRef + useImperativeHandle**：子组件通过 forwardRef 包裹，暴露方法给父组件调用
- **CSS @import 顺序**：必须放在所有其他语句之前（除 @charset），Tailwind 指令放最后
- **TypeScript 错误修复**：
  - forwardRef 闭合括号遗漏 → 补全
  - OlStylePicker 元组类型 3→2 元素修正
  - 清理未使用的类型导入（ReactKeyboardEvent 等）

### 验证结果
- `tsc --noEmit`：0 错误（Exit Code: 0）
- `vite build`：✅ 通过（CSS 49.79KB），2.64s 完成
- 380 modules transformed，无构建错误

---

## 第十八轮续：构建警告修复 + 目录结构整理

### 1. Vite chunk 过大警告修复
**问题**：主 bundle 769KB 超过 500KB 限制，触发 rollup 警告。
**修复**（`vite.config.ts`）：
- 添加 `build.rollupOptions.output.manualChunks` 代码分割：
  - **`tiptap-vendor`**：所有 @tiptap/* 扩展包（540KB）
  - **`react-vendor`**：react + react-dom
- 添加 `chunkSizeWarningLimit: 600`（TipTap 本身较大，540KB 合理）
- **效果**：主业务代码从 769KB → **229kB**（gzip 67KB）

### 2. Rust dead_code 警告修复
**修改文件**：
- `src-tauri/src/file_system.rs`：`file_exists` 加 `#[allow(dead_code)]`
- `src-tauri/src/markdown.rs`：`parse_to_tiptap` 和 `convert_to_markdown` 加 `#[allow(dead_code)]`
- 这三个函数是预留工具函数，当前未被 command 调用但保留供未来使用

### 3. Editor 子组件目录归并
将散落在 `src/components/` 的 Editor 拆分子组件移入 `src/components/editor/` 子目录：

| 文件 | 原路径 | 新路径 |
|------|--------|--------|
| Minimap.tsx | `components/Minimap.tsx` | `components/editor/Minimap.tsx` |
| LineNumbers.tsx | `components/LineNumbers.tsx` | `components/editor/LineNumbers.tsx` |
| EditorPickers.tsx | `components/EditorPickers.tsx` | `components/editor/EditorPickers.tsx` |
| EditorMenus.tsx | `components/EditorMenus.tsx` | `components/editor/EditorMenus.tsx` |

**路径更新**：
- `Editor.tsx` import 路径从 `'./Minimap'` → `'./editor/Minimap'` 等
- 子组件内部相对路径从 `'../utils'`/`'../i18n'` → `'../../utils'`/`'../../i18n'`

---

## 第十九轮：设置页面重构 + 实验性功能 + 关于页整合

### 1. 新增 Mermaid/Vim 实验性功能标记
**i18n 新增键值**（zh-CN.ts / en.ts）：
- `experimental.title/hint/badge` — 实验性功能通用标签
- `experimental.mermaid/vim` + `.hint` — Mermaid 图表渲染 / Vim 编辑器模式
- `settings.nav.*`（8 个）— 设置导航菜单项
- `settings.back` — 返回编辑器按钮

### 2. 设置页面重构为独立全页面
**重写文件**：`src/components/SettingsPanel.tsx`
- **从右侧滑出面板 → 居中全页面模态框**（960×720px，圆角卡片+阴影）
- **左侧导航栏**（200px）：8 个模块菜单项，各带 SVG 图标
  - 外观 / 编辑器 / 视图 / 行为 / 语言 / 快捷键 / **实验性** / **关于**
- **右侧内容区**：点击导航切换对应内容
- **可折叠分组**（CollapsibleGroup）：
  - 标题栏 + chevron 箭头，点击展开/收起，带动画
- **实验性功能区块**：
  - 黄色警告横幅 + "实验性"徽章
  - Mermaid / Vim 各占折叠组，开关禁用状态（未实现）

### 3. 关于页整合进设置体系
- 原 AboutPage 组件不再使用（文件保留）
- 关于内容作为设置最后一个导航项（about section）
- Logo / 简介 / 版本 / 链接 / 致谢，全部 CollapsibleGroup 包裹

### 4. 移除头部菜单中的设置入口
- TopBar.tsx：移除 onOpenAbout prop + 设置齿轮菜单项；"关于"按钮改为打开设置页
- App.tsx：移除 aboutOpen state、AboutPage 导入和渲染

### 5. CSS 全面重写（components.css）
新增：`.settings-page-*` 全页面布局 / `.settings-nav-*` 左侧导航 / `.settings-group-header/body` 折叠分组 / `.experimental-banner/badge` 实验性标记

---

## 第二十轮：原型图对齐重构（CSS 样式系统全面升级）

### 需求
读取 `femark-optimized.html` 原型图文件，分析其页面布局结构和样式组件划分，根据原型图中的视觉层次和组件关系，重构现有页面的样式组件分布，确保组件拆分与原型图的结构保持一致。

### 原型图核心设计语言
- **暗色优先**设计，暖色调品牌 accent 色 `#c96442`（替代原中性灰 `#4B5563`）
- **衬线 display font**（Charter/Georgia）用于标题和品牌，无衬线 body font 用于正文
- **统一间距系统**：8/12/20/32 四级 gap 变量
- **两级圆角**：8px（元素）+ 12px（面板/卡片）
- **三级阴影**：sm/md/lg + oklch 风格 color-mix
- **统一动画曲线**：cubic-bezier(0.22, 1, 0.36, 1)
- **fg-soft 柔化色**：fg 的低透明度版本用于 hover 背景

### 重构的 CSS 文件（13 个模块全部更新）

#### 1. variables.css — 设计令牌系统重写
| 维度 | 旧值 | 新值（对齐原型） |
|------|------|------------------|
| accent | `#4B5563` 中性灰 | `#c96442` 暖橙红 |
| quote-bar | `#CBD5E0` 灰蓝 | `#c96442` accent |
| radius | 三级 (6/4/8px) | 两级 (8/12px) |
| sidebar-w | 240px | **280px** |
| editor-max-w | 800px | **900px** |
| titlebar-h | 44px | **48px** |
| statusbar-h | 32px | **36px** |
| font-display | 无衬线 SF Pro | **衬线 Charter/Georgia** |
| 字号基准 | 16px | **17px** |
| 动画曲线 | 各处不统一 | 统一 cubic-bezier(0.22,1,0.36,1) |
| 阴影 | 单一 box-shadow | **三级** sm/md/lg |
| 间距 | 无统一变量 | **四级** gap-xs/sm/md/lg |
| hover 背景 | var(--border) | **var(--fg-soft)** 柔化色 |

新增变量：`--accent-soft` / `--fg-soft` / `--shadow-sm/md/lg` / `--gap-*` / `--font-body` / 功能色 success/warning/danger

#### 2. layout.css — 容器层级对齐
- `.editor-area` 新增 `margin: 2px 3px` + `border: 1px solid var(--border)` + `border-radius: 8px`（原型的微间距包裹感）

#### 3. titlebar.css — 标题栏对齐
- 高度 44→**48px**，padding 对齐原型 `0 8px 0 16px`
- 底部圆角 `0 0 6px 6px`
- brand 字体改为 **var(--font-display)** 衬线，18px / -0.02em letter-spacing
- 左右 min-width **200px**
- 按钮 28×28（原 32×32），svg 15px（原 18px）
- 窗口按钮 36×32（对齐原型），close 仅右上圆角
- unsaved-dot 8px + box-shadow 发光效果
- gap 统一：left 16px / center 12px / right 4px

#### 4. sidebar.css — 侧边栏对齐
- 宽度 240→**280px**
- 右侧圆角 `0 8px 8px 0`
- 文件项 padding `8px 20px`（更宽松），gap **12px**，左边框 **3px**
- hover 背景改用 **var(--fg-soft)**
- folder-toggle 16×16 + rotate(90deg) 展开
- file-children 缩进 **20px**
- file-status **6px**
- tab 圆角对齐原型 `radius radius 0 0`

#### 5. editor.css — 编辑器区域对齐
- 字号 16→**17px**，line-height 1.8（不变）
- max-width 800→**900px**
- padding-top 56→**64px**
- welcome-logo 48→**56px**，衬线字体，-0.03em letter-spacing
- welcome-tagline 15→**18px**，max-width 400px
- welcome-btn 更大：padding `14px 28px`，font-size 16px，radius-lg 12px
  - hover: accent 填充 + 反色文字 + translateY(-2px) + shadow-md
- h1 衬线字体，36px / h2 28px / h3 22px（全部增大）
- blockquote: 4px accent 色左边框（原 3px quote-bar）
- code 内联：15px 字号，fg-soft 背景
- pre 代码块：surface 背景 + border + radius-lg 12px
- img radius-lg 12px，margin 20px
- toolbar: surface 背景，padding `10px 20px`，gap 8px

#### 6. statusbar.css — 状态栏对齐
- 高度 32→**36px**，surface 背景，顶部圆角 `6px 6px 0 0`
- padding-left **20px**
- status-dot **8px** + saved 发光效果
- **新增** `.view-mode-group` 胶囊式切换组（bg 底色 + radius + 2px padding）
  - `.view-mode-btn` 11px / 500 weight / surface 激活态
- **重写** `.settings-gear-btn`：带边框样式（border + radius + accent hover）
  - 替代原来的无边框 icon-only 样式
- word-count: tabular-nums 等宽数字

#### 7. components.css — 设置面板双模式
- **保留全页面模式**（960×720 居中模态框）用于实验性功能等场景
- **新增侧滑式模式**（对齐原型）：
  - `.settings-overlay` fixed + opacity 过渡
  - `.settings-panel` **480px** 宽，right:0，translateX 滑入，-8px 0 32px 左阴影
  - `.settings-header` 16px 24px padding，衬线字体 h2
  - `.settings-body` 24px padding
  - toggle 44×24（原 40×22），完全圆角 12px，accent 激活色
  - radio group bg 底色，shadow-sm active 态
  - select 10px 12px padding

#### 8. overlays.css / misc.css 微调
- shadow 引用统一用变量
- scrollbar 6px（minimap-thumb 8px 对齐原型）
- 圆角统一为 radius/radius-lg 两级

### 关键差异对照表
| 组件 | 原型特征 | 重构结果 |
|------|----------|----------|
| 整体风格 | 暖色调暗色系 / 衬线标题 | ✅ accent #c96442 / font-display 衬线 |
| 标题栏 | 48px / 三段200px / 28px按钮 | ✅ 完全对齐 |
| 侧边栏 | 280px / surface底 / 右圆角 | ✅ 完全对齐 |
| 编辑器 | 17px / 900px / 64px pt / 衬线h1 | ✅ 完全对齐 |
| 欢迎屏 | 56px logo / 18px tagline / 大按钮 | ✅ 完全对齐 |
| 状态栏 | 36px / 视图组 / 齿轮带边框 | ✅ 完全对齐 |
| 设置面板 | 480px 侧滑 / 分组列表 | ✅ 新增侧滑模式（保留全页） |
| 交互反馈 | fg-soft hover / 缩放 / 上浮 | ✅ 统一应用 |

### 构建验证
- tsc --noEmit：**0 错误**
- vite build：**通过**（379 modules, CSS 56.61KB / JS 233KB+540KB gzip 68KB+173KB）
- 修复 1 个 CSS 语法警告（`::after` 双冒号伪元素）

### Commit 建议
```
feat(ui): 原型图对齐重构 — 设计令牌系统全面升级

对齐 femark-optimized.html 原型图的视觉层次和组件关系：
- 设计令牌：暖橙 accent #c96442 / 衬线字体 / 两级圆角 / 三级阴影 / 四级间距
- 容器尺寸：sidebar 280px / titlebar 48px / statusbar 36px / editor 900px/17px
- 交互细节：fg-soft hover / 视图模式胶囊组 / 设置齿轮带边框 / 侧滑设置面板480px
- 13个 CSS 模块全部更新，构建 0 错误 0 警告
```

---

## 第二十一轮：UI 布局优化调整（状态栏/顶部栏/设置页）

### 需求
5 项 UI 优化调整，精确对齐原型图布局规范。

### 变更详情

#### 1. 状态栏重构（`src/App.tsx` + `src/styles/statusbar.css`）
**左侧（从左到右）**：
- ● 保存状态指示器（saved/saving/unsaved + 文字标签）
- `Markdown` 格式标签（新增 `.statusbar-format`：小字号/等宽/大写/border）
- 字数统计（新增 `.word-count`：等宽数字 + tabular-nums）

**右侧（从左到右）**：
- 视图模式胶囊切换组（编辑/预览/分屏，3 个 `.view-mode-btn`）
- 光标位置（第 X 行, 第 Y 列）
- 编码标识 UTF-8（新增 `.statusbar-encoding`：等宽/11px）
- 设置齿轮按钮 ⚙️（使用 CSS 类样式替代内联 style）

#### 2. 顶部栏右上角重构（`src/components/TopBar.tsx` 完全重写）
- **菜单按钮图标**：从汉堡 ☰ 改为下拉箭头 ▾（polyline）
- **下拉菜单精简为 4 项**：
  1. 💾 保存（Ctrl+S）
  2. ⬇ 导出/下载
  3. 🌓 主题切换图标（根据当前主题动态显示 sun/moon/monitor）
  4. ℹ️ 关于 → 打开设置页并导航到"关于" section
- **移除项**：视图切换、新建文件、打开文件夹、导入（这些功能保留在其他入口）
- **Props 精简**：移除 onToggleSidebar/onNewFile/onOpenFolder/onCycleMode/onImport
- **新增 prop**：onSave / onOpenSettings(section?) 支持 section 参数

#### 3. 移除侧边栏折叠按钮
- 删除 TopBar 中 `sidebar-toggle` 按钮（整个 JSX 块和 SVG）
- 删除 App.tsx 中 `handleToggleSidebar` 函数
- sidebarOpen/sidebarCollapsed state 保留（仍被布局使用），setter 加 `_` 前缀

#### 4. 设置页面视觉优化（`src/styles/components.css`）
**字体清晰度增强**：
- 导航项 font-weight: 500（默认）/ 600（激活态）
- 设置行 label 14.5px + font-weight: 500 + antialiased
- 内容区标题 20→22px
- 分组标题 13→14px + font-weight: 600
- 全局 `-webkit-font-smoothing: antialiasing`

**图标明暗模式适配**：
- 所有 nav-icon SVG 使用 `stroke: currentColor` 自动继承父元素颜色
- 注释明确标注 currentColor 语义

**卡片式分组升级**：
- margin-bottom: 4→12px（增加间距）
- border-radius: radius-btn→radius-card（更大圆角）
- 新增 `background: var(--surface)` 卡片底色
- 新增 `box-shadow: 0 1px 2px rgba(0,0,0,0.04)` 轻微阴影
- header 背景 transparent→透明（让卡片底色统一）
- padding: 11px 14px→13px 16px（增加呼吸感）

#### 5. 关于页整合进设置（`src/App.tsx` + `src/components/SettingsPanel.tsx`）
- App.tsx 新增 `activeSettingsSection` state
- `onOpenSettings` 升级为 `(section?: string) => void`
- SettingsPanel 新增 `initialSection` prop
- useEffect 监听 initialSection 变化自动导航到指定 section
- 点击顶部栏"关于"→ `onOpenSettings('about')` → 设置页直接显示关于内容

### i18n 新增键值
| 键 | zh-CN | en |
|---|---|---|
| status.wordCount | 字数: {n} | Words: {n} |
| topbar.save | 保存 | Save |

### 构建验证
- tsc --noEmit：**0 错误**
- vite build：**通过**（379 modules, CSS 57.21KB / JS 229KB+540KB gzip 68KB+173KB）

### Commit 建议
```
feat(ui): UI 布局优化 — 状态栏/顶部栏/设置页五项调整

1. 状态栏精确对齐原型图：左侧(保存/格式/字数) + 右侧(视图组/光标/编码/设置)
2. 顶部栏菜单精简为4项(保存/导出/主题/关于) + 下拉箭头图标
3. 移除左侧边栏折叠按钮(sidebar-toggle)
4. 设置页视觉升级：字体层级增强/图标currentColor/卡片式分组
5. 关于页整合：点击关于→打开设置并导航到about section
```

---

## 第二十一轮（续）：UI 布局优化修复 — 7项问题修复

### 修改文件清单
| 文件 | 变更内容 |
|------|----------|
| `src/styles/components.css` | ① 折叠箭头居中(flex+svg block) ② 卡片间距增大(12→16px)+双层阴影 |
| `src/components/SettingsPanel.tsx` | ③ 全部8个导航图标添加 fill=none/stroke=currentColor/stroke-width=1.8 + 太阳图标 circle r=4→r=5 |
| `src/components/TopBar.tsx` | ④ 完全重写：菜单移到 titlebar-right(窗口控制前) + 视图模式切换组(Live/Read/Source)替代原主题切换位 + 太阳图标增大(r=5+fill=none) + 还原 sidebar-toggle 按钮 + 新增 editorMode/onEditorModeChange/sidebarCollapsed/onToggleSidebar props |
| `src/App.tsx` | TopBar 调用新增 props(editorMode/onEditorModeChange/sidebarCollapsed/onToggleSidebar) |
| `src/styles/titlebar.css` | 新增视图模式切换组样式(.app-menu-view-modes/view-label/view-buttons/view-btn) |
| `src/i18n/locales/zh-CN.ts` | 新增 key: status.mode.source/status.viewMode/topbar.expandSidebar/collapseSidebar |
| `src/i18n/locales/en.ts` | 新增英文对应 key |

### 7个问题逐一解决
1. ✅ **箭头不居中** — `.settings-group-chevron` 添加 `display:flex;align-items:center;justify-content:center` + svg `display:block`
2. ✅ **折叠块边界不清** — margin-bottom 增大到 16px + 双层阴影增强层次感
3. ✅ **导航图标纯黑** — 全部 8 个 SECTIONS 图标 SVG 添加 `fill="none" stroke="currentColor" stroke-width="1.8"`
4. ✅ **太阳图标小黑点** — light 模式太阳 SVG: `circle r=4→r=5` + 显式 `fill="none" stroke="currentColor"` (TopBar + SettingsPanel 两处)
5. ✅ **菜单加视图切换** — 下拉菜单内新增「视图模式」组，含 Live/Read/Source 三按钮（带图标），active 态 accent 高亮
6. ✅ **菜单移到右上角** — app-menu 从 titlebar-left 移到 titlebar-right，位于 win-ctrls 最小化按钮前面
7. ✅ **还原 sidebar-toggle** — 左上角品牌旁恢复侧边栏折叠/展开按钮（chevron-left 图标，collapsed 时旋转）

### 构建验证
- tsc --noEmit: **0 错误**
- vite build: **379 modules, CSS 58.27KB, built in 2.55s**

### 提交记录
- commit `1d15e66` — feat(ui): 顶部栏菜单与底部状态栏重构，对齐原型图布局
- 暂存并提交 8 个源码文件（TopBar / StatusBar / SettingsPanel / App + components·statusbar·titlebar.css + zh-CN·en i18n）
- 排除 `.project-memory/memory/2026-07-16.md`（本地工作日志，不入库）

### 代码块三个 bug 修复（未提交）
1. **语言选择器滚动不同步** — Editor.tsx 新增 scroll 事件监听，滚动 `.editor-scroll` 时 `setCodeBlockLang(null)` 隐藏下拉框
2. **语言下拉框只显示一个选项** — EditorPickers.tsx 中 `CodeBlockLangPicker` 将 `<input>+<datalist>` 替换为 `<select>`（WebView2 中 datalist 不可靠）；当前语言不在预设列表时自动加入首位；CSS width 110→120px + cursor:pointer
3. **语法提示在非起始位置也悬浮** — syntaxHint handler 增加 `if ($from.parentOffset !== 0) { setSyntaxHint(null); return }` 检查
- tsc --noEmit: 0 错误；vite build: 379 modules, 58.42KB CSS, 4.14s

### 第二次提交
- commit `37759ae` — fix(editor): 代码块语言选择器与语法提示三处修复
- 含记忆文件一并提交（用户要求）

### 第三轮修复（未提交）
1. **右键菜单宽度撑满** — EditorMenus.tsx 三个右键菜单 inline style 加 `right: 'auto'` 覆盖 app-menu-dropdown 的 `right: 0`
2. **语言下拉框支持选择+手动输入** — CodeBlockLangPicker 改为 combobox：input 可输入 + 自定义 dropdown 面板过滤 34 种语言；Enter/blur 确认，Esc 取消
3. **语言下拉框融入代码块** — CSS：input 默认 transparent 背景（融入 code-bg）、hover fg-soft、focus 才显示 surface 背景+边框；新增 .code-lang-dropdown/.code-lang-option 面板样式；caret 箭头 focus 时隐藏
- tsc 0 错误；vite build 379 modules, 59.40KB CSS, 3.48s

### 第四轮修复（未提交）
1. **语言下拉框无法展开/无法修改** — 根因：容器 div 上 `onMouseDown preventDefault` 阻止 input 获取焦点；修复：移除容器 preventDefault，仅在 option 按钮上保留；blur 时仅在语言实际变化时才触发 onChange
2. **六个反引号中间回车** — 根因：``` 检测被包裹在 `$from.parentOffset === parent.textContent.length`（行尾）条件内；修复：将 ``` 检测移出行尾条件，增加 `textAfter` 检查 `/^```\s*$/`，光标在 `````` 中间回车时后三个作为结尾标记丢弃，创建代码块
- tsc 0 错误；vite build 379 modules, 59.60KB CSS, 4.03s

### 第五轮修复（未提交）
- **语言下拉框打开即筛选只剩一个** — 根因：`filtered` 用 `open && query.trim()` 判断，打开时 query 已是当前语言（如 plaintext），立即筛选只剩一个匹配项；修复：增加 `typing` 状态标记，`onFocus` 时 `setTyping(false)` 显示全部，`onChange` 时 `setTyping(true)` 才开始筛选