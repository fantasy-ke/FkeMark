# 2026-09-21

## 清理 TipTap 迁移遗留并补充 README 说明

- 需求：① 清理 BlockNote 迁移后残留的 TipTap 遗留；② 补充 README 的技术栈与项目结构说明。
- 现状核实（需求 ①）：`src/` 与 `tests/` 中共 12 个文件仍写着 `import type { Editor } from '@tiptap/react'`，`package.json` 也仍声明 `@tiptap/core@^2.1.0` 与 `@tiptap/react@^2.1.0`。核实后发现这不只是「没用到的旧依赖」，而是一处**版本错配**：BlockNote 0.46 内部依赖 **`@tiptap/core@3.29.0`**（嵌套在 `node_modules/@blocknote/*/node_modules/@tiptap/` 下），而项目顶层声明的是 v2.27.2，`node_modules` 中因此同时存在两套 TipTap。代码里 `blockNoteEditor._tiptapEditor as unknown as TiptapEditor` 的双重断言把真实的 v3 实例硬转成 v2 类型，掩盖了这处不一致。
- 实现（需求 ①）：
  - 新增 `src/types/editor.ts`，用 `export type TiptapEditor = BlockNoteEditor['_tiptapEditor']` 从 BlockNote 自身类型反推底层编辑器类型。类型因此始终跟随 BlockNote 实际使用的 TipTap 版本，项目不再需要直接声明 `@tiptap/*`。
  - 12 个文件不再从 `@tiptap/react` 取类型：其中 11 个改从 `src/types/editor` 导入，`useBlockNoteEditorController.ts` 因双重断言被删除而不再需要该类型导入。`FindReplaceBar.tsx`、`AiSelectionButton.tsx` 中原本就叫 `Editor` 的类型统一改名为 `TiptapEditor`。
  - `Editor.tsx` 与 `useBlockNoteEditorController.ts` 中的 `as unknown as TiptapEditor` 双重断言随之删除（源类型本身就是该别名）。
  - `package.json` 移除 `@tiptap/core` 与 `@tiptap/react` 两条直接依赖；`npm install` 剪除 16 个包，顶层 `node_modules/@tiptap` 目录消失，只剩 BlockNote 嵌套的 v3。
  - 顺带删除 `src/styles/editor.css` 中的 `.fk-math-edit { display: none; }`——它是已删除的 TipTap `MathNode` 时代的死规则（现行代码只用 `fk-math-editing` 与 `fk-math-edit-ta`，CSS 类选择器按完整词元匹配，不会误伤）。
- 实现（需求 ②）：
  - 技术栈描述由「Tauri + React + ProseMirror」更正为「Tauri v2 + React + BlockNote（基于 ProseMirror / TipTap）」。
  - 特性拆成「编辑体验 / 外观与集成」两组，补上原先未提及的能力：四种视图模式、Mermaid、标题与长代码块折叠、小地图、Wiki 双链与反向链接、文档大纲、命令面板与目录内搜索、全局查找替换、片段模板、拼写检查、Vim 模式、自定义快捷键、本地版本历史、回收站、演示模式、13 套主题、AI 助手、MCP 服务、图床与 WebDAV 同步。
  - 更正两处与实现不符的描述：导出格式由「Markdown / HTML / TXT 互转」改为实际的八种（md/html/txt/pdf/docx/epub/rtf/opml）；删除「打字机模式（光标始终居中）」——该能力只存在于 `Design.md`、`FkeMark-roadmap.md` 与文档站，`src/` 中没有任何实现（`focusMode` 仅隐藏侧栏/工具栏/状态栏）。
  - 项目结构树按实际目录重写，并补「常用命令」表与 `AGENTS.md` 指引。
- 涉及文件或模块：`src/types/editor.ts`（新增）、`src/components/Editor.tsx`、`src/components/FindReplaceBar.tsx`、`src/components/editor/AiSelectionButton.tsx`、`src/components/editor/SnippetsMenu.tsx`、`src/components/editor/useBlockNoteEditorController.ts`、`src/components/editor/useEditorAiAssistant.ts`、`src/components/editor/useEditorPerformanceDiagnostics.ts`、`src/components/editor/useSlashMenuTrigger.ts`、`src/components/editor/useVimMode.ts`、`src/components/editor/useWikiLinkPicker.ts`、`src/styles/editor.css`、`tests/ai.chat.test.tsx`、`tests/snippets.test.tsx`、`package.json`、`package-lock.json`、`README.md`。
- 验证结果：`npm test` 67 个套件、461 个用例通过（改动前后完全一致）；`npx tsc --noEmit` 通过；`npm run build` 通过，且**分包产物与改动前逐字节一致**（`vendor-tiptap` 255.87 kB、`vendor-blocknote` 451.59 kB、`index` 671.94 kB 均未变），证明两套 TipTap 中 v2 那套从未被打进产物——它只被 `import type` 引用，编译期即被擦除；`git diff --check` 通过；265 个活跃代码文件超过 800 行的 0 个、超过 1200 行的 0 个。
- 已知限制或未处理的无关问题：
  - `doc/guide/index.md` 与 `doc/en/guide/index.md` 仍在正文里宣传「打字机模式」，`FkeMark-roadmap.md` 第 36 项也把它标记为已完成。本次只改了 README，文档站与路线图未同步，需要单独处理。
  - `vite.config.ts` 的 `vendor-tiptap` 分组里 `/node_modules/@tiptap/` 这一条目前匹配不到任何被引用的模块（嵌套的 v3 路径会先被 `vendor-blocknote` 命中）。保留它是防御性的：若将来顶层再出现 `@tiptap/*`，仍会被正确归组，故未删除。
  - `src-tauri/Cargo.toml` 的 `notify` 仍是无任何引用的依赖；它与 TipTap 迁移无关，本次未处理。
  - 行数规则仍只由文档约束，没有自动化守卫测试。
- Git 提交信息：`refactor: 清理 TipTap 迁移遗留的直接依赖与类型引用`、`docs: 补充 README 说明并记录本次清理`

---

## 同步文档站与路线图中的「打字机模式」表述

- 需求：上一轮清理后遗留的文档不一致——文档站正文与路线图仍在宣传并不存在的「打字机模式」，需要同步。
- 核实：全仓库搜索 `打字机` / `typewriter` 只命中文档，`src/` 中没有任何打字机滚动或段落聚焦实现；`focusMode` 的实际行为仅是隐藏侧栏、工具栏与状态栏，不含光标居中或非当前段落变淡。
- 实现：
  - `doc/guide/index.md` 与 `doc/en/guide/index.md` 的「三分钟工作流」第 5 步删去打字机模式，只保留专注模式（专注模式本身在 `doc/guide/settings.md` 与 `doc/guide/shortcuts.md` 中已有正确记载，快捷键 F11）。
  - `FkeMark-roadmap.md` 第 36 项「打字机滚动 + 段落聚焦」去掉 ✅，恢复为待做项——该清单的约定是有 ✅ 表示已实现。
  - `Design.md` 第 28、184 行仍保留打字机模式：该文件是规划期的产品设计与优先级矩阵（列为 P2 外围完善），描述的是计划而非现状，不构成失实，故未改动。
- 涉及文件或模块：`doc/guide/index.md`、`doc/en/guide/index.md`、`FkeMark-roadmap.md`。
- 验证结果：在 `doc` 目录执行 `npm run build`（VitePress）通过，`build complete in 10.58s`，页面渲染完成；构建产物 `doc/.vitepress/dist/` 已被 `doc/.gitignore` 忽略；复查 `打字机` / `typewriter` 只剩路线图第 36 项（已无 ✅）与 `Design.md` 的规划描述；`git diff --check` 通过。
- 已知限制或未处理的无关问题：
  - `doc/guide/renderer-compatibility.md` 与 `doc/en/guide/renderer-compatibility.md` 第 57 行仍写着「旧版依赖 TipTap 2 自定义节点的部分悬浮语法提示仍需要分别迁移……不能继续复用旧 TipTap 2 节点」。项目已不再有任何 `@tiptap/*` 引用，该警告的前提已不存在；同时 `src/components/editor/markdownMarkerExtension.ts` 的语法标记提示已是 BlockNote `createExtension` + ProseMirror 插件实现。但「部分」具体指哪些能力无法确证，故本次未改写，留待确认。
  - `FkeMark-roadmap.md` 第 32 项「字数目标 / 写作统计 / 阅读时长」标记为 ✅，但全仓库没有字数目标（写作目标）实现，只有 `src/utils/documentStats.ts` 的字数与阅读时长统计；属部分实现被整体标记为完成，需产品判断后再改。
  - 本次未对路线图全部 38 项逐条核对 ✅，只处理了已确证的失实项。
- Git 提交信息：`docs: 同步文档站与路线图并移除不存在的打字机模式`

---

## 新增 Tab 半自动续写、双链图谱与 Agent Harness

- 需求：① Tab 半自动续写——AI 给出灰色建议，Tab 接受、Esc 拒绝；② 支持双链图谱；③ 增加整套 Agent Harness，可直接修改文件内容，并提供文件快照按钮查看变更。
- 需求澄清（已与用户确认）：三项一次全部实现；Agent Harness 扩展现有 AI 对话侧栏；Agent 直接写磁盘并同步已打开标签页；续写采用「停顿自动触发 + 设置开关」；双链图谱放在编辑器右侧可开关浮层。
- 实现 ①（Tab 半自动续写）：
  - `src/utils/aiGhostText.ts`：纯规则层——触发判定（光标前必须是空白、标点或中日韩文字，避免拉丁词中打断输入）、上下文截取（末尾 4000 字符）、结果清洗（去代码围栏/包裹引号、只留第一行、剔除模型复述的前文尾部、按断句回退截断到 240 字符、`NONE` 视为无建议）、800ms 停顿与 1200ms 接受冷却常量。
  - `src/utils/aiAssistant.ts` 新增 `GHOST_TEXT_INSTRUCTION` / `buildAiCompletionMessages` / `runAiCompletion`，复用现有三种上游格式的请求通道，续写指令禁止 Markdown 语法与换行（幽灵建议是行内文本，插入时按纯文本处理，不做 Markdown 解析）。
  - `src/components/editor/aiGhostTextExtension.ts`：BlockNote `createExtension` + ProseMirror `Plugin`，用 widget decoration 渲染 `.ai-ghost-text`；插件状态在 `docChanged` 或 `selectionSet` 时自动失效，避免接受时插入到过期位置。
  - `src/components/editor/useAiGhostText.ts`：注册在 `document` 捕获阶段的 keydown 拦截 Tab/Esc（必须先于 BlockNote `tabBehavior: 'prefer-indent'` 的缩进），请求号丢弃过期响应，并用文档对象同一性 + 光标位置双重校验结果时效性。只在实时编辑模式生效。
  - 设置项 `aiGhostTextEnabled`（默认 true，仅在 `aiEnabled` 为真时生效）贯通 `src/types/index.ts`、`appDefaults.ts`、`src-tauri/src/settings.rs`、设置界面与中英文文案。
- 实现 ②（双链图谱）：
  - `src/utils/markdown/linkGraph.ts`：`buildWikiLinkGraph` 统计出链/反向链接/边权重（忽略自引用与未解析目标），按连接度排序并在超过 200 篇时截断；`computeLinkGraphLayout` 为确定性力导向布局（圆周初始化、无随机数，同一图谱坐标稳定可测）；路径排序统一用码位比较而非 `localeCompare`，避免不同环境 ICU 差异导致截断结果漂移。
  - `src/components/LinkGraphPanel.tsx`：编辑器右侧浮层，复用反向链接面板的交互（开关按钮、Escape 关闭、刷新）与 `tabContentCache` 优先读取未保存内容；SVG 固定 800×800 viewBox 自适应面板尺寸，支持节点拖拽、悬停高亮邻居、孤立笔记开关。
  - 为了让图谱按路径直接解析双链，`src/utils/markdown/wikiLinks.ts` 的 `resolveWikiNotePath` 由私有改为导出。
- 实现 ③（Agent Harness）：
  - `src/utils/agent/tools.ts`：六个工具（list/read/search/outline/write/append）。路径守卫限制在 `mcpAllowedRoots`（未配置时回落到当前文件夹）内且只允许 `.md`/`.markdown`；写入类工具受 `mcpPermissionMode` 限制（`read-only` 直接拒绝并把实际模式写进提示）；错误一律转成 `ok:false` 的结果回灌给模型，不中断循环。
  - `src/utils/agent/harness.ts`：模型用带 ```tool 围栏的 JSON 单步调用工具（**不用上游原生 function calling**，从而在 chat-completions / responses / anthropic-messages 三种格式下行为一致），工具结果以 user 消息回灌，最多 6 步后停止并提示查看变更列表。
  - 写盘走既有 `write_file_command`，因此**自动复用后端「写入前记录旧内容」的版本快照**，无需额外快照调用；写后通过 `onFileWritten` 回调同步标签页。
  - `src/app/useAppTabs.ts` 的 `applyExternalDocumentChanges` 新增 `force` 选项：Agent 直接写盘后磁盘内容即最终结果，即使标签有未保存修改也要同步，否则界面会继续显示已被覆盖的旧内容。
  - `src/components/VersionDiffDialog.tsx`：从 `VersionHistoryMenu` 抽出的通用差异对话框（含行级 diff、上下跳转、附加操作插槽）；`VersionHistoryMenu` 改用它并保留「Escape 只关对话框、不关快照列表」的原有行为；`src/components/ai/AgentChangeDialog.tsx` 复用它展示本次会话写入过的文件与前后差异。
  - `AiChatSidebar` 新增 Agent 模式开关（`aria-pressed`）、工具调用进度行、变更入口（带数量徽标）与权限提示；Agent 模式为整轮非流式执行（流式与工具循环混用会显著复杂化），执行中显示「Agent 正在调用工具…」。
- 涉及文件或模块：`src/utils/aiGhostText.ts`（新增）、`src/utils/aiAssistant.ts`、`src/utils/agent/tools.ts`（新增）、`src/utils/agent/harness.ts`（新增）、`src/utils/markdown/linkGraph.ts`（新增）、`src/utils/markdown/wikiLinks.ts`、`src/components/editor/aiGhostTextExtension.ts`（新增）、`src/components/editor/useAiGhostText.ts`（新增）、`src/components/editor/useBlockNoteEditorController.ts`、`src/components/editor/VersionHistoryMenu.tsx`、`src/components/Editor.tsx`、`src/components/LinkGraphPanel.tsx`（新增）、`src/components/VersionDiffDialog.tsx`（新增）、`src/components/ai/AgentChangeDialog.tsx`（新增）、`src/components/ai/AiChatSidebar.tsx`、`src/components/settings/SettingsAiSection.tsx`、`src/app/AppLayout.tsx`、`src/app/appDefaults.ts`、`src/app/useAppTabs.ts`、`src/App.tsx`、`src/types/index.ts`、`src-tauri/src/settings.rs`、`src/i18n/locales/ai.ts`、`src/i18n/locales/wikiLinks.ts`、`src/styles/editor.css`、`src/styles/link-graph.css`（新增）、`src/styles/ai-chat.css`、`src/index.css`、`tests/ai.ghost-text.test.ts`（新增）、`tests/link-graph.test.ts`（新增）、`tests/link-graph.panel.test.tsx`（新增）、`tests/agent.tools.test.ts`（新增）、`tests/agent.harness.test.ts`（新增）、`tests/ai.settings.test.tsx`、`tests/ai.chat.test.tsx`、`tests/editor.interactions.test.tsx`。
- 验证结果：`npm test` 72 个套件、503 个用例通过（改动前基线 67 个套件、461 个用例，本次新增 5 个套件 42 个用例）；`npx tsc --noEmit` 通过；`npm run build` 通过（最大 chunk 仍为 `ruby-*.js` 899.87 kB，未触发大 chunk 警告）；`cargo test` 47 项通过（含新增 `ai_ghost_text_enabled` 默认值断言）、`cargo check` 通过、`rustfmt --check src-tauri/src/settings.rs` 通过；`git diff --check` 通过；扫描 `src`/`tests` 全部代码文件，超过 800 行的 0 个、超过 1200 行的 0 个（最大为 `src/i18n/locales/zh-CN.ts` 与 `en.ts` 各 766 行）。
- 界面核验：临时 Vite harness（`Temp/uicheck`，已 gitignore，核验后删除）+ headless Chrome（`--headless=new --dump-dom`）在真实浏览器中渲染真实组件，`fetch` 桩返回笔记内容与 OpenAI 兼容响应。实测：
  - 续写：`.ai-ghost-text` 已渲染、`contenteditable="false"`、颜色 `rgb(138,138,138)`（即 `--muted` #8a8a8a）、`opacity 0.7`，与正文 `rgb(31,41,55)` 对比明显；在真实 BlockNote 编辑器上 `editor.isFocused` 为真时停顿后出现建议，**按 Tab 后文档变为「这是一段已经写好的正文，AI 续写的下一句」且灰色建议消失**；再次触发建议后**按 Escape 文档内容不变且建议消失**；全过程控制台无报错。
  - 双链图谱：面板宽 520px、无横向溢出、高度填满宿主（468 = 520 − 42 − 8），画布 498×378，3 个节点 / 4 条有向边，当前笔记节点填充为 `--accent` #c96442，标签 20px 用 `--fg`，统计文案「3 篇笔记 · 4 条双链」。
  - Agent：变更对话框标题「Agent 文件变更」、副标题为文件路径、1 个文件切换项、diff 为 +2/−1 行；侧栏 Agent 开关点击后 `aria-pressed="true"`、提示文案与 `--accent` 高亮生效。
- 已知限制或未处理的无关问题：
  - 续写只在实时编辑模式生效；源码/分栏/阅读模式没有可渲染行内建议的文档结构，未做 textarea 镜像浮层。
  - 接受建议后有 1.2 秒冷却，冷却期内的文档变化不会重新排期请求（需用户再次输入才会请求下一条），这是为避免请求循环的有意设计。
  - 续写建议按纯文本插入，因此模型若返回 Markdown 语法会以字面字符出现；已通过系统指令禁止 Markdown 语法，但无法在协议层强制。
  - 图谱最多渲染 200 篇笔记（按连接度取前 N），超出时只在面板内提示；每次打开/刷新都会重新并行读取未缓存的笔记文件，大目录下首次打开有等待。
  - Agent 工具依赖 Tauri 桌面环境（直接 `invoke`），浏览器开发模式不可用；Agent 模式整轮非流式，长回答要等全部工具步完成才显示。
  - Agent 没有删除文件的能力（`full-access` 与 `data-read-write` 在当前工具集下等价），也未实现上游原生 function calling。
  - `LinkGraphPanel` 与 `BacklinksPanel` 各自持有一份 4 行的 `readMarkdownFile`（Tauri invoke / `/api/read-file` 回退），与既有写法保持一致，未抽公共函数。
  - 行数规则仍只由文档约束，没有自动化守卫测试（沿用上一轮结论）。
- Git 提交信息：`feat: 新增 Tab 半自动续写、双链图谱与可写文件的 Agent Harness`

---

## 移除双链图谱的「孤立笔记」勾选框

- 需求：用户询问「孤立笔记勾选框是什么功能」，并要求删除该勾选框。
- 功能说明（删除前的行为）：`LinkGraphPanel` 头部有一个复选框，文案取自 i18n 键 `graph.showOrphans`（中文「孤立笔记」/ 英文「Orphans」），默认勾选。它只影响渲染，不改变数据：取消勾选时按边的两端收集已连接节点，把没有任何双链（出链与反向链接都为 0）的节点从图谱中过滤掉，同时该节点的计数、统计文案与布局一并随之变化。数据层 `buildWikiLinkGraph` 始终包含孤立节点，因此该勾选框属于纯视图过滤。
- 实现：
  - `src/components/LinkGraphPanel.tsx` 删除 `showOrphans` 状态与 `visibleGraph` 过滤 `useMemo`，`layout` / `neighbours` / `countGraphLinks` / 节点计数 / 统计文案 / SVG 渲染全部直接消费 `graph`；删除头部 `<label className="link-graph-orphans">` 复选框块，头部只剩刷新与关闭两个按钮。
  - 移除后行为等价于原先的默认态：孤立笔记始终参与渲染（测试中 3 个节点含 1 个孤立节点）。
  - `src/styles/link-graph.css` 删除 `.link-graph-orphans` 与 `.link-graph-orphans input` 两条规则，并从 `.link-graph-heading, .link-graph-actions` 的分组选择器中移除该类名。
  - `src/i18n/locales/wikiLinks.ts` 删除中英文的 `graph.showOrphans` 键（该键无其他引用）。
  - `tests/link-graph.panel.test.tsx`：原用例「可以隐藏孤立笔记并在 Escape 时关闭面板」改写为「在 Escape 时关闭面板」，只保留 Escape 关闭断言；在渲染用例中补充「孤立笔记始终参与渲染」与「头部不再有过滤勾选框」两条断言作为回归守卫。
- 涉及文件或模块：`src/components/LinkGraphPanel.tsx`、`src/styles/link-graph.css`、`src/i18n/locales/wikiLinks.ts`、`tests/link-graph.panel.test.tsx`。
- 验证结果：`npm test` 72 个套件、505 个用例全部通过；`npm run build` 通过（`✓ built in 19.57s`）；`git diff --check` 通过；改动文件行数 `LinkGraphPanel.tsx` 298、`link-graph.css` 206、`wikiLinks.ts` 59、`link-graph.panel.test.tsx` 150，均低于 800 行完成标准。
- 已知限制或未处理的无关问题：
  - 本次未做浏览器/桌面端界面核验（本会话无可用浏览器工具），只由组件测试断言头部已无复选框、孤立节点仍被渲染；界面观感建议在 Tauri 应用中人工确认一次。
  - 工作区在本任务开始前已存在两处未提交改动（`LinkGraphPanel.tsx` 与 `tests/link-graph.panel.test.tsx` 中「没有打开的 Markdown 文档时不渲染图谱入口」及其两个测试），与本次改动位于同一批文件。按用户选择拆成两次提交：先把已有改动单独提交为 `a016007`，再提交本次删除。
- Git 提交信息：`fix: 无打开的 Markdown 文档时隐藏双链图谱入口`（已有改动，先提交）、`refactor: 移除双链图谱的孤立笔记勾选框`（本次改动）

---

## AI 对话侧栏支持直接切换大模型

- 需求：在 AI 对话（Agent）侧栏里直接切换大模型，不必每次进设置面板改。
- 需求澄清（已与用户确认）：① 切换写入全局设置 `settings.aiModel`，聊天、Agent、Tab 续写、选中改写共用同一个模型；② 下拉框 + 从当前端点拉取模型列表；③ 选择器放在输入框上方新增的一行。
- 现状：模型此前只能在「设置 → AI → 模型」修改（自由输入 + `datalist` 候选，候选需手动点「获取模型」）；`AiChatSidebar` 完全没有模型入口，也没有写回设置的通道。
- 实现：
  - `src/components/ai/AiChatSidebar.tsx` 新增可选属性 `onModelChange?: (model: string) => void`，以及 `models` / `modelsBusy` / `modelsError` 三个状态与 `loadModels()`（复用 `src/utils/aiAssistant.ts` 已有的 `fetchAiModels`，失败时把错误文本渲染在行下方而不是抛出）。
  - 候选列表 `modelOptions` 始终把当前模型放在第一位：接口返回的列表可能不含手填的私有模型，否则 `<select>` 的 `value` 会匹配不到任何 `option` 而显示空白。
  - 选择器行位于 `.ai-chat-composer` 表单项最前（Agent 模式提示行上方），结构为「标签 + select + 获取模型按钮」；请求进行中（`busy`）或列表拉取中（`modelsBusy`）时禁用，与同文件的历史会话下拉保持一致。
  - 文案完全复用已有的 `ai.settings.model` / `ai.settings.models.fetch` / `ai.settings.models.loading` / `ai.settings.models.error` 四个键，未新增 i18n 条目。
  - `src/app/AppLayout.tsx` 传入 `onModelChange={(model) => handleSettingsChange({ ...settings, aiModel: model })}`，因此改动会经由既有 `save_settings` 持久化，无需新增设置字段（`AppSettings.aiModel` 与 Rust 侧字段都已存在）。
  - `src/styles/ai-chat.css` 新增 `.ai-chat-model-row` / `.ai-chat-model-fetch` / `.ai-chat-model-error`，沿用 `--border`/`--bg`/`--fg`/`--muted`/`--accent` 语义变量与既有的 28px 控件高度。
- 涉及文件或模块：`src/components/ai/AiChatSidebar.tsx`、`src/app/AppLayout.tsx`、`src/styles/ai-chat.css`、`tests/ai.chat.test.tsx`。
- 验证结果：`npm test` 72 个套件、507 个用例全部通过（新增 2 个用例）；`npm run build` 通过（`✓ built in 14.81s`）；`git diff --check` 通过；改动文件行数 `AppLayout.tsx` 649、`AiChatSidebar.tsx` 476、`ai-chat.css` 413、`ai.chat.test.tsx` 349，均低于 800 行完成标准。
- 界面核验：临时 Vite harness（`Temp/uicheck`，已 gitignore，核验后删除）+ headless Chrome（`--dump-dom` 与 `--screenshot`，`fetch` 桩返回 `/models` 列表）在真实浏览器中渲染真实组件。实测：
  - 正常路径：DOM 中 `.ai-chat-model-row` 含标签「模型」、`aria-label="模型"` 的 select 与获取按钮；点击获取后 `<option>` 为 `llama3.1, qwen2.5:7b, deepseek-r1:14b`；截图 430×780 下该行位于「引用当前文档」按钮与输入框上方，宽度撑满、无溢出、无错位。
  - 失败路径：`/models` 返回 503 时，行下方渲染红色「获取模型失败：AI model list request failed (503): endpoint unreachable」，两行换行不破坏布局，选择器仍可用。
  - 坑：Vite 4 在 Windows 上对正在写入的 `.tsx` 触发 `EBUSY` 会让 dev server 的 watcher 崩溃退出，核验脚本把 dump/截图输出写到 Vite root 之外并设置 `server.watch: null` 才稳定；另外 `chrome.exe` 是 GUI 子系统程序，PowerShell 直接 `>` 重定向拿不到 `--dump-dom` 输出，必须用 `Start-Process -RedirectStandardOutput`。
- 已知限制或未处理的无关问题：
  - 模型列表只存在于侧栏组件状态里，应用重启后需要重新点一次「获取模型」；没有做成持久化缓存，也没有与设置面板的列表共享。
  - 侧栏的模型选择只改模型名，不切换 provider / 上游格式 / 端点；这些仍需在设置面板修改，因此换了端点后要重新获取列表。
  - 拉取模型列表会真实请求当前端点；端点不可达时侧栏会显示错误行，不会静默失败。
- Git 提交信息：`feat: AI 对话侧栏支持直接切换大模型`

---

## 修复双链图谱入口在欢迎页显示

- 需求：用户反馈「优化打开双链图谱按钮，在首页这个按钮也显示在哪里，这是不正常的」。
- 根因：`LinkGraphPanel` 的入口按钮只要 `!open` 就无条件渲染，而相邻的 `BacklinksPanel` 带 `if (!currentIsMarkdown) return null` 守卫；两个面板都渲染在 `AppLayout` 的 `showWelcome` 分支之外，因此在没有标签页的欢迎页（首页）上，图谱按钮会浮在右上角，反向链接按钮则正常隐藏——两个相邻入口的显示条件不一致。
- 实现：`src/components/LinkGraphPanel.tsx` 增加与反向链接面板一致的 `currentIsMarkdown` 判断（当前文件存在且为 `.md` / `.markdown`），并在**全部 hook 之后**加 `if (!currentIsMarkdown) return null`；由于整组件不渲染，从 Markdown 文档切到其他文档时已打开的面板也会一并关闭。守卫必须在 hook 之后，否则 hook 调用数量会随渲染变化。
- 涉及文件或模块：`src/components/LinkGraphPanel.tsx`、`tests/link-graph.panel.test.tsx`。
- 验证结果：新增两个用例（无 Markdown 文档时不渲染入口；从 Markdown 切到 `.txt` 时关闭已打开面板）；`npm test` 72 个套件、505 个用例通过（含并行会话的孤立笔记改动），`npx tsc --noEmit`、`npm run build`、`git diff --check` 通过，扫描 `src`/`tests` 无文件超过 800 行。
- 界面核验：临时 Vite harness（`Temp/uicheck2`、`Temp/uicheck3`，已 gitignore，核验后删除）+ headless Chrome 渲染真实 `WelcomeScreen` + `BacklinksPanel` + `LinkGraphPanel`。实测欢迎页（无文件夹、无文档）`graphToggle = 0`、`backlinksToggle = 0`、`graphPanel = 0`；打开 Markdown 文档后 `graphToggle = 1`、`backlinksToggle = 1`，按钮 title 为「打开双链图谱」；同时复核并行会话的改动：图谱面板头部只剩「重新扫描 / 关闭」两个按钮，孤立笔记仍参与渲染（3 节点 · 2 条双链）。
- 已知限制或未处理的无关问题：
  - 该修复由并行会话按用户指示单独提交为 `a016007`，本会话补充记录根因与浏览器核验证据。
  - 仅打开文件夹、尚未打开任何笔记时仍属欢迎页状态，此时图谱入口同样不显示；与反向链接面板一致，需要先打开一篇笔记才能进入图谱。
  - 核验期间工作区还有另一会话正在编辑 AI 对话侧栏（模型切换器，涉及 `src/app/AppLayout.tsx`、`src/components/ai/AiChatSidebar.tsx`、`src/styles/ai-chat.css`、`tests/ai.chat.test.tsx`），本会话未触碰、未暂存也未提交这些文件。
- Git 提交信息：`fix: 无打开的 Markdown 文档时隐藏双链图谱入口`（`a016007`，由并行会话提交）、`docs: 记录双链图谱入口显示条件的修复与核验`（本次记忆更新）

---

## AI 对话改为文件引用标识，并支持暂停 / 停止生成

- 需求：① AI 聊天里的文件引用不应该把文件内容整个发出去，应该只发一个文件引用标识，点击可以焦点锁定到被引用的文件；② AI 要增加暂停思考的功能。
- 需求澄清（已与用户确认）：暂停语义**两者都要**——聊天流支持「暂停 / 继续」，同时提供「停止」；Agent 工具循环只支持停止。引用胶囊点击行为=切换到该文件标签页。工作区有并行会话未提交的模型切换器改动，选择「直接在现有工作区继续改，最后一起提交」。
- 实现 ①（文件引用标识）：
  - `src/types/index.ts` 新增 `AiFileReference { path, name }`，`AiChatMessage` 增加可选 `references`（localStorage 里的历史会话向后兼容）。
  - `ActiveAiDocument` 增加 `path`（`AppLayout` 传入 `currentFile`），引用与「引用当前文件」按钮的可用性都改为依赖路径而不是内容——未保存的新文档没有路径，因此无法引用。
  - `AiChatSidebar` 中「引用当前文档」不再把 `activeDocument.content` 拼进请求，只把**路径标识**作为上下文行发送（`引用的文件标识:` + 路径），并给消息挂上 `references`；选中的 Markdown 仍按内容发送（润色/翻译需要原文），两种上下文语义明确分开。
  - 消息渲染新增引用胶囊 `.ai-chat-file-ref`：显示文件名、`title` 为完整路径、点击调用新的 `onOpenFile` prop 切换到该文件；与当前活动文件路径一致时加 `is-active` 高亮（路径比较归一化分隔符与大小写）。
  - `createConversationTitle` 跳过引用标识行，避免会话标题变成文件路径。
  - 引用只发标识意味着非 Agent 模式下模型看不到内容，因此新增提示文案 `ai.chat.referenceHint`，并新增 `ai.chat.referenceOnlyPrompt` 作为「只引用不提问」时的默认指令。
- 实现 ②（暂停 / 停止）：
  - `src/utils/aiAssistant.ts` 新增 `AiStreamControl { signal?, waitWhilePaused? }`，`runAiChat` 增加第 5 个可选参数并透传到 `runAiStreamingRequest`。
  - 暂停：在读取前与读取后各过一次暂停门，暂停期间既不消费流也不追加内容，恢复后把已读到的内容补上；停止：中止请求并保留已生成内容，空结果不再抛错（否则「停止」会被显示成请求失败）。
  - `src/utils/agent/harness.ts` 的 `runAgentHarness` 透传 `signal`，停止后直接返回 `stopped: true` 且不再解析工具调用，避免用半截回复触发写盘；`AgentRunResult` 增加 `stopped`。
  - `AiChatSidebar` 生成期间把发送按钮换成「暂停/继续」（仅聊天流）与「停止」两个按钮，思考指示在暂停时显示「已暂停」；停止后若没有任何内容则只保留用户消息，不留空 AI 气泡。
- 界面核验发现并修复的两个真实缺陷（单元测试无法覆盖，靠浏览器核验暴露）：
  - **流空闲时点停止会挂起**：循环只在顶部检查 `signal.aborted`，而 `await reader.read()` 在流没有新数据时会一直 pending，导致停止后界面停在「生成中」。修复为监听 `abort` 立即 `reader.cancel()`，让 pending 的 read 以 `done` 结束。
  - **暂停后仍会追加一段**：点暂停时可能已有一个 read 在飞，该段会在暂停后仍被追加。修复为读取后再过一次暂停门，先挂起再追加。
- 涉及文件或模块：`src/types/index.ts`、`src/utils/aiAssistant.ts`、`src/utils/agent/harness.ts`、`src/components/ai/AiChatSidebar.tsx`、`src/app/AppLayout.tsx`、`src/styles/ai-chat.css`、`src/i18n/locales/ai.ts`、`tests/ai.assistant.test.ts`、`tests/ai.chat.test.tsx`、`tests/agent.harness.test.ts`。
- 验证结果：`npm test` 72 个套件、518 个用例通过（本次新增 6 个用例，并改写了原「附加当前文档」用例为「引用标识」语义）；`npx tsc --noEmit`、`npm run build`、`git diff --check` 通过；扫描 `src`/`tests` 无文件超过 800 行（最大仍为 `src/i18n/locales/zh-CN.ts` 与 `en.ts` 各 766 行）。
- 界面核验：临时 Vite harness（`Temp/uicheck4`，已 gitignore，核验后删除）+ headless Chrome（`--headless=new --dump-dom`）渲染真实 `AiChatSidebar`，`fetch` 桩返回 SSE 流并由 harness **逐块放行**（虚拟时间会把普通定时器快进，固定 sleep 无法验证暂停）。实测：
  - 引用卡片标题「已引用文件：首页.md」、预览是路径 `D:\notes\首页.md` 而不是正文；消息里含路径、**不含**文档正文；胶囊 `is-active` 为真、title 为「切换到该文件：D:\notes\首页.md」，点击后 `onOpenFile` 收到该路径。
  - 生成期间输入行恰好是 `.ai-chat-pause` + `.ai-chat-stop` 两个按钮，`scrollWidth - clientWidth = 0`（无横向溢出）。
  - 暂停后 `aria-pressed=true`、指示为「已暂停」，此时放行下一段数据界面内容**不变**；恢复后内容补上；停止后暂停/停止按钮消失、发送按钮回来、已生成内容保留；控制台无报错。
  - 踩坑记录：Vite 4 在 Windows 上对正在写入的 `main.tsx` 触发 `EBUSY` 会让 dev server 的 watcher 崩溃退出，因此必须在**写完 harness 文件之后**再启动 dev server。
- 已知限制或未处理的无关问题：
  - 非 Agent 模式下引用只发标识，模型看不到文件内容，需要用户开启 Agent 模式让 AI 用工具读取；这是本次需求的直接结果，已在侧栏用提示文案说明。
  - 「暂停」只停止本地消费，服务端仍在继续生成（照常计费），暂停过久可能因连接超时中断；这是流式协议下暂停的固有代价。
  - 停止语义是「中断并保留已生成内容」，不是可续传的暂停；恢复只能重新提问。
  - 引用只支持单个文件（当前活动文件），没有做多文件引用或「锁定为持续引用上下文」。
  - 本次改动与并行会话的「AI 对话侧栏切换模型」落在同一批文件，按用户选择在同一工作区继续改；提交时对方已先一步把它的改动提交为 `feat: AI 对话侧栏支持直接切换大模型`，因此本次提交只包含上述两项改动，两者在历史上是分开的（本记录不写本次提交的哈希：把哈希写进同一次提交会让哈希自身失效）。
- Git 提交信息：`feat: AI 对话支持文件引用标识与暂停停止生成`

---

## 修正 AI 侧栏 composer 中 Agent 提示与引用按钮的间距

- 需求：用户反馈「Agent 模式的气泡应该跟引用当前文档按钮有点间隔」。
- 根因：`.ai-chat-composer` 是普通块级容器，没有 `gap`，间距完全由每个子块的 `margin-bottom` 承担（模型行、模型错误、引用按钮、上下文卡片都是 8px）。`.ai-chat-agent-hint` 漏了这个 `margin-bottom`，因此紧贴下方的「引用当前文档」按钮；同一位置新增的 `.ai-chat-reference-hint` 有同样问题，会紧贴其后的上下文卡片。
- 实现：`src/styles/ai-chat.css` 给 `.ai-chat-agent-hint` 与 `.ai-chat-reference-hint` 各补 `margin-bottom: 8px`，与 composer 内其它区块保持一致；未改动任何结构或逻辑。
- 涉及文件或模块：`src/styles/ai-chat.css`。
- 验证结果：`npm test` 72 个套件、518 个用例通过；`npm run build`、`git diff --check` 通过。
- 界面核验：临时 Vite harness（`Temp/uicheck5`，已 gitignore，核验后删除）+ headless Chrome 渲染真实 `AiChatSidebar` 并同时打开 Agent 模式与文件引用，实测相邻区块垂直间距为：Agent 气泡 → 引用按钮 8px、引用按钮 → 引用提示 8px、引用提示 → 上下文卡片 8px、上下文卡片 → 快捷动作 8px，全部一致；composer 无横向溢出，控制台无报错。
- 已知限制或未处理的无关问题：composer 仍靠各子块自身的 `margin-bottom` 维持间距，新增区块时需要记得补这一条（本次未改为容器 `gap`，避免影响既有子块的独立间距）。
- Git 提交信息：`style: 补齐 AI 侧栏 Agent 提示与引用提示的下间距`



