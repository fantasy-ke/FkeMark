# 2026-07-31 工作记录

## 新增本地订阅与 7 天试用入口

- 需求：在设置中新增订阅功能，提供月度、季度、年度和永久方案；新电脑首次使用应进入 7 天试用；界面样式需要符合现有设置页布局。
- 实现：新增 `src/utils/subscription.ts` 统一管理方案、试用期、订阅有效期和状态判断；在前端默认设置与 Tauri 设置结构中加入订阅方案、开始时间、到期时间和当前设备试用开始时间，并在旧设置反序列化时保持默认兼容；设置面板新增“订阅”导航、状态卡片、方案卡片和本地启用按钮；同步中英文语言资源和设置页样式。
- 涉及文件或模块：`src/types/index.ts`、`src/app/appDefaults.ts`、`src/App.tsx`、`src/utils/subscription.ts`、`src/components/SettingsPanel.tsx`、`src/components/settings/SettingsSubscriptionSection.tsx`、`src/styles/components/settings-panel.css`、`src/i18n/locales/subscription.ts`、`src/i18n/locales/zh-CN.ts`、`src/i18n/locales/en.ts`、`src-tauri/src/settings.rs`、`tests/subscription.test.ts`、`tests/subscription.settings.test.tsx`、`tests/editor.interactions.test.tsx`。
- 验证结果：`npm test` 通过（42 个测试文件，265 个测试）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（23 个 Rust 测试）；`git diff --check` 通过；活跃代码文件行数扫描无超过 800 行；通过本地 Vite 页面进行设置页订阅入口视觉核验。
- 已知限制或未处理的无关问题：本次只实现本地订阅状态与本地方案启用入口，未接入支付、账号、服务器校验或防篡改机制；删除或改写本地设置可能重置当前设备的试用或订阅状态。
- Git 提交信息：`feat: 新增本地订阅与试用入口`

## 限制生产包 DevTools 与浏览器右键入口

- 需求：dev 编译包需要在设置中提供开关，控制是否允许浏览器原生右键菜单、F12 与 DevTools 调试入口；release 环境不展示开关，默认禁用这些入口，且不能破坏已有组件自定义右键菜单。
- 实现：在 `src/utils/updater.ts` 增加 DevTools 访问策略与快捷键识别；在 `src/app/AppLayout.tsx` 按构建通道和设置开关统一拦截浏览器原生右键与 F12 / Ctrl+Shift+I 等快捷键，并保留已 `preventDefault` 的组件自定义右键逻辑；在关于设置页仅 dev 通道展示调试开关和打开按钮；同步设置默认值、类型、中英文文案和设置搜索索引；Rust 侧通过 `devtools` Cargo feature、`UPDATE_CHANNEL=dev` 和持久化设置三重限制 `open_devtools` 命令，release 构建移除默认 DevTools feature 与内部切换权限；CI 的 dev 包显式启用 `--features devtools`，release 包保持禁用。
- 涉及文件或模块：`src/utils/updater.ts`、`src/app/AppLayout.tsx`、`src/components/settings/SettingsAboutSection.tsx`、`src/components/SettingsPanel.tsx`、`src/types/index.ts`、`src/app/appDefaults.ts`、`src/i18n/locales/about.ts`、`src-tauri/src/lib.rs`、`src-tauri/src/settings.rs`、`src-tauri/Cargo.toml`、`src-tauri/capabilities/default.json`、`src-tauri/gen/schemas/capabilities.json`、`.github/workflows/dev.yml`、`.github/workflows/release.yml`、`tests/devtools-access.test.ts`、`tests/core.test.ts`。
- 验证结果：`npm test -- tests/devtools-access.test.ts tests/core.test.ts` 通过；`npm test` 通过（43 个测试文件，270 个测试）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（24 个 Rust 测试）；`UPDATE_CHANNEL=dev cargo check --manifest-path src-tauri/Cargo.toml --features devtools` 通过；`git diff --check` 通过；活跃代码文件行数扫描无超过 800 行；通过本地 Vite 浏览器页面核验 dev 通道设置页可见调试开关与打开按钮，而 latest 通道不显示该入口。
- 已知限制或未处理的无关问题：尚未在真实 Tauri 桌面窗口中再做一次人工界面核验；自定义右键菜单需要在自身处理逻辑中调用 `preventDefault` 才能绕过全局原生菜单拦截。
- Git 提交信息：`fix: 限制生产包开发者工具入口`

## ??????????

- ????? `src/components/settings/SettingsSubscriptionSection.tsx` ?? `trial`?`active`?`expired` ????????????????????????????
- ???????????????????? `getStatusDisplay` ????????????????????? `active` ???? `lifetime` ?????????????????
- ????????`src/components/settings/SettingsSubscriptionSection.tsx`?`tests/subscription.settings.test.tsx`?`tests/subscription.test.ts`?
- ?????`npm test -- tests/subscription.settings.test.tsx tests/subscription.test.ts` ???`npm test` ???43 ??????270 ?????`npm run build` ???`git diff --check` ???????????????? 800 ??
- ?????????????????????????????????????????????? high ?????????????
- Git ?????`fix: ??????????`

## 重构外观主题设置为卡片分组

- 需求：设置 → 外观 → 主题不再使用单个下拉框，改为上方切换“明亮 / 深色 / 跟随系统”，下方按当前明暗模式展示对应主题卡片，主题不再与基础模式同级展示。
- 实现：在 `SettingsAppearanceSection` 中改为 mode tabs 与 palette card grid；扩展 `src/utils/themes.ts` 主题元数据，增加明暗分组、描述资源键与预览色；通过 `systemDark` 在跟随系统时决定展示明亮或深色主题组，同时继续用单字段 `AppSettings.theme` 持久化以兼容旧设置；新增独立 `theme-picker.css` 并清理旧下拉样式；同步中英文主题文案。
- 涉及文件或模块：`src/components/settings/SettingsAppearanceSection.tsx`、`src/components/SettingsPanel.tsx`、`src/app/AppLayout.tsx`、`src/utils/themes.ts`、`src/styles/components.css`、`src/styles/components/settings-page.css`、`src/styles/components/theme-picker.css`、`src/i18n/locales/zh-CN.ts`、`src/i18n/locales/en.ts`、`tests/theme.settings.test.tsx`、`tests/core.test.ts`。
- 验证结果：`npm test -- tests/theme.settings.test.tsx tests/core.test.ts` 通过（2 个测试文件，43 个测试）；`npm test` 通过（44 个测试文件，276 个测试）；`npm run build` 通过；`git diff --check` 通过，仅保留 Git 的 CRLF 提示；活跃代码文件行数扫描无超过 800 行；通过本地 Vite preview 页面核验主题切换与卡片激活状态。
- 已知限制或未处理的无关问题：`src/i18n/locales/en.ts`、`src/i18n/locales/zh-CN.ts`、`src/styles/components/settings-page.css` 仍有 Git 的 LF/CRLF 提示，未在本次任务中统一行尾；本次未再次运行 Rust 检查，因为未修改 Tauri/Rust 代码。
- Git 提交信息：`feat: 重构主题设置为分组卡片`

## 修正主题配色数量

- 需求：设置 → 外观 → 主题卡片中明亮和暗色都需要各 6 个配色，避免明亮组只显示 3 个。
- 实现：将 `catppuccin`、`everforest`、`gruvbox` 调整为明亮 palette，并同步改为浅色 CSS 变量与预览色；暗色组保留 `absolutely`、`ayu`、`codex`、`dracula`、`linear`、`vs-code-plus`；同步中英文主题描述。
- 涉及文件或模块：`src/utils/themes.ts`、`src/styles/variables.css`、`src/i18n/locales/zh-CN.ts`、`src/i18n/locales/en.ts`、`tests/theme.settings.test.tsx`、`tests/core.test.ts`。
- 验证结果：`npm test -- tests/theme.settings.test.tsx tests/core.test.ts` 通过（2 个测试文件，44 个测试）；`npm test` 通过（44 个测试文件，277 个测试）；`npm run build` 通过；`git diff --check` 通过，仅保留 Git 的 CRLF 提示；活跃代码文件行数扫描无超过 800 行。
- 已知限制或未处理的无关问题：未新增 CSS 变量明暗亮度自动检测，当前通过元数据断言、UI 数量断言和人工审查保证一致。
- Git 提交信息： `feat: 重构主题设置为分组卡片`

## 优化订阅布局与头部菜单关闭态

- 需求：优化订阅功能布局样式；主页头部菜单展开后需要显示可关闭图标，点击后收起菜单。
- 实现：将订阅状态卡和方案卡改为更紧凑的网格布局，状态指标先显示标签再显示值，方案列表补充 `role="list"` / `role="listitem"`；订阅样式从 `settings-panel.css` 拆到 `subscription.css` 并由 `components.css` 聚合，避免设置面板样式文件继续膨胀；顶栏新建菜单和主菜单按钮在展开态切换为关闭图标，并补充 `aria-label`、`aria-expanded` 与激活态样式；同步中英文 `topbar.closeMenu` 文案。
- 涉及文件或模块：`src/components/TopBar.tsx`、`src/components/settings/SettingsSubscriptionSection.tsx`、`src/styles/titlebar.css`、`src/styles/components.css`、`src/styles/components/settings-panel.css`、`src/styles/components/subscription.css`、`src/i18n/locales/zh-CN.ts`、`src/i18n/locales/en.ts`、`tests/subscription.settings.test.tsx`、`tests/topbar.menu.test.tsx`。
- 验证结果：`npm test -- tests/subscription.settings.test.tsx tests/topbar.menu.test.tsx` 通过（2 个测试文件，5 个测试）；`npm test` 通过（45 个测试文件，280 个测试）；`npm run build` 通过；`git diff --check` 通过，仅保留 Git 的 CRLF 提示；活跃代码文件行数扫描无超过 800 行；本地 `http://localhost:4173` 预览服务 HTTP 烟测返回 200 和标题 `FkeMark`。
- 已知限制或未处理的无关问题：浏览器工具两次未能挂载，未完成真实可视化截图核验；工作区存在并发/无关修改 `src/components/SettingsPanel.tsx`、`src/components/settings/FlatGroup.tsx`，本次不纳入提交。
- Git 提交信息：`feat: 优化订阅布局与头部菜单交互`

## 优化设置页整体布局与控件密度

- 需求：优化设置页面整体布局、控件密度和滚动观感，让设置页更丝滑、更舒适；保持克制、低噪声、桌面工具感，不改设置模型和业务逻辑。
- 实现：恢复 `FlatGroup` 分组标题与徽标展示；将搜索结果项改为可聚焦按钮并增加分组路径；重整全页面设置模式的导航、搜索、内容滚动、分组卡片、行级密度、焦点态和响应式样式；将快捷键页包入分组并统一列表密度；微调主题 mode tabs 与 palette 卡片的边界、密度和激活态。
- 涉及文件或模块：`src/components/SettingsPanel.tsx`、`src/components/settings/FlatGroup.tsx`、`src/styles/components/settings-page.css`、`src/styles/components/shortcuts.css`、`src/styles/components/theme-picker.css`。
- 验证结果：`npm test -- tests/theme.settings.test.tsx tests/core.test.ts` 通过（2 个测试文件，44 个测试）；`npm run build` 通过；`npm test` 通过（45 个测试文件，280 个测试）；`git diff --check` 通过，仅保留 Git 的 CRLF 提示；本任务触碰文件 BOM 检查均为 no BOM；本任务触碰代码文件均未超过 800 行；本地 `http://127.0.0.1:4173` 预览服务打开设置页，DOM/交互核验显示分组标题、主题卡片与设置页结构正常。
- 已知限制或未处理的无关问题：内置浏览器截图工具在当前会话返回 viewport 宽度异常，未能产出可靠截图；工作区存在无关 `doc/` 修改，本次不纳入提交；全仓库 `src` 行数扫描发现既有 `src/i18n/locales/zh-CN.ts` 与 `src/i18n/locales/en.ts` 各 814 行，非本次触碰，未处理；本次未修改 Rust/Tauri 代码，未运行 Rust 检查。
- Git 提交信息：`refactor: 优化设置页布局密度`

## 优化文档站 Studio Graphite 主题

- 需求：优化 `doc/` 文档站样式，当前主题过于单调，需要选型新主题并插入到文档中。
- 实现：保留 `vitepress-theme-teek` 作为导航、搜索、代码块、文章信息和页脚能力底座，新增 `Studio Graphite` 本地视觉主题层；将全局/正文样式放在 `custom.css`，首页模块样式拆到 `home.css` 并由主题入口导入；同步 Teek 主题色配置为 Studio 铜与编辑器青绿；更新中英文首页、主题说明、快速开始索引、维护 README、可复用 Markdown 预览主题 CSS，并将三张文档 SVG 插图改为暖纸石墨色板。
- 涉及文件或模块：`doc/.vitepress/config.mts`、`doc/.vitepress/theme/index.ts`、`doc/.vitepress/theme/custom.css`、`doc/.vitepress/theme/home.css`、`doc/index.md`、`doc/en/index.md`、`doc/guide/theme.md`、`doc/en/guide/theme.md`、`doc/guide/index.md`、`doc/en/guide/index.md`、`doc/README.md`、`doc/public/theme/fkemark-vellum.css`、`doc/public/images/guide/workflow.svg`、`doc/public/images/guide/settings.svg`、`doc/public/images/guide/toolbar.svg`。
- 验证结果：`npm --prefix doc run build` 通过；`npm test` 通过（45 个测试文件，280 个测试）；`git diff --check` 通过，仅保留 Git 的 LF/CRLF 提示；活跃文档代码文件行数扫描无超过 800 行；本地 `http://127.0.0.1:5179/` DOM/CSS 核验显示 `--vp-c-bg=#f3eee6`、`--vp-c-bg-elv=#fffaf2`、`--vp-c-brand-1=#c96442`，首页 `.paper-card` 样式和 `Studio Graphite 主题` 功能卡生效。
- 已知限制或未处理的无关问题：`npm --prefix doc run build` 仍输出 Teek 内部 `iconfont.* didn't resolve at build time` 运行时解析提示，构建成功，非本次引入；内置浏览器截图工具仍返回页面加载错误，未产出可靠截图，已用 DOM/CSS 核验替代；本次未修改 Rust/Tauri 代码，未运行 Rust 检查。
- Git 提交信息：`docs: 优化文档站主题样式`

## 补充近期功能中英文文档说明

- 需求：把最近新增和调整的功能补充到文档中，并同步更新中文与英文内容。
- 实现：在中英文设置页补充主题卡片选择、工具栏布局、本地订阅与 7 天试用、开发版 DevTools 访问策略；在中英文功能页补充订阅入口、头部菜单关闭态、设置搜索和开发版调试入口；同步首页、快速开始、使用方法和更新日志中的四模式、订阅、主题和近期功能说明；移除已删除行号功能的陈旧文档描述。
- 涉及文件或模块：`doc/guide/settings.md`、`doc/en/guide/settings.md`、`doc/guide/features.md`、`doc/en/guide/features.md`、`doc/index.md`、`doc/en/index.md`、`doc/guide/index.md`、`doc/en/guide/index.md`、`doc/guide/usage.md`、`doc/en/guide/usage.md`、`doc/guide/changelog.md`、`doc/en/guide/changelog.md`。
- 验证结果：过期关键词检查确认文档中不再出现已删除的行号说明或旧三模式描述；触碰文档 BOM 检查为 no BOM；`npm --prefix doc run build` 通过；`git diff --check` 通过，仅保留 Git 的 LF/CRLF 提示。
- 已知限制或未处理的无关问题：`npm --prefix doc run build` 仍输出 Teek 内部 `iconfont.* didn't resolve at build time` 运行时解析提示，构建成功，非本次引入；本次只改文档，未运行 Rust/Tauri 检查。
- Git 提交信息：`docs: 补充近期功能文档说明`

## 修复文档站主题 CSS 评审问题

- 需求：修复文档站 Studio Graphite 主题的浅色代码块对比度、正文卡片溢出裁剪、主题色板中间断点和首页文档链接深色模式对比度问题。
- 实现：将浅色模式 `--vp-code-block-bg` 改为暖纸浅色背景，并显式设置浅色代码块文字、语言标识和行号颜色；将浅色 Shiki 主题切换为 `github-light-high-contrast`；限定 inline code 样式只作用于非 `pre` 代码；保留深色模式代码块背景；将正文内容卡片从整体 `overflow: hidden` 改为仅隐藏横向溢出并允许纵向可见；为 `.theme-token-grid` 补充 960px 双列断点；将首页主文档链接文字色改为主题变量并增加 `.dark` 覆盖。
- 涉及文件或模块：`doc/.vitepress/config.mts`、`doc/.vitepress/theme/custom.css`、`doc/.vitepress/theme/home.css`。
- 验证结果：`npm --prefix doc run build` 通过；`npm test` 通过（45 个测试文件，281 个测试）；`npm run build` 通过；`git diff --check` 通过，仅保留 Git 的 LF/CRLF 提示；浏览器实测浅色代码块背景为 `rgb(245, 237, 227)`、默认代码文字为 `rgb(33, 28, 24)`、首个 Shiki token 为 `rgb(14, 17, 22)`；本次触碰 CSS/配置文件均无 BOM，且行数均低于 800 行。
- 已知限制或未处理的无关问题：`npm --prefix doc run build` 仍输出 Teek 内部 `iconfont.* didn't resolve at build time` 运行时解析提示，构建成功，非本次引入；工作区存在无关 `.github/workflows/code-review.yml` 与 `tests/code-review.workflow.test.ts` 修改，本次不纳入提交；本次未修改 Rust/Tauri 代码，未运行 Rust 检查。
- Git 提交信息：`fix: 修复文档站主题 CSS 评审问题`

## 调整 AI code review 报告等级排序

- 需求：调整 AI code review 流水线，让推送分支生成的审核报告按问题等级排序输出，便于先处理高风险问题。
- 实现：在 `.github/workflows/code-review.yml` 的 `format_ocr_report` 中新增审核块解析与严重等级排序；按 `critical/blocker`、`high/major`、`medium/moderate/normal`、`low/minor`、`info/informational/suggestion`、`nit`、未识别等级的顺序输出，并保持同等级原始顺序；继续保留原有标题格式化和 diff 代码块包裹逻辑。
- 涉及文件或模块：`.github/workflows/code-review.yml`、`tests/code-review.workflow.test.ts`。
- 验证结果：`npm test -- tests/code-review.workflow.test.ts` 通过（1 个测试文件，8 个测试）；`npm test` 通过（45 个测试文件，281 个测试）；`npm run build` 通过；`git diff --check` 通过；触碰代码文件行数均低于 800 行；`.github/workflows/code-review.yml` 文件头 BOM 已检查并移除；迟到复核指出同等级稳定排序测试缺口后，已补充对应回归测试。
- 已知限制或未处理的无关问题：本次只调整 push 报告文件、Actions Summary 和提交评论的报告排序；PR 审查仍使用官方 `alibaba/open-code-review` Action 的内置评论能力，未改其输出顺序。独立复核子代理曾因网络响应提前结束返回失败状态，已根据其部分报告完成补测。
- Git 提交信息：`fix: 调整 AI code review 报告排序`

## 修复文档主题与审核排序评审问题

- 需求：处理评审指出的两个问题：文档站正文卡片 `overflow-x: hidden` 与 `overflow-y: visible` 组合会按 CSS 规范导致纵向变为 `auto`；AI code review 报告等级排序会从代码正文误匹配 `[high]` 等词。
- 实现：将 `doc/.vitepress/theme/custom.css` 中正文卡片横向裁剪改为 `overflow-x: clip`，保留纵向可见；将 `.github/workflows/code-review.yml` 的严重等级识别限制为审核块头部第一条非空元数据标签，并支持 `[bug high]` 与 `[bug · high]` 两种格式；测试中加入正文代码包含 `[high, medium, low]` 和注释 `[major]` 的未知等级块，确保不会被误排到高等级。
- 涉及文件或模块：`doc/.vitepress/theme/custom.css`、`.github/workflows/code-review.yml`、`tests/code-review.workflow.test.ts`。
- 验证结果：`npm test -- tests/code-review.workflow.test.ts` 通过（1 个测试文件，8 个测试）；`npm test` 通过（45 个测试文件，282 个测试）；`npm run build` 通过；`npm --prefix doc run build` 通过；`git diff --check` 通过；触碰文件行数均低于 800 行；`.github/workflows/code-review.yml` 与 `doc/.vitepress/theme/custom.css` 最终 BOM 检查为 no BOM。
- 已知限制或未处理的无关问题：`npm --prefix doc run build` 仍输出 Teek 内部 `iconfont.* didn't resolve at build time` 运行时解析提示，构建成功，非本次引入；本次未修改 Rust/Tauri 代码，未运行 Rust 检查。修复过程中曾短暂引入测试数据字符串语法错误，已在最终验证前修复并重跑全量测试。
- Git 提交信息：`fix: 修复 code review 排序与文档 overflow 问题`
